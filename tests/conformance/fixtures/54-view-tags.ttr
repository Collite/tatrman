schema db

def view active_customers {
    description: "Active customers"
    tags: ["reporting", "core"]
    definitionSql: "SELECT * FROM customers WHERE active = 1"
    columns: [
        def column id { type: int }
    ]
}
