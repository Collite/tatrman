# PL-P7 (⑦) — Zorya + harvest scheduling (stages S1–S2)

> Pre-flight: PL-P2 done (door API); PL-P4 done (principals + policy); PL-P5 done for the harvest wiring. DoD: [`../plan.md`](../plan.md) §PL-P7. ⚠ Pre-generated 2026-07-09 — re-validate against preceding reviews. Both stages `tatrman-platform`. **Zorya is deliberately thin (F-1-γ)**: triggers fire the door, nothing more — calendars/sensors/backfill belong to registered orchestrators; FQ-1 stays parked (watch for scope magnetism, plan.md risk list). Planning note (deviation from the plan's shorthand "cron/manual/upstream-run triggers"): **manual fires are pure door calls and never pass through Zorya** — functionally equivalent, one fewer hop, F-1-γ-cleaner; raise at the phase review if it should be revisited.

## S1 · Zorya — the trigger layer {#s1}

Verify: `./gradlew :services:zorya:test` green; on the local deploy, a `*/2 * * * *` fixture envelope fires twice, under its envelope principal, with correct run ids; restart-during-window behavior proven.

- [ ] **T1 (tests first).** `CronTriggerTest.kt`: `"cron schedules parse + next-fire computed in the envelope's timezone (Europe/Prague fixture crosses a DST boundary)"` (cron-utils, pinned) · `"fire calls POST /v1/runs with {envelope, trigger id, fire-time} — the run id components (FQ-2)"` · `"the run executes under the ENVELOPE's principal; Zorya's own principal only authenticates the door call"` · `"misfire policy is explicit config: skip | fire-once-late — never silent catch-up (P3)"` · `"disabled envelope → trigger inert"`.
- [ ] **T2 (tests first).** `UpstreamRunTriggerTest.kt` (F-4-v): `"program B's upstream-run trigger fires on A's SUCCEEDED event — and ONLY on SUCCEEDED (a handled on-failure run does not fire it)"` · `"at-least-once ingest + fired-marker ⇒ exactly-one fire per upstream run"` · `"cycles (A upstream-of B upstream-of A) rejected at deploy validation"` (add the `PLT-ENV` case + fixture to Radegast's suite).
- [ ] **T3 (tests first).** `TriggerDurabilityTest.kt`: trigger state (next-fire, fired markers) lives in PostgreSQL; `"restart mid-window ⇒ no double-fire, no silent skip beyond the misfire policy"` · `"clock skew tolerance documented + tested at ±60s"`.
- [ ] **T4.** Service skeleton `services/zorya` (house pattern, ingress, its Postgres schema) + the cron scheduler loop (coroutine ticker + cron-utils; one scheduler, leader-elected via a Postgres advisory lock — no second scheduler framework).
- [ ] **T5.** Upstream-run subscription: Zorya tails Veles's ingested RunEvents (`GET /v1/runs?state=SUCCEEDED&since=<cursor>` polling ⚑ — one moving part, latency SZ-class seconds is fine for nightly-grade triggers; note the future push channel). **This task also owns the Veles-side change**: add the `since` cursor param to `GET /v1/runs` (+ its test in Veles's suite — PL-P1.S7.T4 built the route without one). Manual triggers stay pure door calls (header note) — assert in a test that Zorya has no manual code path.
- [ ] **T6.** Deploy integration: Radegast notifies Zorya on envelope accept/disable (or Zorya polls envelope store — pick, record in the service README); expose Zorya's read surface `GET /v1/triggers` (per-envelope next-fire + last-fire status — the S3 deploy panel consumes it); helm + roster.
- [ ] **T7.** Run Verify, check tracker boxes, commit `PL-P7.S1: Zorya trigger layer (cron, upstream-run, manual-by-door)`.

## S2 · Harvest scheduling + OpenMetadata import + stats harvest {#s2}

Verify: `./gradlew :services:veles:test :connectors:...:test` green; on the local deploy: a schema change in the fixture Postgres produces a **proposal PR**; stats rows appear in the BQ-2 store and change an optimizer decision on the next fetch+compile; OM import round-trip green in the conformance lane.

- [ ] **T1 (tests first).** `RefreshOrganTest.kt` (Veles): `"configured connector cadences fire harvest() through the scheduling organ (the PL-P1.S9 manual-trigger seam goes scheduled)"` · `"export() runs on its own cadence too — the organ drives BOTH connector directions (design §5: the refresh organ drives connectors; OM export must not stay manual-only)"` · `"connector failure → recorded, alerted, next cadence unaffected"` · `"concurrent harvest of one connector prevented (mutex per instance — MetadataRefresher discipline)"`.
- [ ] **T2 (tests first).** `PgHarvestConnectorTest.kt` (`ttr-connect-postgres`): `"INFORMATION_SCHEMA fixture → world/model document PROPOSAL (branch + PR against the platform-world repo), reusing the S4 import-schema mapping code verbatim (I-2: same mapping, two packagings)"` · `"same external state ⇒ same proposal bytes; re-run with no change ⇒ NO new PR (I-3)"` · `"proposals attributable to the connector's service principal"` · `"stats output kind: row counts/sizes land DIRECTLY in the BQ-2 store as §4 entries (no review, by temperament)"`.
- [ ] **T3 (tests first).** `OmImportTest.kt` (extends `ttr-connect-openmetadata`): `"OM table entities fixture → TTR document proposals under the IQ-2 mangling rules with source-name properties"` · `"import NEVER touches objects TTR already owns (one writer per object — I-1)"` · determinism replay.
- [ ] **T4.** Implement the refresh-organ scheduling for **both directions** (harvest + export cadences per connector instance in server config — the OM export goes scheduled here), the `ttr-connect-postgres` harvest connector (both output kinds), and the OM import half (capability `HARVEST_SCHEMA`).
- [ ] **T5.** Git plumbing for proposals: the connector frame gains the branch/PR writer against the platform-world/project repos (`ttr-metadata-git` for the commit; the PR via the forge API from deployment config) — robots write through git; **never auto-merge** (assert no merge call exists in the frame).
- [ ] **T6.** Close the loop drills: merge a fixture proposal → Veles refresh picks it up → `ttr fetch` shows the lock diff (K/BQ-3's "the platform's view changed" made literal); stats harvested → next compile's record shows them in `statsUsed`. Record both transcripts.
- [ ] **T7.** Run Verify, check tracker boxes, commit `PL-P7.S2: harvest scheduling + pg/OM import connectors + stats harvest`.
