# PL-P5 (E track) — Airflow 3 support package (stages S4–S5)

> Pre-flight: S1–S2 done (SPI + kit); S4's native half is MIT-only, but the door-calling half and all of S5 *(need door)* wait for PL-P2. DoD: [`../plan.md`](../plan.md) §PL-P5. ⚠ Pre-generated 2026-07-09 — re-validate against preceding reviews. S4 is `tatrman`; S5 is `tatrman-platform`. The two DAG shapes must never blur (E-3): **door-calling = a frontend** (hall intact, policy holds); **native = an emit target** (no platform on the runtime path, credential-bounded).

## S4 · `ttr-emit-airflow3` — both bindings {#s4}

Verify: `./gradlew :packages:kotlin:ttr-emit-airflow3:test` green; `ttrp conform emit-determinism --plugin org.tatrman:ttr-emit-airflow3` passes; goldens for both bindings pinned.

- [ ] **T1 (tests first).** `Airflow3DoorCallingTest.kt` goldens (platform-world compile): hero → one Python DAG file — `"a single whole-program task (E-3-α-1): obtains a client-credentials token from the PLATFORM IdP's token endpoint (Keycloak, H-2-ii — NOT Airflow's own /auth/token, which is S5's business), then POST <door>/v1/runs and polls GET /v1/runs/{id} to terminal state"` · `"door base URL + IdP token URL + principal client-id all come from ONE Airflow Connection ref named by the world instance entry — never inline in the DAG file"` (B-4: the adapter half may know platform URLs; secrets never) · `"cancel propagates: task kill → POST /v1/runs/{id}/cancel"`.
- [ ] **T2 (tests first).** `Airflow3NativeTest.kt` goldens (standalone-world compile): `"one task per island, wave order as task dependencies, islands invoked per the F-lite bindings (BashOperator psql / PythonOperator polars)"` · `"TTR_CONN_* sourced from Airflow Connections/env on the user's side — no platform reference anywhere in the file"` (H-8 credential-bounded line — assert textually).
- [ ] **T3.** Implement `org.tatrman:ttr-emit-airflow3` (`targetId = "airflow3"`; code-defined target ⇒ the heavier determinism burden — generate from string templates with zero ambient input, no f-string timestamps, stable task-id derivation from island names).
- [ ] **T4.** Binding selection is **world-driven** (E-3-γ): the resolved executor *instance* entry carries `delegation: door-calling | native`; platform worlds declare door-calling, standalone worlds native; the plugin refuses a platform world without a door binding (P3-explicit).
- [ ] **T5.** Ship the Airflow 3 executor-type manifest (control [fs, ss] via dependencies; events [cron, external] via Airflow schedules; invocation via operators; manifestSchema accepts {1,2}); one type manifest — MWAA/Composer/Astronomer are instance overlays (EQ-3 note in the manifest's doc-comment).
- [ ] **T6.** Determinism-kit CI wiring for the plugin; run Verify, check tracker boxes, commit `PL-P5.S4: ttr-emit-airflow3 (door-calling + native)`.

## S5 · `ttr-connect-airflow3` — run harvest *(needs door + PL-P2 event spine)* {#s5}

Verify: `./gradlew :connectors:ttr-connect-airflow3:test` green; the conformance-lane job proves a delegated hero run **indistinguishable from a native run** in Veles's run + lineage views.

- [ ] **T1 (tests first).** `AirflowHarvestMappingTest.kt` goldens: Airflow REST fixtures (`GET /api/v2/dags/{id}/dagRuns` pages) → `event.v1` batches — **for door-calling (α-1) runs the connector emits ORCHESTRATOR-CONTEXT events only** (AuditEvent-shaped: dagRun id, logical_date, Airflow state, citing the door-minted run id from the DAG's conf) — `"the run's TRANSITION history stays Radegast's alone: the connector never emits RunEvent transitions for a door-minted run id"` (Radegast's outbox is the one state emitter; a second emitter would duplicate + reorder the spine) · `"harvest is idempotent: re-polling emits no duplicate event_ids"` · `"impedance room: Airflow logical_date lands in trigger context per FQ-2's rider"`. (RunEvent-transition mapping is reserved for future native-run harvest — leave the mapping table stubbed with a `// post-v1: native-run harvest` marker.)
- [ ] **T2 (tests first).** `HarvestDeterminismTest.kt` (I-3 discipline applied to events): same fixture Airflow state ⇒ same event batch bytes; connector auth via `authRef` → SecretRef only.
- [ ] **T3.** Implement `ttr-connect-airflow3` on the Connector SPI (contracts §18, capabilities `{HARVEST_RUNS, EVENT_SUBSCRIPTION}`): poll the Airflow 3 REST API under a client-credentials principal; map per T1; hand batches to Veles ingest.
- [ ] **T4.** Registration fixtures: the Airflow environment-instance world entry (E-2 — `extends` the plugin's type manifest, connection ref only) checked into the platform-world fixture repo; document that registering = this entry, nothing else (the wizard renders it at ⑦).
- [ ] **T5.** Conformance-lane job: dockerized Airflow 3 (pinned tag) + the local platform stack — deploy the S4 door-calling DAG, let Airflow fire it, harvest back, assert: run visible in Veles with lineage citing the manifest section, its **transition history identical in shape to a natively triggered run's** (single emitter — T1's rule), plus the harvested orchestrator-context events attached.
- [ ] **T6.** External-event seam: the connector's `EVENT_SUBSCRIPTION` capability registers as a trigger *source* stub (`// post-v1: external events via connector EVENT_SUBSCRIPTION — F-4 defers them; no v1 phase consumes this`); document in the connector README.
- [ ] **T7.** Run Verify, check tracker boxes, commit `PL-P5.S5: ttr-connect-airflow3 run harvest`; request the PL-P5 `/review`.
