# Platform Frontends — Control Room

> The single dashboard for the **Platform Frontends** design effort: giving the Tatrman Platform its own product surfaces — a **BI/analysis frontend** over TTR-P results and an **entry/planning frontend** over MD-model cubelets (summary-level entry, spread down) — after the platform design (A–K, 2026-07-08/09) deliberately left Iris and Sysifos on the Kantheon side.
> Open this first every session. Companion docs: [`01-design-space-map.md`](./01-design-space-map.md) (the branches); option docs `02+` as workstreams deep-dive.
>
> **Status:** ✅ **EFFORT COMPLETE (2026-07-10).** All workstreams A–F 🟢; sweep S-1..S-6 ratified (S-2 amended: technical names ecosystem-wide — entry product = `tatrman-entry`); final deliverables written: [`../design.md`](../design.md) (for `/planning`) + [`../detailed-design.md`](../detailed-design.md) (the manual) + [`prototypes/c-entry-grid-v0.html`](./prototypes/c-entry-grid-v0.html) (normative illustration). **Next step: a `/planning` session consuming `design.md`.**

---

## 0. How we run this

Same protocol as the TTR-P and Platform efforts (`../../ttr-p/design/00-control-room.md` §0, `../../platform/design/00-control-room.md` §0): **diverge before we converge**; three gears (Framing → Divergence → Convergence); ≥3 alternatives per fork including the weird one; leans are not decisions; deferred is a tracked outcome; the append-only decision log is ground truth.

Cross-effort citations use the source prefix: **`PL`** = the platform design effort (e.g. `PL FI-3`, `PL D-3`, `PL Q-4-a`, `PL G-1`), `TTR-P`, `MD`n (ttr-metadata), `TR`-n (ttr-translator). This effort's own IDs are bare letters; other efforts cite them as `PF <id>`.

---

## 1. Workstream dashboard

Status legend: ⚪ not started · 🔵 diverging · 🟡 options captured · 🟢 converged/decided · 🟠 converged-but-dirty · ⏸ parked

| # | Workstream | Status | Core question | Notes |
|---|---|---|---|---|
| **A** | Product shape & placement | 🟢 | How many surfaces (one workbench / two apps / Designer Extensions), for whom, and on which side of the license line? | **Converged 2026-07-09 → the SPLIT VERDICT** ([`05-product-shape-options.md`](./05-product-shape-options.md)): analysis (B-read viewer + B-author) = **Designer Extensions**; entry = **its own standalone platform product**; **Excel add-in = recorded future alternative client** of D's contracts; both surfaces `cz.tatrman` (no new MIT frontend); planners never author canon (config-vs-use line). Resolves LF-1 + LF-6. |
| **B** | The BI/analysis surface | 🟢 | Buy/embed an OSS BI vs build on the query door vs reuse Iris's render pipeline — and what does "interactively generate TTR-P" mean as a product? | **Converged 2026-07-10** → [`02-bi-surface-options.md`](./02-bi-surface-options.md) (+ §6 BQ-6 verification): **B-read = the δ-composite** (semantic projection md→Cube/OSI · α-3 bring-your-BI, Superset reference pairing · thin native viewer as Designer extension w/ lineage-aware drill) · **B-author = native Designer extension** · **R fork = R3** (semantic layer as policy carrier; R2 rejected; R1 parked/BQ-1) · BQ-4 = ttrl split (shared views canon, personal in prefs) · BQ-5/Q-1 = interactive + saved views v1, packs parked. LF-2 resolved. BQ-2/BQ-3 → cross-effort/planning. |
| **C** | The entry/planning surface (UX) | 🟢 | The cubelet grid: how a planner enters at summary level, spreads down, overrides, locks, re-spreads, and commits — versions and entry workflow included. | **Diverging 2026-07-10** → [`06-entry-grid-options.md`](./06-entry-grid-options.md) (thread 1: grid core) + working prototype [`prototypes/c-entry-grid-v0.html`](./prototypes/c-entry-grid-v0.html). Grid brief = GI-4..GI-9; three-grains clarification. **Thread 1 (grid core) CONVERGED 2026-07-10:** CL-β pin-sum cascade (CQ-1 = error-only, IPF parked) · immediate per-gesture recalc · vocabulary v1 (formulas v1.5) · host = custom model + thin grid (stack → F) · auto-lock = user toggle · non-additive out of v1 · **LOCK-FREE COMMITTED PASS** (amends E wording + D-iii) · GI-9 loaded-scope. **Thread 2 (commit/preview) 🟢 substantially converged 2026-07-10** → [`07-commit-preview-options.md`](./07-commit-preview-options.md) + prototype v0.7: **C-T14 CHECK-OUT/CHECK-IN (amends D-iv)** · T8 β preview · T10 β journal-drafts (values-only v1) · T11 α whole-batch · explicit edit toggle · CQ-14 admin-unfreeze v1. Finding: **journal = two row kinds (seed | assignment), seeds-first** — D contract refinement. **CQ-15 resolved: loaded-scope product-slice reservations, member-set overlap, snapshot-at-acquisition.** **Thread 3 (form vocabulary) 🟢 CONVERGED 2026-07-10** → [`08-form-vocabulary-options.md`](./08-form-vocabulary-options.md): **the form = a `ttrl`-family layout document, authored canon** ("this is exactly it"); minimal core + **parameterized version**; validation catalogue approved; demand spec split ttrl-schema (GQ-1) vs TTR-M package. **Q-4, Q-5, Q-7 RESOLVED; CQ-4 shaped** (pivot state = ttrl content in prefs). Residual opens: CQ-8, 13, 16..18 minor (planning-stage); CQ-19/20 = cross-effort coordination. **Q-3 RESOLVED 2026-07-10 → α+γ "the minimal round"** ([`09`](./09-workflow-options.md)): round = version lifecycle · read-only round dashboard · done flag · note field (CQ-12 ✓) · drafts private (CQ-10 ✓) · roles = admin+git convention; submit/approve parked. **ALL C THREADS CONVERGED → 🟢.** |
| **D** | The data-write path | 🟢 | Where do entered numbers *land*? The platform has two doors (query, program) and git for canon — a budget cell is none of these. Third door? Entry-as-program? Direct write? | **Converged 2026-07-09** → [`03-write-path-options.md`](./03-write-path-options.md): **γ-front + β-ii-back composite** — entry journal beside the cube (draft/session/audit substrate) + commit = run of the standing **canon entry-apply program** (table-valued typed input) + **entry record** provenance; D-i journal semantics, D-ii Argos-symmetric write RLS (PL amendment), ~~D-iv cell-LWW + advisory claims~~ **D-iv AMENDED 2026-07-10 → check-out/check-in reservations (C-T14)**, D-v version metadata in md. DQ-1/2 + reservation registry = named PL amendment proposals; DQ-3..5 → planning-stage if effort graduates. |
| **E** | Spreading & allocation semantics | 🟢 | Where does spread/split logic live — frontend, a platform service, or **the TTR-P language itself** — and what is the operation vocabulary (proportional/even/profile/driver, locks, rounding)? | **Converged 2026-07-09 → the TWO-PASS MODEL (Bora's amendment)**: visible pass = in-memory working view, rich vocabulary non-authoritative (→ C); invisible committed pass = **single mechanism: proportional-over-unlocked-remainder**, zero base = ERROR, existing support only, additive measures v1, rebase-on-commit; placement γ (one TTR-P construct) + δ ergonomics; defaults in TTR-M md. → [`04-spreading-options.md`](./04-spreading-options.md) resolution. |
| **F** | Reuse, stack & contracts | 🟢 | What is actually reusable from Iris / Sysifos / report-renderer / the Designer given `PL D-3` ("kantheon owns nothing shared") and the Vue-vs-React split; which contracts must move or be re-owned? | **Converged 2026-07-10** → [`10-stack-reuse-options.md`](./10-stack-reuse-options.md): **F-1 = α React everywhere platform-side + γ pure-TS-core discipline** (FQ-1: "React is fine"); **F-2 = α** kit family split on the `PL A-1` MIT line, framework-free core; **F-3 = F-γ** (envelope question dissolved; charts = Vega-Lite-as-library); **F-4** Iris/Sysifos DNA-only + OTel pattern. **LF-5 RESOLVED by dissolution.** Rider: **Iris+Sysifos→React = kantheon-side initiative proposal** (parked with handoff); framework-free kit consumable by Vue apps today (`PL D-3` downstream). Naming → sweep (FQ-3). |

---

## 2. Framing inputs (Bora, 2026-07-09 — FIXED)

- **FI-1 · The gap is real and named.** The platform design (`PL` C-0 inventory) left *all* product frontends on the Kantheon side; the Designer is a metadata/ops surface (catalog, graphs, runs, lineage), not a data product. After the split, **the Tatrman Platform has no frontend for its own output**. This effort fills that gap.
- **FI-2 · Two product needs.** (a) A **BI frontend** to see the results of TTR-P transformations, *and* to interactively author/generate the TTR-P that produces them; (b) a **data-entry frontend** ("post-Sysifos") over MD-model cubelets, with **splitting/spreading as the core capability** — planning & budgeting style: enter on a summary level, spread down.
- **FI-3 · Iris and Sysifos both stay in Kantheon — for different reasons.** Iris is agent-coupled (chat surface over Themis dispatch — intelligence, `PL FI-3`). Sysifos has **no intelligence** (its own design doc: BFF has "no business logic"; all derivation in Midas-core) but is **Midas-coupled** — its only backend is Midas-core's book of record. The platform entry surface is a **sibling** of Sysifos (same UX DNA), not a move or rename of it.
- **FI-4 · Effort home & timing.** New effort folder (this one), separate from the closed `PL` control room. **Design-ahead, build post-v1:** full freedom to *propose* amendments to platform contracts, but the PL-P0–P7 plan is untouched except by explicit amendment PRs; the frontends build after (or late in) the strangler ①–⑦.
- **FI-5 · Surface count is a fork, not framing.** One workbench vs two apps vs Designer Extensions is workstream A's LF-1, decided consciously.

### Grounding inputs (diverge-worthy, not decisions)

- **GI-1 · "So many OSS BI solutions that developing another one seems overkill"** (Bora). Buy/embed leans first for B — but gets ≥3 recorded alternatives like everything else.
- **GI-2 · "Maybe we can reuse a lot of Iris and Report Renderer?"** (Bora). Recorded as a branch of B/F — with the `PL D-3` ownership problem stated (see §4 LF-5).
- **GI-3 · Spreading is the entry surface's *point*.** Not a nice-to-have: summary-level entry with spread-down (planning/budgeting) is the differentiating capability vs a generic CRUD grid.

**The grid brief (Bora, 2026-07-10 — grounding for C's grid core):**

- **GI-4 · The view/form defines the load grain and the pivot surface.** Data comes from a TTR-MD-defined cubelet; the view declares the load grain (may be an *aggregate* of the cubelet), the available hierarchies (**max one per dimension**), and the initial layout (shown/hidden, rotation). The grid gives Excel-pivot manipulation (header filters, attributes+hierarchies on rows/columns) with (sub)totals per available hierarchy.
- **GI-5 · Entry semantics: leaf = overwrite, summary = spread; all in memory.** Entering at a view leaf overwrites; entering at a view summary spreads. Every operation affects only the in-memory copy of the view (re-affirms E's visible pass).
- **GI-6 · Locks anywhere; infeasible = error.** The planner can lock any cell — leaf, subtotal, or grand total. A locked cell cannot change; operations affect the others. If the lock set makes an operation impossible to proceed, that is an **error**.
- **GI-7 · v1 spreading = two proportional mechanisms:** one for SUM-aggregated measures (new total spreads proportionally over all unlocked leaves) and a different one for non-aggregating measures (prices). ~~⚠ *Tension with E's additive-only-v1 scope cut — CQ-3 in `06`, needs an explicit call.*~~ **Resolved 2026-07-10: Bora retracted the non-additive half — v1 = SUM-able measures only; E's cut stands (see log).**
- **GI-8 · Rich ops named for v1+:** ephemeral session-scoped formulas (never stored); block select + operation (the hero: locked row total + "decrease H1 by 10 %" ⇒ H2 catches up proportionally); pattern/seasonality-curve apply to a block.
- **GI-9 · The FE operates on *loaded* scope, not *visible* scope (2026-07-10).** Collapse **hides** rows/columns but they stay **loaded**; spreads and lock compensations run over the full load grain, including hidden cells (the journal shows them). Display grain is presentation only.

---

## 3. What we already have (asset inventory)

- **The query door (Theseus, `PL C-3-γ`)** — synchronous single-validated-plan door, built for "agents, Designer previews, debug", interactive priority at Kyklop (`PL F-5-γ`), caller-principal identity (`PL H-2`). **The BI read path exists by construction.**
- **The Designer + Extensions surface (`PL Q-4-a`, `PL G`)** — MIT React 19 frontend, backend-selectable; platform-shipped panels on an MIT-defined extension surface; reader-first v1 (catalog, graphs, runs, column lineage); edit mode designed (writes-through-git). A candidate *home* for BI panels — and a competing option to any standalone app.
- **TTR-M `md` modeling + `@tatrman/md-catalog`** — the MD calc-map catalog (`MD_CALC_CATALOG`); cubes/dimensions/hierarchies are already language citizens. The entry surface's "cubelet" has a modeling-language substrate to bind to.
- **TTR-P + the E-5 manifest** — programs, static column lineage, read-only program graphs already rendered in the Designer. "Interactively generate TTR-P" composes with the existing compile/validate path (browser-worker LSP exists).
- **Sysifos design DNA** (`kantheon/docs/design/sysifos/sysifos-design.md`) — propose-don't-write; preview→commit; single form + paste-friendly bulk grid; hybrid sync/Draft+SSE write model; three-layer validation (FE Zod ← BFF pre-flight ← core authority); quick-create-mid-entry. Proven shape for correct-and-fast entry.
- **Iris render pipeline** (`kantheon/frontends/iris`) — Vue 3 + PrimeVue + **Vega-Lite** chart rendering, `envelope/v1` block model (text/table/chart + drilldowns + chips), dockview workspace. Technically mature; **kantheon-owned** (see LF-5).
- **report-renderer, kallimachos, pinakes** — swept "stays kantheon" (`PL D-6`); pinakes = verify-then-place in ②.
- **The platform contract set** (`PL` plan/contracts.md v1.1) — envelope, run/lineage/audit events, door API, snapshot archives, `ttr.lock`, policy bundles. The frontends *consume* these; gaps become amendment proposals (FI-4).

---

## 4. Load-bearing forks (decide consciously, not by drift)

1. ~~**LF-1 · Surface topology.**~~ **RESOLVED 2026-07-09 → the split verdict: analysis = Designer Extensions; entry = own standalone product; Excel add-in = future alternative client (A).**
2. ~~**LF-2 · BI make-or-take.**~~ **RESOLVED 2026-07-10 → the δ-composite:** semantic projection (md→Cube/OSI) as the governed substrate · R3 policy carrier · bring-your-BI (Superset reference, PowerBI) · thin native viewer + B-author as Designer extensions. Neither pure make nor pure take — the layer is ours, the heavy clients are theirs, the differentiating viewer is small.
3. ~~**LF-3 · The write path for entered data.**~~ **RESOLVED 2026-07-09 → journal-front + standing canon entry-apply program + entry record (D = γ+β-ii composite).**
4. ~~**LF-4 · Where spreading lives.**~~ **RESOLVED 2026-07-09 → the two-pass model: rich tooling in the in-memory working view (non-authoritative, → C); committed pass = single proportional TTR-P construct (E = γ+δ).**
5. ~~**LF-5 · Reuse vs re-own.**~~ **RESOLVED 2026-07-10 by dissolution (F):** nothing is reused in place, nothing needs re-owning — typed door results + the grid model + Vega-Lite-as-library removed every envelope consumer; Iris/Sysifos carry design DNA and spec knowledge only; the platform surfaces stand on tatrman-side code and published contracts. React everywhere platform-side; framework-free kit core.
6. ~~**LF-6 · License & repo placement.**~~ **RESOLVED 2026-07-09 → both surfaces platform-side `cz.tatrman`; analysis extensions ride the MIT extension surface (`PL D-2` pattern); no new MIT frontend (A).**

---

## 5. Design principles (P-n; cite by ID)

- **P1 · Inherited: `PL P1–P3` + `PL B-4` apply verbatim.** Standalone is not a demo; one-way arrow; no miracles; seam legality.
- **P2 · Deterministic surfaces. (candidate — ratify)** Platform frontends contain no LLM/agent intelligence; NL analytics stays Iris/Kantheon (`PL FI-3`). If a surface wants "ask in natural language", that is a kantheon integration point, not a platform feature.
- **P3 · Propose, don't write. (candidate — ratify)** Every entry surface assembles a proposal, previews server-derived effects (spread results, validation), and commits explicitly; the frontend is never the authority. (Sysifos DNA, generalized.)
- **P4 · Spreads are explainable. (candidate — ratify)** Every derived number can show *how it was derived* (method, driver, locks honored, rounding) — P3-no-miracles applied to allocation.

---

## 6. Hero scenario (carried through every workstream)

**"The FY2028 OPEX budget."** MD model: cube `opex` — measure `amount`; dimensions `account` (hierarchy), `cost_center` (company → division → department → cc), `month` (12 + quarter/year rollups), `version` (ACT / BUD / FCST).

1. **Enter.** A planner opens the entry surface on the `opex` cubelet (version `BUD2028`), enters **Sales division, FY total = 120 M CZK** at division × year level.
2. **Spread.** The system spreads down to department × account × month **proportional to FY2027 actuals** (driver = `ACT2027` slice). The planner overrides one leaf — March marketing campaign, +2 M — **locks it**, and re-spreads the remainder; totals still reconcile to 120 M. Every cell can explain its derivation (P4).
3. **Commit.** Preview shows the derived cell set; commit lands the entries through the platform write path (LF-3) under the planner's principal, RLS-checked, audited; version `BUD2028` gains a new state.
4. **Analyze.** An analyst opens the BI surface: budget vs FY2027 actuals variance, pivots department × quarter, drills to account, charts the bridge — reads ride the query door. She then interactively authors a small TTR-P transform (variance bridge as a derived table), sees results live, and saves it — the program can graduate to a deployed bundle later.

Every workstream renders its options against this scenario.

---

## 7. Decision log

> Append-only. Format: `YYYY-MM-DD · [id] · Decision · Why · Alternatives rejected`.

- **2026-07-09 · [FRAME] · FI-1..FI-5 accepted as fixed framing** (the gap; two product needs; Iris agent-coupled + Sysifos Midas-coupled both stay kantheon, entry surface = sibling not move; new effort folder, design-ahead/build-post-v1; surface count = fork). · Bora's opening statement + framing Q&A, this session. · Alternatives (reopening the PL control room; pushing frontends into v1 scope) consciously rejected in the same Q&A.

### D / write path + E / spreading (options + resolutions in `03-` / `04-`)

- **2026-07-09 · [D = γ+β-ii composite] · THE ENTRY WRITE PATH: journal-front + standing-program-back.** The surface writes to an **append-only entry journal beside the cube** (draft/session/audit substrate); **commit = a run of the standing, canon `<cube>-entry-apply` TTR-P program** with the journal batch as table-valued typed input; the version slice is its materialization; every commit produces an **entry record** (compile-record sibling: principal, assignments, method params, locks, leaf-set hash, run ref). Rides the whole existing spine (Argos, Kyklop, quota, run store, events, lineage). · Why: program materialization is the platform's one existing data-plane write mechanism; the composite keeps interactive feel (journal) without a second authority. · Rejected: α full third door (reduces to the missing door verb + session endpoints once the composite is seen); β-i ephemeral program-per-commit (run pollution; unreviewed generated text as authority); δ direct BFF write (bypasses the spine — floor).
- **2026-07-09 · [D-i/ii/iv/v] · Write-semantics sub-forks:** journal + materialized current state (rejected: versioned overwrite — thin audit); **RLS-on-write = Argos-symmetric write predicates** with door-PEP coarse check in front (a `PL H` amendment proposal); concurrency v1 = cell-LWW over the journal + advisory slice claims (G-1-γ's presence idiom at the data plane) + **rebase-on-commit** (leaf-set-hash drift ⇒ re-preview, never silent re-portioning); version protection (ACT read-only, BUD open-then-locked) = **TTR-M md version-dimension metadata** (canon), enforced at the write check. Scenario-as-data-branch → parking lot.
- **2026-07-09 · [E = TWO-PASS MODEL] · Spreading splits into a visible and an invisible pass (Bora's amendment — supersedes the single-pass lean).** **Visible pass:** the planner works in an **in-memory working view** (the cubelet aggregated over declared dims to displayable size); ALL rich manipulation mechanisms (proportional, even, profiles, driver-based, manual+re-spread, …) live here as **non-authoritative tooling** — the user ratifies the resulting summary assignments (their vocabulary design moves to **C**). **Invisible pass (committed):** ratified assignments → underlying leaves by **exactly one mechanism: proportional-over-unlocked-remainder against the current base**, rounding = largest-remainder with stable tie-break. · Why: bounds the interactive problem to displayable size; dissolves the client-authority tension (ratified output ≠ trusted computation); shrinks the committed semantics — and the TTR-P demand — to one deterministic construct. · Rejected: rich-vocabulary-as-committed-semantics (the original lean — more language surface for no authority gain); E-α authoritative client (P3/P4); E-β service (semantics as unreviewable service code); E-ζ no-spread floor.
- **2026-07-09 · [E-zero/null] · Zero base = ERROR; existing support only.** If the base under an edited summary cell sums to zero, the spread **refuses** (P3-explicit) — the planner opens a finer view or **seeds explicitly** (seed = a leaf-level copy operation, e.g. `BUD2028 := ACT2027`, a distinct C-level entry operation involving no spreading). The invisible pass distributes only over **existing leaf combinations**; creating combinations is a separate explicit act (no silent densification). · Rejected: seed-automatically + even fallback (miracle-shaped); even-fallback-only (flat distributions masquerading as decisions); dense-over-enterable-region (silent densification).
### A / product shape (compact catalogue in `05-`)

- **2026-07-09 · [A = the SPLIT VERDICT] · Analysis = Designer Extensions; entry = its own product; Excel add-in = future client.** (1) B-read's thin viewer + B-author ship as platform extensions on the `PL Q-4-a` surface — the Designer is the platform's single pane of glass for everyone who reads and builds; (2) the entry surface is a dedicated, deliberately simple standalone platform product (planner persona, round rhythm, journal sessions; Slavic name parked → J register); (3) **A-δ Excel add-in recorded as a future alternative client** of D's client-agnostic entry contracts (parked: post-v1 planner-adoption demand); (4) **LF-6: both surfaces `cz.tatrman`** — extensions ride the MIT extension surface (`PL D-2` pattern), the entry app is "operate" (`PL A-1`); no new MIT frontend; (5) config-vs-use line: **planners never author canon** — entry-form/working-view declarations are modeling-time canon, authored IDE/Designer-side (details → C's Q-7). Resolves **LF-1 + LF-6**. · Why: D made frontends thin clients; B's decomposition made both analysis halves extension-shaped; personas differ hard on the entry side. · Rejected: β one-workbench (persona collision); pure γ (planning inside metadata chrome); pure α (BI shell duplicates the Designer).

- **2026-07-09 · [E-placement = γ+δ, scope cuts] ·** The one committed construct is a **TTR-P language capability** (MIT per `PL A-1`, engine-executable, reviewable text — the demand spec to the TTR-P effort shrinks to this single construct + error semantics); the platform ships the **deterministic fragment generator** (δ ergonomics) so surfaces never string-build TTR-P. **Additive measures only in v1** (non-additive entry → parking lot). Spread/driver defaults declared in **TTR-M md** (E-i), overridable per entry, recorded in the entry record. **Consequence: “drivers” (e.g. ACT2027) are visible-pass tooling + seed sources — the committed pass has no driver concept (its base IS the current slice).** · Resolves EQ-2 (over-constrained = error always) and EQ-5 (base = current state at commit + rebase-on-commit); EQ-4 (echo latency) moves to C.

### C / grid core (options in `06-`; prototype `prototypes/c-entry-grid-v0.html`)

- **2026-07-10 · [LOCK-FREE COMMITTED PASS] · Locks and formulas are frontend-only, ephemeral, never stored anywhere — the backend spread is always lock-free.** (Amends the *lock clause* of E's committed-mechanism wording and drops **locks from the D-iii entry record field list**; everything else in D/E stands.) Locks do all their work in the visible pass; because ratified assignments live at load grain and load-grain cells partition the storage leaves, each assignment spreads within a **disjoint** storage region — the "unlocked remainder" clause was vacuous at commit anyway. The committed construct shrinks to: *per assignment, scale the existing storage base beneath it proportionally to the assigned value; zero base = error; largest-remainder rounding* — the TTR-P demand spec gets simpler still. Committed derivations remain fully replayable from (assignments + leaf-set hash + program version) without lock knowledge; the visible-pass op log is session ephemera. **Note:** D-iv's *advisory slice claims* are concurrency presence, not value locks — untouched by this amendment. · Bora, 2026-07-10 ("locks and formulas are only FE things; take the stored lock out"). · Rejected: locks-in-entry-record (audit dead weight — they cannot affect committed math); persistent lock objects (never demanded; would drag FE session semantics into canon).
- **2026-07-10 · [C-CQ-3 = Q-8] · Non-additive spreading is OUT of v1 — E's additive-only cut stands.** v1 spreads SUM-able measures only; GI-7's second mechanism retracted by Bora; non-additive entry stays in the parking lot (revisit: first rates/prices entry demand; option (c) visible-pass-only re-level recorded in `06` §6 as the likely shape when it comes). · Bora, 2026-07-10.
- **2026-07-10 · [C-CQ-2] · Auto-lock-on-manual-edit = a user toggle, not fixed behavior.** "Sometimes I want the locks, sometimes I do not" — both modes are legitimate planner intents; the prototype's toggle graduates to a product setting (per-user preference, session-sticky; exact persistence home rides CQ-4). · Bora, 2026-07-10, after seeing the prototype. · Rejected: always-on (silent lock accumulation → mystery over-constraints); always-off (typed values washed away by later spreads with no recourse but manual locking).
- **2026-07-10 · [C-T2 = CL-β] · Lock semantics = pin-sum + compensation cascade.** A lock pins the cell's aggregate; contents may reshuffle; gestures are atomic (apply assignments → reconcile violated locks smallest-region-first, user-touched cells excluded → error + full rollback if it can't converge). Carrying rules: uniform scaling of unlocked leaves preserves proportionality at every level; outer compensations freeze satisfied inner-lock regions. **CQ-1 v1 answer folded in: crossing locks that oscillate = error-only** (honest per P3); IPF recorded as the escalation path → parking lot (revisit: real planning rounds hit the rejection often). All FE-only per the lock-free-committed-pass decision. · Bora ratified the lean 2026-07-10; prototype-verified (16 assertions). · Rejected: CL-α freeze-contents (hero-refuted — makes lock-total-then-rebalance impossible); CL-γ global solver as v1 (P4: "the solver found the nearest feasible point" is not a planner-explainable derivation).
- **2026-07-10 · [C-T3] · Immediate per-gesture compensation.** The grid is always reconciled: compensation runs inside the gesture (batch ops = ONE op, one compensation); per-gesture undo over the op log. · Bora ratified the lean. · Rejected: deferred balance action (grid shows wrong totals between balances — untrustworthy summaries); refuse-edits-needing-compensation (kills the hero's H1/H2 interaction).
- **2026-07-10 · [C-T4] · Visible-pass vocabulary v1 pinned:** typed entry with shorthand (`120M`/`450k` absolute, `+2M`/`-500` delta, `±x%` relative) · summary entry = proportional spread over unlocked view leaves · block select + scale (locked cells skipped-with-count; direct edit of a locked cell = hard error) · curve/pattern apply (each row's block total preserved, locks honored) · **seed as a first-class gesture** (explicit leaf-copy from a named source slice, fills empties only, reachable from the zero-base error). **Ephemeral formulas → v1.5** (CQ-6 scope question stands: view-cell refs vs driver-slice refs, syntax home). · Bora ratified the lean.
- **2026-07-10 · [C-T5 = β] · In-memory host = custom model + thin virtualized grid.** The solver/provenance model layer is platform code (no off-the-shelf grid does pin-sum spreading); the render shell is a thin virtualized grid we own. FINOS Perspective recorded as re-evaluate-at-scale trigger (load grains past ~10⁵ cells or pivot-UX cost); framework choice (React/Vue) and component stack remain **F**'s call — this decision constrains F only to "no heavyweight grid dependency for the entry app". · Bora ratified the lean. · Rejected: Perspective-as-host v1 (its aggregation pipeline owns the numbers — fighting the engine or double-bookkeeping); commercial grid (license/vendor coupling, still no constraint spreading); embedded spreadsheet engine (second computation authority to fence off).
- **2026-07-10 · [C-T14 = CHECK-OUT/CHECK-IN, amends D-iv] · Concurrency = a reservation model: the planner checks out a cubelet (or part), works, checks in; "merging data cubes makes no sense".** Check-out = exclusive write reservation on a slice (overlap denied, holder visible, readers unaffected); check-in = D's commit + **requires the held reservation**; commit does not release (keep-working vs release both first-class). Supersedes the optimistic composite: **cell-LWW moot, advisory claims upgrade to mandatory reservations, rebase-on-commit/drift demoted to a server-side internal invariant** (can never fire under a reservation; firing = platform bug). Preview→commit congruence now holds by construction. Prior-art precision: this is SVN's lock-modify-unlock discipline for non-mergeable (binary) content, not SVN's default copy-modify-merge. Terminology hygiene: *reservation* ≠ FE value pin (C-T2) ≠ version lock (D-v) — never call the reservation a "lock". Reservation registry beside the journal, enforced at the journal/commit API = **named PL contract amendment proposal** (joins DQ-1/2). Open: CQ-14 stale-reservation policy (TTL/heartbeat/admin-break/version-close — mandatory before planning), CQ-15 scope algebra, acquisition mode (lean: read-only open, acquire on first edit). · Bora, 2026-07-10 ("I would strongly prefer subversion… let's do the check-out / check-in pair"). · Rejected: optimistic composite (previously converged D-iv — built to survive concurrent writes to *mergeable* data; cube slices aren't); hybrid advisory-claims+drift (soft claims reintroduce drift UX for no benefit). → [`07`](./07-commit-preview-options.md) §7a; prototype v0.6.
- **2026-07-10 · [C-T8/T10/T11 + reservation UX] · Thread 2 batch convergence (Bora).** **C-T8 = β** — check-in preview = verdict chips (the gate) + batch list + on-demand storage-grain expansion. **C-T10 = β** — session drafts autosave into the entry journal; **v1 drafts = values only** (assignments + seeds); locks & formulas omitted *in v1* — mechanics persistence moved to the parking lot (un-parking consciously revisits the stored-nowhere clause). **C-T11 = α** — whole-journal check-ins; selective commit parked; CQ-12 note field still open. **Acquisition = explicit edit toggle** — open is read-only; "I want to start editing this" acquires the reservation; a taken slice's header shows *who* holds it. **CQ-14 (stale reservations) = manual admin unfreeze in v1** — the header shows whom to call; TTL/heartbeat/auto-expiry/version-close-voiding parked as enhancements. · Rejected: verdict-only & full-diff previews (T8 α/γ); pure in-memory & explicit-save drafts (T10 α/γ); selective/auto commits (T11 β/γ); auto-acquire & acquire-on-first-edit; TTL-in-v1 (complexity before demand). → `07` updates; **prototype v0.7** (read-only open, ✏️ Start editing, holder banner + admin force-release).
- **2026-07-10 · [CQ-15 RESOLVED] · The reservation scope algebra, v1 (`07` §10a ratified).** Reservation = **one product slice**; **scope = the form's load slice as opened** (version pinned + form filters + optional subtree narrowing that equally narrows the load — the ripple problem makes anything finer than the loaded scope unsound in v1); overlap = **per-dimension resolved-member-set intersection at storage grain** (never hierarchy-node comparison — cross-hierarchy scopes break it); **snapshot at acquisition**. Bora's ratification notes: **loaded scope is the only scope that exists contractually** — "visible is just FE display work" (GI-9 extended: display grain appears in no contract); **mid-round dimension/member edits = discipline, not technical prohibition** — "not everyone, everytime" — md-as-canon already gates *how* (git/review); the round adds a *who/when* policy layer → filed under **Q-3 round governance**. · Rejected: symbolic self-updating scopes (silent reservation resize); node-level overlap; sub-slice-below-load (parked with pinned-boundary option b); unions/exceptions (parked).
- **2026-07-10 · [C-T15/T16/T17/T18 = thread 3 converged — resolves Q-4, Q-5, Q-7] · The form = a `ttrl`-family layout document, authored canon; minimal core; version parameterized.** **T15 = α merged into `ttrl`** (Bora: "this is exactly it") — not a new invented kind: the family's "how this thing is presented and interacted with" artifact, bound to a cube, extended with entry semantics. Demand spec re-splits: layout/entry content → the **ttrl schema** (via `PL` GQ-1's family-wide coordination — never fork); model metadata (E-i defaults, D-v version protection, Q-5 cubelet, `default_hierarchy`) → **TTR-M**, one amendment package. **Two lifecycles, one schema:** forms = the family's first *authored canon* citizens; per-user pivot state = the same schema shapes in the platform prefs store (CQ-4 shaped) — never in the repo. **T16 = α + parameterized version** (binding + grain mandatory; md defaults; version = declared parameter role, picked at check-out; form instance = form × version). **T17 approved** (author-time validation catalogue: grain ≥ storage grain · single rollup path · additive measures v1 · seed sources resolve · guard ≤ cap · narrowing ⊂ grain dims — review-time errors, cross-document validation on LSP/compiler turf). **T18 approved** (form edits = Q-3 discipline; reservations immune — member-set snapshots; deletion degrades gracefully). · **Resolves: Q-4** (yes — vocabulary lands in ttrl + TTR-M as split), **Q-5** (cubelet = cube × version slice at storage grain; the form adds the rest), **Q-7** (form = canon, pivot state = prefs). · Rejected: T15 β form-as-TTR-P-program (forms configure, programs transform); γ platform config (reservation scope from unreviewed config — floor); δ saved-plan+flags; T16 β everything-explicit, γ templates (parked); baked-in version (form-per-round churn). Open: CQ-16..18 minor; **CQ-19/20 = the two cross-effort coordination items** (TTR-M `default_hierarchy` collision check; ttrl schema reconciliation with TTR-P C1/GQ-1). → [`08`](./08-form-vocabulary-options.md).
- **2026-07-10 · [Q-3 = α+γ "THE MINIMAL ROUND" — C's last thread; WORKSTREAM C CONVERGED] · v1 adds almost no workflow machinery, because the round already exists as a by-product:** round = the **version lifecycle** (D-v OPEN→LOCKED, both ends disciplined md canon changes gated by git permissions — "not everyone, everytime" as convention, not code); a **read-only round dashboard** over existing objects (reservations = who holds what · entry records = what landed · commit coverage · done flags); the one new state object = a **per-form-instance "done" flag** (planner-declared, pairs with check-in-and-release; the version owner locks when the board is green; rejection = a phone call). **CQ-12 → yes** (free-text note on the entry record, v1). **CQ-10 → drafts private in v1** (only commits + the done flag are visible state — P3: proposed ≠ real). **Roles** (version open/lock, reservation admin-break) = platform admin + git permissions, documented convention. · Bora ratified the lean 2026-07-10. · Rejected: β submit/approve chains in v1 (a genuine new subsystem; approval flows are the most org-idiosyncratic part of every planning tool — **parked**, revisit when a real round demands sign-off *inside* the tool); α-pure (the missing completeness signal costs one bit to fix); δ workflow-as-data (control-plane state in the data plane — boundary marker). → [`09-workflow-options.md`](./09-workflow-options.md) (compact catalogue, A/`05` precedent).

### B / the BI-analysis surface (options in `02-`)

- **2026-07-10 · [B = THE δ-COMPOSITE — LF-2 RESOLVED; ALL WORKSTREAMS CONVERGED] · B-read = semantic projection + bring-your-BI + thin native viewer; B-author = native Designer extension; R fork = R3.** (1) **B-author** ratified as settled-by-architecture: the TTR-P authoring loop (author → validate → door preview → graduate) ships as a **Designer extension** (A; browser-worker LSP + door machinery exist). (2) **B-read = δ-composite:** the **semantic projection** — TTR-M `md` → Cube model / OSI document, deterministically generated, a `PL I-1` projection-out, with the BQ-3 lossiness ledger as companion · **α-3 bring-your-BI** — orgs point Superset (reference pairing; BQ-6-verified: embedded SDK + guest-token RLS in the Apache-2.0 open core) or PowerBI at the layer · **thin native viewer** as a Designer extension (Perspective + Vega-Lite; hierarchy-true drill; **lineage-aware drill number→program→run→entry-commit = the differentiator**). (3) **R3** = the Cube-shaped semantic layer as the single policy carrier (model from md, row policies generated from Perun bundles — BQ-2 = named H-amendment work); **R2 rejected** (per-engine RLS provisioning = a second enforcement surface, the `PL H-7` drift class); **R1 parked** (BQ-1 revisit: is semantic-scoped SQL→plan bounded?). (4) **BQ-4 resolved by C thread 3:** shared/published analysis views = **authored canon ttrl-family documents**; personal views = ttrl shapes in prefs — one schema family with entry forms. (5) **BQ-5/Q-1 resolved:** v1 = interactive exploration + saved views; scheduled/emailed packs parked (`PL D-6`). BQ-6 verification pass in `02` §6 (Superset ✔ better-than-assumed · Metabase ✔ out · Lightdash ✔ out, dbt+agentic drift · DataLens healthy spare). · Bora ratified 2026-07-10. · Rejected: α-1 wholesale embed (hierarchies flatten, two UX worlds, R paid in full); α-2 integrate-fork (the historical trap); γ envelope re-own (F confirmed the dissolution); ε-as-whole (excludes the click-first analyst); R2 (above). Remaining B-items → planning/cross-effort: BQ-1 (parked), BQ-2, BQ-3.

### F / stack & reuse (options in `10-`)

- **2026-07-10 · [F CONVERGED — LF-5 RESOLVED BY DISSOLUTION] · F-1 = α React everywhere platform-side + γ's pure-TS-core discipline** (FQ-1 answered: "React is fine"; C-T5's solver already framework-free — the framework governs only shells, a later switch costs a shell not a product) · **F-2 = α** one kit family split on the `PL A-1` line (MIT `org.tatrman`: door client, Veles metadata client, ttrl utils, formatting · `cz.tatrman`: entry-plane client + grid solver; all core framework-free) · **F-3 = F-γ** envelope don't-reuse (question dissolved: typed door results, grid model, Vega-Lite-as-library for charts) · **F-4** Iris/Sysifos = design-DNA-only + OTel pattern (born-instrumented entry app). **LF-5 resolves by dissolution: nothing re-owned, nothing reused in place; `PL D-3`'s arrow honored trivially.** **Rider — Bora: "can we migrate Iris + Sysifos to React? I would really want one package":** recorded as a **kantheon-side initiative proposal** (FI-3/FI-4 boundary — theirs to run, ours to recommend; → parking lot with handoff). Key observation softening the wait: the framework-free kit means **Vue Iris/Sysifos can consume the package family today** (kantheon = legal downstream per `PL D-3`) — "one package" at logic level precedes any migration; component-level unity follows it. · Rejected: β Vue entry app (two frontend worlds re-imported); γ-as-framework (it's a discipline, adopted, not a choice); envelope re-own/fork (no consumer left). → [`10`](./10-stack-reuse-options.md).

### Consolidation sweep (2026-07-10 — **S-1..S-6 RATIFIED by Bora, with S-2 AMENDED**)

> **Ratification note (Bora, 2026-07-10):** batch approved with one amendment — **the Slavic/mythological naming register is abandoned ecosystem-wide** (a parallel Tatrman-ecosystem decision, external to this effort; supersedes the `PL J` register — flag as a rider to the PL docs). **Technical names only.** S-2(a) amended: the entry product = **`tatrman-entry`** (working technical name); no Sudice. S-2(b)/(c) stand as proposed (already descriptive). S-6 accepted with the name adjusted accordingly. **P5 adopted. The effort proceeds to `design.md` + `detailed-design.md`.**

- **S-1 · Ratify the candidate principles.** **P2** (deterministic surfaces — no LLM/agent intelligence on platform frontends; NL stays Iris/Kantheon), **P3** (propose, don't write — preview→check-in, the frontend is never the authority), **P4** (spreads are explainable — factor chains in-session, entry records after) — all three cited constantly since framing, never violated. **Plus a new P5 earned by the effort:** *"Discipline over machinery — prefer visibility and convention (canon review, holder-in-the-header, round dashboard) over enforcement subsystems; add machinery only when a real round demands it."* (Cited de facto in: mid-round model edits, stale reservations, the minimal round.)
- **S-2 · Naming (`PL J` register: platform-native services/products = Slavic; packages = descriptive).** (a) **The entry product** — proposal: **Sudice** (the Slavic fate-allotters, who *apportion* each person's share at birth — the semantics of budget allocation exactly; alternates on offer: **Dažbog** (the wealth-giving sun deity), **Mokoš**). (b) **Kit packages = descriptive, not mythological** (consistent with PL: services get names, artifacts get functions): MIT `@tatrman/door-client`, `@tatrman/md-client`, `@tatrman/ttrl`, `@tatrman/format`, `@tatrman/tokens`; platform `@tatrman/entry-client`, `@tatrman/grid-core`. (c) **FQ-2:** one shared design-token set (`@tatrman/tokens`, data-only, MIT) across Designer, extensions, entry product — kantheon-consumable downstream.
- **S-3 · The four outgoing demand-spec packages, ratified as design.md outputs.** (1) **→ TTR-P:** the single committed spread construct (per-assignment proportional-over-existing-base, zero-base error, largest-remainder rounding) + the deterministic fragment generator (emits **text**, EQ-3 resolved; construct-vs-stdlib = their call, EQ-1 travels in the spec). (2) **→ TTR-M:** one md package — E-i spread/driver defaults, D-v version metadata, the cubelet contract (Q-5), `default_hierarchy` (CQ-19 collision-check rider). (3) **→ ttrl schema (GQ-1):** the form document kind + the saved-analysis-view kind (BQ-4), the authored-canon-vs-generated-view-state lifecycle distinction (CQ-20 reconciliation rider). (4) **→ PL amendments:** DQ-1 (run-deployed-program-with-input door verb), DQ-2 (table-valued typed params + the two-row-kinds journal refinement), the **reservation registry** (C-T14), D-ii Argos-symmetric write RLS, BQ-2 (Perun→Cube/engine policy generation).
- **S-4 · Leftover leans batch-ratified as decisions:** CQ-5 base-presence bitmap in the load payload = **yes** · CQ-13 others'-changes on reload = **quiet marks** · C-T12 three-layer validation contract ("check-in may only newly fail on drift-invariant/permission") = **decided** · C-T13 reload semantics (locks re-pin, op log clears, provenance handoff to entry record) = **decided** · CQ-7 load-guard = **both** form-author-time validation and load-time check; default cap ~10⁵ cells, exact number = planning · CQ-16 multi-measure forms = **out of v1** (single-measure forms; field set already supports more — parked) · CQ-17 curve/profile library = **md reference data (canon)** · CQ-18 enterable-regions-beyond-measures = **not in v1** (D-v version/time protection covers the real cases; parked) · entry shorthand grammar (`120M`/`450k`/`+2M`/`-10%`) + the provenance color taxonomy = **v1 UX spec baseline** (as prototyped).
- **S-5 · Parking lot confirmed as recorded** (all entries + revisit conditions re-read this pass; no orphans).
- **S-6 · The v1 acceptance statement (strawman, Bora's to edit):** *"A planner opens the Sudice form on `opex`/BUD2028 read-only, checks out the Sales Division slice, enters 120 M at division×FY, locks the March campaign override, block-adjusts H1 with the total held, seeds the empty account from ACT2027, previews the storage-grain expansion, and checks in — producing an entry record; the round dashboard shows the slice done; an analyst drills the result hierarchy-true in the Designer viewer, follows lineage from a cell to that entry commit, and the org's controller reads the same numbers in PowerBI through the semantic layer. Every derived number explains itself."*

---

## 8. Parking lot

| Item | Why parked | Revisit when |
|---|---|---|
| NL/TTR-B surfaces on the Platform | Inherited (`PL` parking lot); intelligence stays kantheon | kantheon-side agent design matures |
| Scheduled/published/bursted reports (report-renderer territory) | v1 lean = interactive-first; report-renderer stays kantheon (`PL D-6`) | B converges; first "email me the budget pack" demand |
| Surface naming (Slavic register, `PL J`) | Name after the products have shapes | A + B + C converged |
| Mobile / tablet entry | No demand named | first field-planning use case |
| PL-P6.S2.T6 smoke-drill amendment: name an Iris **and Sysifos** e2e query explicitly in the adoption acceptance | Belongs to the PL plan, not this effort; offered to Bora 2026-07-09, pending | at PL-P6 execution (or next PL plan-amendment batch) |
| Scenario-as-data-branch (Iceberg/Nessie-style branch per what-if, merge on approve) | D-iv weird option; alien on PG/MSSQL v1 | first real scenario-planning demand |
| Non-additive measure entry (rates, prices, averages — don't spread proportionally) | E scope cut: additive-only v1; re-ratified 2026-07-10 (CQ-3); likely shape when un-parked = `06` §6 option (c), no committed construct | first rates/prices entry demand |
| IPF/global solver for crossing locks (CL-γ escalation) | v1 = error-only (C-T2); purely a FE/grid question since the lock-free-committed-pass decision | real planning rounds hit the crossing-locks rejection often |
| Reservation lifecycle enhancements (TTL, heartbeat renewal, auto-expiry, version-close voiding) | CQ-14 v1 = manual admin unfreeze; header shows the holder to call | stale reservations become a recurring operational pain |
| Submit/approve workflow chains (Q-3 β: statuses, approver roles, inbox, rejection flows) | Q-3 v1 = the minimal round (α+γ); approval flows are org-idiosyncratic — design against a real demand, not speculatively | first customer round demanding sign-off *inside* the tool |
| **Iris + Sysifos → React migration** (Bora: "I would really want one package") | Kantheon-side initiative — FI-3/FI-4 boundary: theirs to run, ours to recommend (handoff item, PL-P6.S2.T6 pattern). Interim: the framework-free F-2 kit is Vue-consumable today (`PL D-3` downstream) — logic-level "one package" precedes migration | kantheon post-split arcs get planned (⑥/doc sweep); or first shared-component demand |
| Persisting session mechanics in drafts (op log incl. FE locks/formulas) | C-T10 v1 = values-only drafts; would consciously revisit the "stored nowhere" clause | demand for full session recovery (locks restored on reopen) |
| Sub-slice check-out with pinned boundary (`07` §10a option b) | v1 scope = the form's load slice; pin machinery exists but the arithmetic surprises planners | shared-form department-planner demand |

---

## 9. Rolling open questions

- ~~**Q-1 ·** Saved/shared artifacts in v1?~~ **RESOLVED 2026-07-10 (B/BQ-5) → v1 = interactive exploration + saved views** (shared = authored canon ttrl documents; personal = ttrl-in-prefs — BQ-4 via C thread 3); scheduled/emailed packs parked (`PL D-6`).
- ~~**Q-2 ·** Entry concurrency model~~ **RESOLVED 2026-07-09 v1 → cell-LWW + advisory claims + rebase-on-commit (D-iv)** — **AMENDED 2026-07-10 → check-out/check-in reservations (C-T14); drift check survives only as an internal invariant.**
- ~~**Q-3 ·** Entry workflow — v1 scope or later?~~ **RESOLVED 2026-07-10 → α+γ "the minimal round"** (round = version lifecycle · read-only dashboard · done flag · note field · drafts private · roles = admin+git convention; submit/approve **parked**). → [`09`](./09-workflow-options.md). **Workstream C fully converged.**
- ~~**Q-4 ·** Does TTR-M need new vocabulary for *entry forms*~~ **RESOLVED 2026-07-10 → yes, split: the form document = `ttrl`-family (layout/entry content, GQ-1 coordination); model metadata (E-i, D-v, `default_hierarchy`, cubelet) = TTR-M, one amendment package (C-T15/T16).**
- ~~**Q-5 ·** What exactly is a "cubelet" contractually~~ **RESOLVED 2026-07-10 → cubelet = cube × version slice at storage grain (existing md objects, no new concept); the ttrl form adds grain/hierarchies/layout/entry on top (C-T15/T16).**
- ~~**Q-6 ·** Where do *drivers* for spreads come from~~ **RESOLVED 2026-07-09 → drivers are visible-pass tooling + seed sources (md-declared defaults, ad-hoc legal, recorded in the entry record); the committed pass has no driver — its base is the current slice.**
- ~~**Q-7 ·** The working-view definition: who declares, and is a saved working view canon?~~ **RESOLVED 2026-07-10 → the modeler declares the form (ttrl-family, authored canon: load grain, hierarchy choice, initial layout, entry); the planner's pivot state & personal views = the same ttrl schema shapes in platform prefs, never canon (C-T15/T18).**
- ~~**Q-8 ·** Does GI-7 (two v1 mechanisms incl. non-additive/prices) **amend E's additive-only-v1 cut**?~~ **RESOLVED 2026-07-10 → no: non-additive spreading is out of v1; E's additive-only cut stands; parking-lot entry unchanged (CQ-3 closed).**

---

## 10. Session index

| Date | Gear | What happened | Artifacts |
|---|---|---|---|
| 2026-07-09 | Framing | Effort opened from the "where are Iris and Sysifos" gap. Grounding: PL design set re-read; kantheon sysifos-design/iris-design read; Sysifos = no-intelligence-but-Midas-coupled established (FI-3). FI-1..FI-5 fixed; workstreams A–F cut; LF-1..LF-6 named; P2–P4 candidate principles proposed; hero scenario pinned (FY2028 OPEX budget). | this doc, `01-design-space-map.md` |
| 2026-07-09 | Divergence (B) | OSS-BI landscape researched (semantic layers + components verified vs official sources; Tier-1 classics flagged ⚠ unverified-2026). B-α..ε catalogued + rendered vs hero; **policy-honesty problem named (external BI speaks SQL; the platform's governed read is a plan door — reconciliations R0–R3)**; **decomposition finding: B-author (TTR-P authoring loop) is platform-native in every branch, LF-2 really decides B-read**; leans recorded (δ-composite; Vega-Lite-as-library instead of envelope re-own; ε floor-marked). BQ-1..6 opened. B → 🟡 | `02-bi-surface-options.md` |
| 2026-07-09 | Divergence (D+E, joint) | **Key observation reframes D: program materialization is the platform's one existing data-plane write mechanism — every branch is a way to ride/extend/bypass it.** D-α..δ catalogued + cross-cutting sub-forks D-i..v (journal write semantics, RLS-on-write via Argos symmetry, the **entry record** as compile-record sibling, concurrency incl. weird scenario-as-data-branch, version protection in md metadata). E: method vocabulary pinned as candidate set with invariants (sum-preservation, locks-or-error, declared rounding); placements α..ζ. **The load-bearing pairing: E-γ spread-in-TTR-P inside D-β-ii's standing canon entry-apply program** — leans recorded accordingly (D: γ-front+β-ii-back; E: γ+δ ergonomics, TTR-M-declared defaults). DQ-1..5, EQ-1..5 opened; TTR-P demand-spec = cross-effort item. D, E → 🟡 | `03-write-path-options.md`, `04-spreading-options.md` |
| 2026-07-09 | Convergence (D+E closed) | **Bora ratified the D+E leans with the TWO-PASS AMENDMENT** (visible in-memory working view with rich non-authoritative tooling → C; invisible committed pass = single proportional-over-unlocked-remainder construct) + **zero base = ERROR** (seed = explicit C-level copy op) + **existing support only** + additive-only v1 + rebase-on-commit. D 🟢 (γ+β-ii composite, entry record, D-i/ii/iv/v) · E 🟢 (γ+δ placement, one-construct TTR-P demand spec, md-declared defaults). Q-2/Q-6 resolved; Q-4 narrowed; Q-7 opened; EQ-2/EQ-5 discharged; EQ-4 → C. Scenario-branches + non-additive entry → parking lot. **Board: B 🟡 · D 🟢 · E 🟢 · A/C/F ⚪.** | decision log, `03`/`04` resolution sections |
| 2026-07-09 | Convergence (A closed) | **A discussed briefly in chat and converged: the SPLIT VERDICT** (analysis = Designer Extensions · entry = own product · Excel add-in = future client · both `cz.tatrman` · planners never author canon). LF-1 + LF-6 resolved; compact catalogue written post-hoc. **Board: B 🟡 · A/D/E 🟢 · C/F ⚪.** | `05-product-shape-options.md`, decision log |
| 2026-07-10 | Divergence (C, thread 1: grid core) | Bora's grid brief recorded (GI-4..GI-8). **Three-grains clarification** (storage / load / display; zero-base pre-flight). Lock fork CL-α/β/γ catalogued — α refuted by rendering the hero, lean β pin-sum + compensation cascade (IPF = escalation path, CQ-1); edit-propagation + auto-lock fork (CQ-2, prototype toggle); manipulation vocabulary v1 pinned as candidates; host fork (Perspective vs custom shell — model layer custom regardless); **GI-7 vs E's additive-only cut flagged (CQ-3 ⚑ / Q-8)**; Q-7 narrowed (form=canon, pivot state=prefs). **Working in-browser prototype built + solver verified (16 assertions, hero end-to-end).** C → 🔵 | `06-entry-grid-options.md`, `prototypes/c-entry-grid-v0.html` |
| 2026-07-10 | Convergence (C partial, from prototype feedback) | Bora: prototype "exactly what it needs to be". Ratified: **auto-lock = user toggle** (CQ-2); **non-additive out of v1** (CQ-3/Q-8 — E's cut stands); **LOCK-FREE COMMITTED PASS** — locks & formulas FE-only/ephemeral/never stored, entry record drops locks, committed construct simplifies to per-assignment proportional-over-existing-base (disjoint-regions observation made the unlocked clause vacuous at commit). GI-9 added (FE operates on loaded scope, not visible; collapse hides but does not unload). | decision log §7 C-section, `06` updates, prototype text aligned |
| 2026-07-10 | Convergence (C thread 1 closed) | Bora ratified the remaining grid-core leans in one pass: **C-T2 = CL-β** pin-sum + compensation cascade (CQ-1 = error-only v1, IPF → parking lot) · **C-T3** immediate per-gesture compensation, atomic gestures, op-log undo · **C-T4** vocabulary v1 (shorthand, summary spread, block scale, curve, seed; formulas v1.5, CQ-6 open) · **C-T5 = β** custom model + thin virtualized grid (Perspective = at-scale trigger; stack framework → F). **Grid core done; C stays 🔵 for threads 2+** (commit/preview UX, Q-3 workflow, form-declaration vocabulary) + CQ-4/5/6/7. | decision log §7 C-section, `06` resolution updates |
| 2026-07-10 | Divergence (C, thread 2: commit/preview) | Threads C-T8..T13 catalogued (preview depth · drift/rebase UX · session drafts · commit granularity · validation topology · post-commit). **Findings: journal = two row kinds (seed = storage-grain copy, applied first; assignment = load-grain value)** — refinement proposal to D's table-valued contract; CQ-5 bitmap demonstrated; scoped drift via per-row base hashes (server stateless about previews); op-log persistence marked illegal-by-construction (would store locks). **Prototype → v0.5:** simulated storage grain (2 CCs under load grain), preview modal with expansions, Petra concurrent-commit → drift rejection → scoped re-preview → entry record → rebase; backend verified (13 assertions). Leans recorded (β/β/β/α+note/3-layer contract/rebase-as-prototyped); CQ-8..13 opened. Thread 2 → 🟡 | `07-commit-preview-options.md`, prototype v0.5 |
| 2026-07-10 | Convergence (C-T14 decided, amends D-iv) | Bora: **"strongly prefer subversion — check-out / check-in; merging data cubes makes no sense."** Reservation model decided: exclusive slice check-out (overlap denied, holder visible) · check-in requires reservation · commit ≠ release (keep-working first-class) · cell-LWW moot, claims → mandatory reservations, drift → internal invariant · terminology hygiene (reservation ≠ FE pin ≠ version lock) · reservation registry = PL amendment proposal. CQ-14 (stale reservations) + CQ-15 (scope algebra) opened; CQ-9 superseded; C-T9 superseded. **Prototype → v0.6:** reservation banner, Petra denied at check-out (acquires after release), check-in keep/release, drift chrome removed (15 assertions). | decision log C-section, `07` §7a, prototype v0.6 |
| 2026-07-10 | Convergence (C thread 2 batch) + CQ-15 deep dive | Bora ratified: **T8 β · T10 β journal-drafts values-only-v1 (mechanics persistence → parking lot) · T11 α · explicit edit toggle (read-only open; header shows holder) · CQ-14 = manual admin unfreeze v1 (TTL etc. parked)**. **CQ-15 deep-dived on request** (`07` §10a): four issues named — regions must intersect on resolved member sets (cross-hierarchy scopes break node comparison) · **the ripple problem** (gestures write beyond clicked cells ⇒ v1 scope = the form's load slice; sub-slice-with-pinned-boundary = parked v2) · granularity economics (version-pinned × one subtree × rest-full is the whole practical vocabulary) · snapshot-at-acquisition vs symbolic. **v1 shape proposed, awaiting ratification ⚑.** Prototype → v0.7 (read-only open, ✏️ Start editing, holder + admin force-release). Thread 2 → 🟢-substantially. | decision log, `07` updates, prototype v0.7 |
| 2026-07-10 | Convergence (CQ-15 ratified — thread 2 CLOSED) | Bora ratified the §10a v1 shape ("yes, this makes sense; I always thought of the **loaded** scope — visible is just FE display work"). Loaded-scope reservations pinned contractually; **mid-round dimension edits = discipline, not prohibition** ("not everyone, everytime") → Q-3 round governance owns the who/when policy over md's canon path. **Thread 2 🟢.** C remaining: thread 3 (form-declaration vocabulary) + Q-3. | decision log CQ-15 entry, `07` §10a |
| 2026-07-10 | Divergence (C, thread 3: form vocabulary) | The declaration's weight established (load slice = view extent = reservation scope = ripple boundary = draft home). **Decision-traced field inventory** (§0 of `08`: binding · grain · hierarchies · layout · entry · guards · narrowing — each demanded by a named decision). Forks: **T15 ontology** (α md canon document kind, lean · β TTR-P artifact · γ platform config, floor · δ saved-plan weird) · **T16 minimality** (α minimal core + md defaults, lean; **version parameterized**, picked at check-out) · **T17** validation rules = the review-time error catalogue; TTR-M demand spec = ONE amendment package (form + E-i + D-v + Q-5 + `default_hierarchy`) · **T18** lifecycle (form edits under Q-3 discipline; reservations immune to form changes — member-set snapshots). Hero form rendered (`08` §5 + prototype v0.8 panel). CQ-16..19 opened. Thread 3 → 🟡 | `08-form-vocabulary-options.md`, prototype v0.8 |
| 2026-07-10 | Convergence (C thread 3 closed) | Bora ratified with one structural addition: **T15 = α merged into the `ttrl` layout-document family** ("because this is exactly it") — grounded against the repo (`.ttrl` = the family-wide per-document view-state sidecar, PL G-5/GQ-1 shared schema); forms become the family's first *authored canon* citizens; demand spec re-split (ttrl schema ← layout/entry via GQ-1 · TTR-M ← model-metadata package); CQ-4 shaped free (pivot state = ttrl content in prefs). **T16 = parameterized α**; T17/T18 approved. **Q-4/Q-5/Q-7 RESOLVED.** CQ-20 opened (ttrl schema reconciliation). **Thread 3 🟢 — C's last thread: Q-3.** | decision log, `08` resolution, prototype v0.8 panel text |
| 2026-07-10 | Convergence (Q-3 closed — **C 🟢**) | Q-3 re-presented with accumulated context (round view, draft visibility, change discipline, admin-break, note field); key observation: *most of a round already exists as a by-product* (D-v version state, entry records, reservation registry, journal drafts, canon-gated changes). **Bora ratified α+γ "the minimal round"**: version lifecycle as the frame · read-only round dashboard · one done flag per form instance · note field yes (CQ-12) · drafts private (CQ-10) · roles = admin+git convention; **β submit/approve parked** (revisit on real in-tool sign-off demand); δ workflow-as-data rejected. Compact catalogue `09` written post-hoc (A/`05` precedent). **WORKSTREAM C FULLY CONVERGED** — threads 1/2/3 + workflow, prototype v0.8 renders the composite end-to-end. **Board: A/C/D/E 🟢 · B 🟡 · F ⚪.** | `09-workflow-options.md`, decision log, dashboard |
| 2026-07-10 | Divergence (F) | F catalogue written → `10-stack-reuse-options.md`, grounded against the repos (Designer = React 19 + cytoscape + LSP client; Iris = Vue + Vega-Lite + OTel). Walls restated (A/C-T5/PL D-3/D-6/A-1); **F-1 entry framework** = the one live fork (α React everywhere · β Vue split · γ framework-agnostic-core discipline — C-T5's solver is already pure TS); **F-2 kit** (α MIT-line split: `org.tatrman` contract clients + `cz.tatrman` entry plane/solver); **F-3** envelope question dissolved (F-γ; Vega-Lite-as-library); **F-4** DNA-only + OTel pattern; **LF-5 lean = resolution by dissolution**. FQ-1 (team Vue-vs-React preference — Bora) gates F-1; FQ-2/3 → sweep. F → 🔵 | `10-stack-reuse-options.md` |
| 2026-07-10 | Convergence (F closed — **LF-5 resolved, ALL LFs done**) | FQ-1: **"React is fine"** → F-1 = α + γ discipline · F-2 α (MIT-line kit, framework-free core) · F-3 F-γ · F-4 ratified · **LF-5 resolved by dissolution**. Rider recorded: **Iris+Sysifos→React** = kantheon-side initiative proposal, parked with handoff; interim logic-level "one package" via the Vue-consumable framework-free kit. **Board: A/C/D/E/F 🟢 · B 🟡.** Remaining: B convergence (BQ-6 owed) → consolidation sweep (S-items, P2–P4 ratification, naming) → `design.md` + `detailed-design.md` → `/planning`. | decision log F-section, `10` resolution, dashboard |
| 2026-07-10 | Convergence (B closed — **BOARD ALL GREEN**) + sweep opened | BQ-6 verification pass run (web-verified: Superset ✔ open-core embedding, Metabase ✔ out, Lightdash ✔ out, DataLens healthy) → `02` §6. **Bora ratified the δ-composite**: B-author = Designer extension · B-read = projection + bring-your-BI + thin lineage-aware viewer · **R3** policy carrier (R2 rejected, R1 parked) · BQ-4 = ttrl split · BQ-5/Q-1 = interactive+saved-views v1. **LF-2 resolved — all six LFs, all six workstreams 🟢.** **Consolidation sweep compiled: S-1..S-6 proposed** (P2–P4+new P5 · naming incl. entry-product proposal **Sudice** · four demand-spec packages · leftover-lean batch · parking-lot confirm · v1 acceptance strawman) — awaiting batch ratification. | decision log B-section + sweep section, `02` §6, dashboard |
| 2026-07-10 | Sweep ratified + **EFFORT CLOSED** | Bora ratified S-1..S-6 with **S-2 amended: mythological naming abandoned ecosystem-wide** (supersedes `PL J`; rider owed to PL docs) — entry product = **`tatrman-entry`**; P5 adopted. **`design.md`** (planning-facing: decisions, contract inventory, the four demand-spec packages, scope boundary, acceptance statement) **+ `detailed-design.md`** (the manual: concepts, worked hero, algorithms, rationale, rejected roads) written at `docs/ecosystem/platform/design/frontends/`; prototype v0.8 = normative illustration. **Next: `/planning` consuming `design.md`.** | `../design.md`, `../detailed-design.md`, status header |
