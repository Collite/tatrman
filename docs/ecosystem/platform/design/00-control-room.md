# Tatrman Platform — Control Room

> The single dashboard for the **Tatrman Platform** design effort: making the **two-mode** (standalone / connected) architecture a robust, first-class design choice, and carving the deterministic half of Kantheon into a platform that Tatrman owns.
> Open this first every session. Companion docs: [`01-design-space-map.md`](./01-design-space-map.md) (the branches), [`02-mode-seam-options.md`](./02-mode-seam-options.md) (workstream B).
>
> **Status:** ✅ **EFFORT COMPLETE — RE-OPENED 2026-07-10 for the ecosystem-redraw amendment batch, re-closed the same day.** The 2026-07-10 strategy session (`../../ecosystem.md` + `../../next-steps-260710.md`) re-tiered the converged design: the open read spine ships as **Tatrman Server** (Apache-2.0, new `tatrman-server` repo per RO-1); the operate tier designed here is **parked by sequence** as Tatrman Platform. Amendments in §7 → "Ecosystem redraw amendment batch" (STRAT-1..9 + RO-1..14). **Live core docs: `docs/ecosystem/server/`; the PL plan set (`../implementation/`) is frozen as the operate-tier reference.**
> Original close: fully converged (A–K all 🟢) 2026-07-09; final deliverables written 2026-07-09: [`./design.md`](./design.md) (planning-facing) + [`./detailed-design.md`](./detailed-design.md) (narrative). Next: `/planning` on `design.md`. Started 2026-07-08.

---

## 0. How we run this

Same protocol as the TTR-P effort (`../../../features/ttr-p/design/00-control-room.md` §0): **diverge before we converge**; three gears (Framing → Divergence → Convergence); ≥3 alternatives per fork including the weird one; leans are not decisions; deferred is a tracked outcome; append-only decision log is ground truth.

Cross-effort citations use the source prefix: `TTR-P G-b`, `MD3` (ttr-metadata), `TR-3` (ttr-translator). This effort's own IDs are bare letters.

---

## 1. Workstream dashboard

Status legend: ⚪ not started · 🔵 diverging · 🟡 options captured · 🟢 converged/decided · 🟠 converged-but-dirty (ratified review riders open — dirt listed in the doc's status section) · ⏸ parked

> **2026-07-10 redraw note:** the decisions below stand; their *tier* changed (STRAT-1/2, §7). Roughly: B (seam), C's spine services, G's viewer, H's enforcement half, I's one-shot import, K's direction → **open core (Tatrman Server)**; E, F, Charon, Perun, envelopes/event spine, continuous harvest → **operate tier, parked satellite (c)**. Per-decision dispositions are in the RO entries.

| # | Workstream | Status | Core question | Notes |
|---|---|---|---|---|
| **A** | Product split, scope & licensing | 🟢 | What *precisely* ships in MIT standalone vs the Platform; where is the edition boundary drawn, and by what rule? | **Converged 2026-07-08: the A-α "compile vs operate" rule** + grey-zone placements + P1–P3 ratified. Standalone-persona question → parking lot; monetization → parking lot. |
| **B** | The mode seam (compiler/optimizer contract) | 🟢 | What exactly does "connected" change for compilation and optimization — and what may it never change? | **Converged 2026-07-08** → `02-mode-seam-options.md` (B-1 β SPI seam, mode-blind compiler · B-2 β stats overlay · B-3 α hard parity · B-5 δ fetch-then-compile · B-6 γ one core · BQ-1 archive · BQ-2 γ per-object keying + inline recording · BQ-3 layered lock-for-canon/max-age-for-stats · BQ-4 plugins in lock). Owed: C confirms server contract; H confirms advisory-only compile policy. BQ-5 (lock scope) rides with F/D. |
| **C** | Platform service architecture | 🟢 | The service roster and boundaries: metadata server, workers, dispatcher, movement, scheduler, security, Designer backend — what moves from Kantheon, what's new, what dies? | **Converged 2026-07-08** → `03-service-architecture-options.md` (C-1 β-spine+α-leaves · C-2 γ new metadata server + lineage + exports · C-3 two doors/one hall, Proteus dissolves · C-4 Charon home · C-5-i validator SPI; CQ-3/P2 clarified). Riders: C-5-ii/iii → H; CQ-1 + grey-zone sweep → D; CQ-2 → F. **Amended 2026-07-09:** column-level lineage IS v1 (static, compiler-derived — CQ-5); CQ-6 pinned; **re-converged in the de-dirty pass**. |
| **D** | Kantheon split & repo/infra topology | 🟢 | Which repos exist after the split; where does the Platform's code and its Olymp-like infra repo live; how do the extraction arcs sequence? | **Converged 2026-07-08** → `04-repo-topology-options.md` (new platform repo · license=repo + Q-4-a · "toolchain-touched ⇒ tatrman-owned" · olymp-twin infra, ship-as-chart direction · strangler ①–⑦, CQ-1 closed · grey-zone sweep · Gradle-only · sweep-on-arrival · tatrman-semantics obsolete). |
| **E** | Orchestration-engine integration | 🟢 | Orchestrators (Dagster, Airflow, bash, …) as *compiler emit plugins* AND as *platform-registered engines* — one mechanism or two? | **Converged 2026-07-08** → `06-orchestration-options.md` (E-1 β SPI · E-2 γ world-content registry · E-3 γ world-driven delegation · E-4 β run-harvest · E-5 β manifest contract; **LF-4 resolved**; **EQ-3: v1 targets = {bash, Airflow 3, Kestra}, Dagster post-v1**). Riders: EQ-4 + connector frame → I (✓); secrets/trust → H (✓); registration UX → G (✓). **Amended 2026-07-09 + re-converged:** EQ-2 determinism → H-6; E-5 gains the lineage section (CQ-5); T6 type-manifest amendment queued (sweep). |
| **F** | Scheduler & job execution | 🟢 | The platform server that schedules and runs jobs — in workers and via registered orchestrators. | **Converged 2026-07-08** → `05-scheduler-options.md` (F-1 γ thin-triggers+door-executor · F-2 β envelope+verbatim-bundle (Q-3) · F-3 α executor w/ external run state · F-4 package, FF reserved · F-5 γ-minimal quotas · F-6 β run store (CQ-2) · F-7 γ (BQ-5 dissolved) · F-8 C-4-α persists). LF-6's F half resolved. Riders: FQ-5 → H (✓); F-5-δ parked. **Amended 2026-07-09 + re-converged:** F-2-δ cites `{lock hash + compile record}` (Q-7 ✓); FQ-6 two staging scopes; FQ-7 dissolved (manifest-schema-version compatibility); F-6 events cite manifest lineage. |
| **G** | Designer evolution | 🟢 | Browser Designer on the Platform: writes, multi-user, collab; vs standalone view-only `.ttrl` in IDEs. | **Converged 2026-07-09** → `08-designer-options.md` (G-1 γ writes-through-git · G-2 γ graph+text idioms · G-3 γ stateless-reads/session-ful-edits · G-4 α reader-first v1 **+ TTR-P program graphs in** (GQ-2), edits designed-now-built-second · G-5 standalone statements ratified). Discharged: HQ-3 + both review inputs. GQ-1 coordination, GQ-4 ops item. |
| **H** | Security & governance | 🟢 | The OPA-based security server bound to metadata: enforcement points, identity, what (if anything) security means in standalone mode. | **Converged 2026-07-09** → `07-security-options.md` (H-1 γ+security-block sugar · H-2 α ingress/explicit service principals · H-3 α minimal-spine PEPs, compile advisory-only · H-4 γ PDP, local PEP evaluation · H-5 β+γ secrets, never-at-rest · H-6 β trust+determinism kit · H-7 α-then-β, RLS v1 · H-8 γ+β standalone stance). Discharged: FQ-5, B's IOU, C-5-ii/iii, EQ-2 widening, E-3-β line, Q-1/LF-8. Riders: HQ-2→observability, HQ-3→G, HQ-4→⑥, HQ-5→β-step. |
| **I** | External metadata & megaproviders | 🟢 | Connecting to OpenMetadata / Collibra / Amundsen …; Google & Azure ecosystems (PowerBI for MD, Fabric for metadata). Direction of truth: import, sync, or federate? | **Converged 2026-07-09** → `09-external-metadata-options.md`: **I-1 α "proposals in, projections out" (LF-7 resolved — the last LF)** · I-2 α one connector frame + temperament split + one-shot-CLI-MIT/continuous-platform line (EQ-4 ✓) · I-3 α PR proposals (robots write through git) · I-4 α (IQ-1 → TTR-M) · **I-5 OpenMetadata anchor; DataHub first follow-on, consciously not v1**. |
| **J** | Naming & conventions | 🟢 | Platform name, service names (mythology continues?), repo names, artifact coordinates, edition names. | **Converged 2026-07-09** (map §J): register rule = new-Slavic/transplants-keep-Greek; `tatrman-platform` + `tatry`; **Veles** (metadata) · **Perun** (PDP) · **Radegast** (executor) · **Zorya** (triggers) · Theseus + hall keep names; editions Tatrman / Tatrman Platform (no "community"); **`org.tatrman:*` MIT · `cz.tatrman:*` Platform**; `ttr-emit-*`/`ttr-connect-*`; "Designer Extensions". |
| **K** | World composition | 🟢 | Where does the *platform-managed world* live, and how do project worlds compose with it (merge / import-extends / platform-world-is-THE-world)? Multi-project instance truth. | **Converged 2026-07-09** (map §K resolution): **β explicit reference + α-as-mechanics**; contradiction = compile error; platform world = its own git repo (edit rights → H-3 policy); standalone parity via file-exported copies; project roster = server config, not world content. Resolves Q-5; unblocks G + part of C's dirt. Grammar → amendment sweep. |

---

## 2. Framing inputs (Bora, 2026-07-08 — FIXED)

The split itself is **decided framing**, not a fork (Bora's call, this session's opening):

- **FI-1 · Two modes are FIRST-CLASS.** The compiler/optimizer works in (1) **standalone mode** — reading only repositories, static, offline; (2) **connected mode** — connected to the Tatrman metadata server. This is to be made robust and first-class, not a bolt-on.
- **FI-2 · Standalone = MIT open source.** The languages (TTR-M/TTR-P/TTR-B), textual representations, IDE extensions, and the **compiler** — which emits *scripts to be run* (different scripts for different engines) and is **not an execution platform**. Repositories are the only backing store. Designer in standalone = **view-only over `.ttrl`/model files inside IDEs**.
- **FI-3 · Connected = the Tatrman Platform.** The Platform **takes ownership of the workers from Kantheon**, splitting Kantheon in two: the **working (deterministic) platform → Tatrman Platform**; the **intelligence (agents + LLMs) → Kantheon**, which stays in **Olymp**. The Tatrman Platform gets its own Olymp-like infra repo with cluster definitions.
- **FI-4 · Minimum Platform roster:** (1) workers; (2) Designer as a browser app — later writes, multi-user; (3) metadata server; (4) security server bound to the metadata (the OPA machinery from Kantheon). "Basically the whole deterministic part of Kantheon."

### Grounding inputs (Bora, 2026-07-08 — diverge-worthy ideas, not decisions)

- **GI-1 · Orchestration engines as compiler plugins.** Emit script/XML/YAML/… for Dagster / Airflow / … / bash. The Platform can additionally **host and register** orchestrators (like it registers databases) and work with their live-ish metadata, statistics, and contents.
- **GI-2 · Connect to existing metadata servers** — OpenMetadata, Collibra, Amundsen, ….
- **GI-3 · Connect to megaproviders** — Google ecosystem; Azure ecosystem incl. PowerBI for MD work, Fabric for metadata; ….
- **GI-4 · A platform server to schedule and run jobs** — in the execution engine(s) and by connecting to existing orchestration engines.

---

## 3. What we already have (asset inventory)

The TTR-P + extraction-arc corpus already points at this split; we formalize and extend, not invent:

- **TTR-P T6 · world = compile target.** The world doc describes the surroundings; the compiler compiles *against* it offline; the runtime **verifies compatibility** — never the compile-time source of truth. This is the strongest existing hint at the mode seam: connected mode plausibly changes *where the world comes from*, not what compiling means.
- **TTR-P D-g · offline compile.** The compiler embeds the metadata component and reads model repo + world docs directly from paths — no service at compile time. Kantheon workspace metadata = a *population source* for worlds.
- **TTR-P G-g · one-way seam.** Kantheon consumes published `org.tatrman:*` artifacts and compiled plans; never a running Tatrman service; softened only for agents authoring source (C4-e).
- **TTR-P F-lite vs F-proper.** F-lite (bash wave-parallel bundles, fail-fast, Arrow staging, `TTR_CONN_*` env credentials, JSON manifest + semantic world fingerprint) is converged and is *exactly the standalone story*. **F-proper — orchestrator, events, FF, retries/resume, on-failure islands, runtime params — was deferred "to v2 with the Kantheon orchestrator"; it now has an address: workstream F of this effort.**
- **ttr-metadata arc (2026-07-05).** `org.tatrman:ttr-metadata` (+`-git`) extracted from Ariadne; storage SPI (`ModelStorage`: LocalFs / Classpath / GitArchive); world resolution; `ttr-designer-server` (repo-attached, loopback-only S24, read-only v1); Ariadne becomes a thin gRPC wrapper in kantheon. **The metadata *library* is done-by-design; the metadata *server* of FI-4 is not — its relationship to Ariadne is a load-bearing fork (LF-2).**
- **ttr-translator arc.** `org.tatrman:ttr-plan-proto` + `org.tatrman:ttr-translator`; tatrman owns the plan wire format (TR-3/S25). Precedent: the Platform can own wire contracts kantheon consumes.
- **Kantheon roster** (the deterministic candidates for the carve-out): **Theseus** (orchestrate) · **Proteus** (translate; already thinning to a ttr-translator wrapper) · **Argos** (validate/RLS — presumed home of "the OPA thing", verify) · **Kyklop** (dispatch) · workers **Brontes** (MSSQL) / **Steropes** (Polars) / **Arges** (PG) · **Charon** (data movement) · **Ariadne** (metadata; thinning per MD arc). Intelligence side (stays Kantheon): **Pythia** + agents/LLM machinery.
- **Designer** (`packages/designer`, TS/React) with the MD6 WS data-source adapter — the seed of the browser Designer.
- **Olymp** — kantheon's infra repo (cluster definitions); the template for the Platform's infra repo.

---

## 4. Load-bearing forks (decide consciously, not by drift)

1. ~~**LF-1 · The seam object.**~~ **RESOLVED 2026-07-08 → source SPI behind a mode-blind compiler (B-1 = β), with snapshot/lockfile discipline.** See decision log.
2. ~~**LF-2 · What is the metadata server?**~~ **RESOLVED 2026-07-08 → new platform-native service on the `ttr-metadata` library (C-2 = γ), + lineage organ + export connectors; Ariadne stays kantheon's facade.**
3. ~~**LF-3 · Repo & license boundary.**~~ **RESOLVED 2026-07-08 → new platform repo; license boundary = repo boundary (D-1/D-2); Designer frontend stays MIT (Q-4-a).**
4. ~~**LF-4 · Plugin mechanism.**~~ **RESOLVED 2026-07-08 → JVM SPI for emit (E-1 = β); registration is not a mechanism but WORLD CONTENT (E-2 = γ, B-T6's instance layer); the platform half is connector-shaped (E-4).** One concept (world-declared engine with a manifest), two mechanisms (compile-time emit SPI · platform-side connector SPI), three deliverables per orchestrator (the "support package").
5. ~~**LF-5 · Where does connected compilation run?**~~ **RESOLVED 2026-07-08 → both, one compiler core (B-6 = γ); B-3-α parity makes location a deployment convenience.**
6. ~~**LF-6 · Scheduler: build vs delegate.**~~ **F HALF RESOLVED 2026-07-08 → thin trigger layer + F-proper executor (F-1 = γ); external orchestrators = alternative frontends to the program door.** E's half (what a *registered orchestrator* is, and the frontend-contract pressure-test) stays open → E.
7. ~~**LF-7 · Direction of metadata truth** vs external servers/megaproviders: import (population source, TTR-P D-g stance) · bidirectional sync · live federation. → I.~~ **RESOLVED 2026-07-09 → I-1 = α: "proposals in, projections out — never sync, never federate."** See decision log. **All eight load-bearing forks are now resolved.**
8. ~~**LF-8 · Security reach.**~~ **RESOLVED 2026-07-09 → H-8: standalone = integrity + optional advisory lint (the OSS toolchain carries the policy *linter* and the `security`-block *generator*, both A-α-legal checks); enforcement is inherently platform.** See decision log.

---

## 5. Design principles (P-n; cite by ID)

- **P1 · Standalone is not a demo. (RATIFIED 2026-07-08)** The MIT compiler is complete, correct, and useful alone; connected mode **adds capability** (live worlds, statistics, execution, scheduling, security, collaboration) — it never *fixes* standalone, and no feature of the *language* is gated on the Platform. The open-source story must survive on its own merits.
- **P2 · One-way arrow, generalized. (RATIFIED 2026-07-08; clarified with C-5-i)** OSS never depends on the Platform; the Platform consumes published `org.tatrman:*` artifacts; full chain `tatrman → platform → kantheon` (D-3). **P2 constrains *build-time* dependencies; runtime plugins conforming to Platform/OSS-defined SPIs are legal.** No circular coupling anywhere in the family.
- **P3 · No miracles. (inherited = TTR-P P2; RATIFIED 2026-07-08)** Explicit, or deterministically derived from declared defaults; otherwise an error. Applies with full force to the mode seam: connected mode must not introduce invisible compile inputs.

---

## 6. Hero scenario (carried through every workstream)

**"One program, three lives."** The TTR-P hero program (accounts SQL + sales CSV → join → summarize → branch, two engines):

1. **Standalone life:** compiled offline against a repo world → `.bundle/` (F-lite bash artifact) → run by hand. MIT tools only.
2. **Platform life:** the same program, world served by the Platform's metadata server (live schemas + statistics) → deployed, **scheduled nightly**, executed on Tatrman workers (Arges + Steropes), Charon moves data, OPA authorizes who may run/see what, run history and lineage in the metadata server.
3. **Federated life:** the Platform has Dagster registered as an orchestration engine and OpenMetadata connected; the program is emitted as a Dagster DAG, scheduled there, and its lineage/metadata appear in OpenMetadata.

Every workstream renders its options against these three lives.

---

## 7. Decision log

> Append-only. Format: `YYYY-MM-DD · [id] · Decision · Why · Alternatives rejected`.

- **2026-07-08 · [FRAME] · FI-1..FI-4 accepted as fixed framing** (two first-class modes; standalone = MIT OSS compiler; connected = Tatrman Platform owning Kantheon's deterministic half; Kantheon keeps intelligence, stays in Olymp; Platform gets its own infra repo). · Bora's opening statement, confirmed as framing-not-fork. · Alternatives (single OSS platform; plugin-tier editions) consciously not explored — the split is a product decision, not a design output.

### B / mode seam (full options + resolutions in `02-mode-seam-options.md`)

- **2026-07-08 · [B-1 = β] · The mode seam is a SOURCE SPI: the compiler is MODE-BLIND.** It always compiles against `(models, worlds, manifests, statistics)` through the source SPI (`ttr-metadata`'s `ModelSource`/`ModelStorage` line); standalone binds `LocalFsStorage`, connected binds a `MetadataServerSource`. "Connected" is a *binding* chosen in project config, not a behavior. Extends T6/D-g: connected changes *where the world comes from*, never what compiling means. Resolves **LF-1**. · Rejected: α mode flag (mode-awareness smears across every phase); γ two compilers (drift — the ttrp-conform disease; composes later via B-6-γ if wanted); δ sync-only (can't carry live Designer/platform compile; its lockfile discipline survives in B-5).
- **2026-07-08 · [B-2 = β] · Statistics are a SEPARATE, snapshot-pinnable SOURCE KIND** (overlay beside models/worlds; worlds stay lean and stable). **Absent stats = defined degradation to the static cost model** (TTR-P Z v1), not an error. · Rejected: stats-in-world (volatile churn inside a stable document kind); live optimizer probing (unpinnable; parked as Z 3.x behind snapshot/replay); platform-side re-optimizer (two optimizers; artifact ≠ what the author reviewed).
- **2026-07-08 · [B-3 = α] · HARD PARITY: a compile is a pure function of its recorded snapshot; mode only chooses where the snapshot comes from.** Same program + same resolved inputs ⇒ byte-identical artifacts in both modes; every input (models, world, manifests, stats, plugin versions) is part of the recorded snapshot; `ttrp-conform` gains a mode-drift suite. · Rejected: parity-modulo-stats (unneeded — B-2-β makes pinning cheap); no-guarantee (two products by drift; P1 dies).
- **2026-07-08 · [B-5 = δ] · FETCH-THEN-COMPILE is the only connected shape.** The compiler never talks to the server mid-compile; a fetch step (explicit or policy-driven) refreshes the local snapshot; unreachable server = stale snapshot, nothing else — failure semantics collapse into freshness policy. D-g ("offline, no service at compile time") is preserved *verbatim* in both modes. · Rejected: fail-hard; silent fallback to repo files (P3 violation).
- **2026-07-08 · [B-6 = γ] · ONE compiler core, bound both ways:** CLI/IDE bind it locally; the Platform's compile/deploy service binds the same version server-side; B-3-α makes location a deployment convenience. Resolves **LF-5**. · Rejected: client-only; platform-only.
- **2026-07-08 · [BQ-1] · Snapshot transport = ARCHIVE/PROTOCOL** — content-addressed snapshot archives served by the metadata server, cached locally by hash; `LocalFsStorage` remains the plain-repo binding. · Rejected: repo-shaped file tree (slower; sync-flavored).
- **2026-07-08 · [BQ-2 = γ] · Stats keyed PER OBJECT: `{qname, object-schema-hash, observed-at, values}`; validity binds to the object's own resolved shape** — a mismatch discards that object's entry only (→ static cost model for it, granular + P3-explicit); **used values are embedded verbatim in the compile record** (δ composed — B-3-α replay reads them; the artifact shows the numbers the optimizer saw). World fingerprint stays stats-free forever. · Rejected: stats versioned with the world fingerprint (hourly churn, spurious T6 failures); whole-snapshot validity (one world edit invalidates untouched objects' stats).
- **2026-07-08 · [BQ-3 layered + BQ-4] · Freshness splits by content temperament: `ttr.lock` (COMMITTED) pins canon — models, world, manifests, EMIT-PLUGIN VERSIONS (BQ-4) — by content hash into the archive cache; fetch = a reviewable lock diff. STATS FLOAT under max-age auto-refresh, never enter the lock, used values recorded per-compile.** Flags: `--frozen` (CI default) / `--offline` (cache + recorded staleness). IDE: background refresh + advisory live-drift diagnostics; server push = later C capability. Reproducibility: canon by lock, stats by compile record — B-3-α holds team-wide (same commit ⇒ same canon). · Rejected: explicit-only fetch (stale-by-default); one policy for both temperaments; stats-in-lock (hourly diff noise).
### A / product split, scope & licensing

- **2026-07-08 · [A-1 = A-α] · The edition boundary rule = "COMPILE vs OPERATE": anything that turns source into artifacts (parse, check, optimize, emit, format, lint, view) = MIT; anything that runs, stores, schedules, secures, or serves shared state = Platform.** Grey-zone placements under the rule + D's decisions: **optimizer fully MIT** (incl. the stats-driven algorithms — the Platform's edge is *data*, per B); **`ttr-designer-server` MIT** (editor infrastructure, S24 — "view"); **conformance harness MIT**; **Designer frontend MIT** (Q-4-a); **emit-plugin *mechanism* MIT**, individual plugins may live either side (→ E). · Rejected: single-player-vs-multi-player rule (fuzzier at the run-helper edge); open-core feature matrix (drift, P1's enemy); everything-OSS (parked with monetization).
- **2026-07-08 · [A-2] · Principles P1 (standalone is not a demo), P2 (one-way arrow, build-time; runtime SPI plugins legal), P3 (no miracles) formally RATIFIED.**
- **2026-07-08 · 🟢 WORKSTREAM A IS CONVERGED.** Standalone-persona question (OSS-forever vs on-ramp) and monetization model → parking lot (product calls, not design blockers).

### D / repo & infra topology (full options + resolutions in `04-repo-topology-options.md`)

- **2026-07-08 · [D-1 = α] · The Platform gets a NEW repo** (name → J); re-founded services (metadata server, program door) are born there; transplanted leaves arrive via history-preserving `git filter-repo` moves. · Rejected: platform-in-monorepo (license wing kills the MIT repo's identity); repo-per-service (premature); kantheon-rename (inherits the single-query center C-1 re-founds).
- **2026-07-08 · [D-2 = α + Q-4-a] · LICENSE BOUNDARY = REPO BOUNDARY.** `tatrman` = 100% MIT; the platform repo = commercial license (name → J/A); no per-directory carve-outs. **The Designer frontend stays MIT in tatrman** — backend-selectable (MD6 adapter: browser-worker / loopback designer-server / platform metadata server); platform-only panels (presence, runs, lineage) ship as **platform-side extensions on an MIT-defined extension surface** (socket-and-plug, same pattern as the validator SPI). Resolves **LF-3** and **Q-4**. · Rejected: per-dir licensing; frontend fork; frontend moving platform-side (violates FI-2).
- **2026-07-08 · [D-3 = α] · Contract ownership rule: "TOOLCHAIN-TOUCHED ⇒ TATRMAN-OWNED (MIT); SERVICE-INTERNAL ⇒ PLATFORM-OWNED."** Plan protos, world/manifest schemas, snapshot/lock/stats formats, validator-SPI interface = tatrman; door/worker/internal protos = platform; kantheon owns nothing shared. **Dependency chain: `tatrman → platform → kantheon`, one-way, three tiers** (P2 generalized to the whole family). · Rejected: contracts repo (reverses TR-3 for no gain); contracts-as-TTR (parked, v2 dogfooding arc).
- **2026-07-08 · [D-4 = α now, δ direction] · Infra = a new olymp-shaped repo treated as "DEPLOYMENT INSTANCE #1"**; product packaging (helm charts) lives in the platform repo beside services; the declared graduation path is **ship-as-umbrella-chart/operator** (every deployment, ours included, an instance of the packaged product). · Rejected: shared olymp (FI-3; customers can't consume it); cluster defs in the product repo.
- **2026-07-08 · [D-5 = γ] · SPLIT SEQUENCING = STRANGLER with per-move mini-arc discipline** (move → adopt → delete): ① MD+TR arcs → ② platform repo bootstrap: metadata server v1 (B contract + Designer serving; pinakes verify-then-place here) → ③ program door + workers/Kyklop → ④ Charon → ⑤ Argos+SPI + whois-descendant (with H) → ⑥ kantheon query-door adoption arc → ⑦ scheduler (F) et al. **Closes CQ-1:** kantheon keeps its mini-spine throughout, adopts the door last — accepted, time-boxed, P2-legal duplication. · Rejected: big bang; greenfield-forever (steady-state P2 inversion).
- **2026-07-08 · [D-6] · Grey-zone sweep ratified:** kallimachos, report-renderer, echo, kadmos **stay kantheon**; **metis stays-but-flagged** ("a worker in disguise" — already Charon-stageable via `metis.v1`; the test case for engine-by-manifest when the worker SPI lands); **pinakes unverified** → verify-then-place task in ②.
- **2026-07-08 · [DQ-1/2/3] · Platform repo = Gradle-only** (all TS stays in tatrman per Q-4-a) **· proto/package roots swept on arrival** (TR/MD precedent) **· `tatrman-semantics` declared obsolete** (no interesting content; archive).
- **2026-07-08 · 🟢 WORKSTREAM D IS CONVERGED.** Post-design work handed off: two kantheon-repo arcs (query-door adoption ⑥; post-split doc sweep) + J naming demands (platform repo, infra repo — "Tatry" parked, extension surface, license name).

### C / service architecture (full options + resolutions in `03-service-architecture-options.md`)

- **2026-07-08 · [C-1] · Roster philosophy = β-SPINE + α-LEAVES, sequenced γ-ish.** Re-found the *execution spine* around the compiled program as unit of work (kantheon's spine is single-query-shaped — its known gap); transplant the *leaves* whose shapes are right (workers, Charon, Kyklop, whois). Sequencing: metadata server + spine + workers first; security/Designer-write/harvest follow. · Rejected: pure transplant (imports the single-query shape); pure re-found (re-designs correct services); control-plane-only (contradicts FI-3).
- **2026-07-08 · [C-2 = γ] · The metadata server is a NEW platform-native service on the `ttr-metadata` library**, designed for its four consumer classes (compiler seam, Designer, runtime, harvest) **+ two organs added: LINEAGE (first-class served graph) and EXPORT connectors (OpenMetadata et al. — outbound half of I)**; Ariadne's useful organs (refresh scheduling, QueryParseWorker pattern) are stolen; Ariadne stays kantheon-side as the agents' facade. Both this server and `ttr-designer-server` share the *library*, not the service. Resolves **LF-2**. · Rejected: Ariadne-comes-home (kantheon-shaped contract, "grows a second body"); grow-designer-server (betrays S24/G-b editor-infrastructure boundary); OpenMetadata-as-backend (TTR semantics second-class; demoted to an I connector).
- **2026-07-08 · [C-3 = γ] · Execution spine = TWO DOORS, ONE HALL.** **Program door** (deployed bundles — the Platform's native unit, F-proper's home, new build) + **query door** (one validated plan, synchronous — kantheon agents, Designer previews, debug; = slimmed Theseus chain) converge on the shared hall **Argos → Kyklop → workers**. **Proteus-the-service dissolves** (compile-time `ttr-translator` lib; E-a's concrete payloads make runtime translation dead for compiled work). Hands F the two-door scheduling/quota problem. · Rejected: wrap-the-old-spine (two orchestration layers; re-derives what the compiler settled); pure re-found (still needs a single-plan door); workers-pull (recorded as F's possible growth direction).
- **2026-07-08 · [C-4 = α] · Charon comes home** — transplanted as the Platform's movement service; the platform-world Transfer binding targets it (E-g realized literally). Charon-as-worker-kind re-examined when F exists.
- **2026-07-08 · [C-5-i = c] · Argos moves with the hall; its LLM Guard becomes a PLUGGABLE VALIDATOR SPI** (Platform defines the hook; Kantheon ships the plugin; no plugin = deterministic default). **Ratifies the P2 clarification (was CQ-3): P2 constrains *build-time* dependencies; runtime plugins conforming to Platform-defined SPIs are legal.** · Rejected: drop-only; hard runtime callback to Kantheon.
- **2026-07-08 · [C-6] · B's IOU discharged: the compiler-facing server contract (content-addressed snapshot archives + per-object stats endpoint) is confirmed buildable-cheap** over `ttr-metadata` (snapshot semantics + GitArchiveStorage precedent; MD arc's thin-skin evidence). Design note: archives are assembled from the *resolved* world, behind the same resolver the Designer uses.
- **2026-07-08 · 🟢 WORKSTREAM C IS CONVERGED.** Platform v1 service map: **metadata server (new) · program door (new) · query door (slimmed Theseus) · Argos+SPI · Kyklop · workers · Charon · whois-descendant (→H) · browser Designer (→G)**. Riders: C-5-ii/iii → H; CQ-1 + grey-zone placement sweep (kallimachos, pinakes, report-renderer, metis) → D; CQ-2 (run-store ownership) → F; CQ-4 (capability vocab mapping) = spine work item.

### F / scheduler & job execution (full options + resolutions in `05-scheduler-options.md`)

- **2026-07-08 · [F-1 = γ] · Scheduler shape = THIN TRIGGER LAYER + F-PROPER EXECUTOR in the program door.** Triggers (time/event/manual/API) are small and owned; execution semantics live in the door's executor; external orchestrators are *alternative frontends* calling the same door. Resolves **LF-6's F half** (E keeps the registered-orchestrator half). · Rejected: α own-full-scheduler (rebuilds mature machinery); β delegate-only (kills hero life 2 — GI-4 names running jobs *in the engines*; orphans the quota problem); δ metadata-server-reconciler (contradicts C-2-γ's four-consumer scoping).
- **2026-07-08 · [F-2 = β] · The deployed unit = the VERBATIM F-lite BUNDLE wrapped in a platform DEPLOYMENT ENVELOPE** (name@version, bundle hash, triggers, param + connection bindings, policy/principal refs, `{lock hash, compile record}` provenance). The door executes the manifest's wave graph (authoritative); `run.sh` = a rendering of it, the standalone/rescue path. **Resolves Q-3**; the envelope is B-4 made structural (everything the seam bans is what the envelope carries). δ (deploy-is-a-pointer, platform compiles from snapshot — sound by B-3-α) recorded as a later legal envelope variant (`source:` vs `artifact:`). · Rejected: α bare bundle (no home for platform state); γ new richer artifact (two artifact grammars = B-1's drift disease).
- **2026-07-08 · [F-3 = α + externalized run state] · Execution = a CENTRAL EXECUTOR in the program door walking waves** (per island Argos → Kyklop → worker; Charon per transfer edge), with run state in a durable store from day one — making β (executor-as-work-item) a later deployment refactor, and γ (reconciler/workers-pull, C-3-δ) the recorded growth direction, not the start. · Rejected: γ-now (rebuilds the hall — contradicts C-1 α-leaves; two dispatch disciplines); δ run.sh-on-a-runner (bypasses the hall = policy hole, disqualifying).
- **2026-07-08 · [F-4 package] · TTR-P F-proper's debt repaid; v1 executor manifest = FS + SS + runtime params (declared/typed, bound at trigger, run-date built-in) + per-island manifest-declared retries + wave-level snapshot-guarded resume + on-failure islands (no `absorbs`) + events (cron/manual/upstream-run). FF STAYS RESERVED** — F-b-β staging+swap is the designed mechanism when a program demands it; bash manifest untouched; T6 gates the vocabulary per world (platform-vs-bash manifests genuinely diverge for the first time). Manifest-as-artifact = FQ-4 work item.
- **2026-07-08 · [F-5 = γ minimal] · Two-door quota = PRIORITY + ADMISSION AT DISPATCH:** one pool, interactive > batch, batch yields at the dispatch slot, running work always finishes; pool labels (β) remain a deployment option (both are Kyklop config). δ identity-priced quotas → parking lot (H/multi-tenant). · Rejected: α FIFO-nothing (decision-by-drift on an explicitly flagged problem).
- **2026-07-08 · [F-6 = β] · Run store is EXECUTOR-OWNED; metadata server INGESTS EVENTS and serves all catalog/lineage reads.** Executor owns writes + "what is running now"; run-completion/lineage events feed C-2-γ's lineage organ; event contract = platform-internal proto (D-3). **Resolves CQ-2.** · Rejected: α metadata-owns (catalog on the execution hot path; availability coupling); γ shared-DB (coupling without an API); δ runs-as-TTR-documents (operational data ≠ canon).
- **2026-07-08 · [F-7 = γ] · BQ-5 DISSOLVES: `ttr.lock` stays PER PROJECT ROOT; the deployment envelope cites `{lock hash, compile record}`** — a program-scoped provenance slice from machinery B already ratified (B-3-α/BQ-2-δ); no new file kind; BQ-3's team-wide guarantee intact; nothing D decided is touched. **Resolves BQ-5.** · Rejected: β per-program locks (cross-program canon skew inside one commit — P3 smell).
- **2026-07-08 · [F-8] · C-4-β rider closed without a decision:** with F-3 = α, **C-4-α persists** (Charon = the movement service the executor calls); revisit condition re-pinned = the platform grows toward F-3-γ.
- **2026-07-08 · [FQ dispositions] · FQ-1 backfill OUT OF V1 (Bora)** → parking lot; **FQ-2** run-id schema + **FQ-4** executor-manifest artifact = planning-stage work items; **FQ-3** trigger topology = explicit non-decision (contract split is the design content; naming → J); **FQ-5** scheduled-run principal → **H** (envelope reserves the field).
- **2026-07-08 · 🟢 WORKSTREAM F IS CONVERGED.** Riders out: FQ-5 + F-5-δ → H; E owes the frontend-contract pressure-test (LF-6's other half); FQ-2/FQ-4 → planning.

### E / orchestration-engine integration (full options + resolutions in `06-orchestration-options.md`)

- **2026-07-08 · [E-1 = β] · Emit-target mechanism = JVM SPI PLUGIN ARTIFACTS.** SPI = tatrman-owned MIT (D-3); plugin identity pinned in `ttr.lock` (BQ-4 verbatim); **determinism is a stated SPI obligation** (B-3-α); plugins own the *orchestration layer only* — island payloads stay core-compiler emit; the bash emitter is **extracted** as the SPI's proving plugin (MD/TR discipline applied to the compiler's edge). δ (deploy-time adapters reading the manifest graph) = the platform's complementary half, split along the B-4 line. · Rejected: α in-tree-only (D-2 makes commercial targets impossible — contradicts A-1); γ template-driven (manifests carrying behavior — the shape B-T6 already rejected).
- **2026-07-08 · [E-2 = γ] · REGISTRATION = WORLD CONTENT.** Registering an engine (database *or* orchestrator) = an environment-instance entry (B-T6 layer 2: `extends` type manifest + deltas + connection *ref*) in the platform-managed world; **the metadata server is the registry because it serves the world** (C-2-γ); the compiler sees registered engines through the ordinary seam (B-1/B-4). The registry hunch confirmed with zero new concepts; secrets platform-side only (→ H). · Rejected: α dedicated registry (second source of engine truth — P3); β two registries by kind (re-splits T6); δ connector-is-the-registration (invisible runtime facts).
- **2026-07-08 · [E-3 = γ] · DELEGATION SHAPE IS WORLD-DRIVEN, from day one:** γ is the rule (shape = a world-declared invocation binding), α-1/β are its two day-one contents — **α-1 door-calling whole-program op = the platform-world binding** (hall intact); **β native DAG = the standalone-world binding** (an MIT emit target — *not a frontend*). α-2 per-island mirroring = later visibility upgrade with stated drift cost. **Pressure-test verdict: F-1-γ holds for door-calling shapes**; door frontend contract = `{start(envelope ref, params), poll/subscribe, cancel}` (largely F-6-β's operational API). · Rejected: either shape smuggled as the other; δ trigger-only = α-1's degenerate case.
- **2026-07-08 · [E-4 = β] · Harvest v1 = RUN-RESULTS ONLY:** per-orchestrator connectors map engine run events into F-6-β's ingest contract (delegated runs land in the same lineage graph); F-4-v-γ external events ride the same connectors; **connector SPI frame shared with I**. · Rejected for v1: α none (life-3 lineage dies); γ full harvest (front-runs LF-7); δ bidirectional (two sources of run truth).
- **2026-07-08 · [E-5 = β] · The bundle manifest schema (graph + fingerprints + checksums) becomes a DOCUMENTED-STABLE, VERSIONED, TATRMAN-OWNED (MIT) CONTRACT** (D-3) — it is load-bearing three ways (door execution, F-7 provenance, adapters). External-standard mapping (OpenLineage-shaped) lives in C-2-γ's export connectors, not the artifact.
- **2026-07-08 · [LF-4 RESOLVED] · The support-package synthesis:** one *concept* (world-declared engine with a manifest), two *mechanisms* (compile-time emit SPI · platform-side connector SPI), three deliverables per orchestrator (emit plugin · world instance entry · adapter+harvest connector).
- **2026-07-08 · [EQ-3 dive] · Anchor-orchestrator long list researched & written** (five tiers, ~27 contenders, scored on emit/frontend/harvest + manifest honesty + base). Structural findings: data-defined vs code-defined engines split the emit story; **the enterprise-WLA tier (Control-M, AutoSys, Stonebranch, …) = A-1's commercial-plugin market**, not anchor material. **Lean: Airflow 3 anchor · Kestra fast-follow (emit-fitness champion, JVM kin; second target proves the SPI) · Dagster third** (hero's mention was exemplary; asset-model impedance + community-grade OpenLineage). Bora's call pending.
- **2026-07-08 · [EQ-3 = both] · V1 EXECUTOR-TARGET SET = {bash, Airflow 3, Kestra} (Bora's call).** Consciously widens pick-one-anchor: the SPI-proving second target moves from fast-follow into v1 scope; Airflow 3 = harvest/base anchor, Kestra = emit-fitness/data-defined anchor, bash = the shipped F-lite floor. **Dagster = first post-v1 target**; WLA plugins demand-driven/commercial. · Rejected: single anchor (leaves the SPI unproven until v1.x); Dagster-in-v1 (three full support packages is scope, not proof).
- **2026-07-08 · 🟢 WORKSTREAM E IS CONVERGED.** Riders out: connector SPI frame + EQ-4 → I; secrets/trust → H; registration UX → G; EQ-1/EQ-2 → planning. **E+F both 🟢 → the penciled independent review session is now due** (process note, `next-steps-260708.md`).

- **2026-07-08 · 🟢 WORKSTREAM B IS CONVERGED.** The standing seam-legality rule for all future connected features = B-4's line: *the seam admits data (sources, registries, overlays) and diagnostics; never identity, never side effects.* Owed confirmations: C (snapshot-archive + per-object-stats server contract buildable-cheap); H (compile-time policy = advisory-only). BQ-5 (lock scope: per project root vs per program/bundle) → settles with F/D.

### H / security & governance (full options + resolutions in `07-security-options.md`)

- **2026-07-09 · [H-1 = γ + security-block sugar] · Policy = Rego/OPA bundles bound to metadata qnames, content-hashed canon; PLUS a TTR `security` block (MIT sugar, precedented by the E-R→DB shortcut and `semantics` block) that GENERATES standard policy fragments for common cases** (read grants, column masks/classifications, ownership); hand Rego for everything else. Pins: one-way deterministic generation; deny-overrides composition; never alters plans, T6-fingerprint-neutral; owner-declaration vs org-policy workflow line; row-predicates stay Rego-side. Grant-reference discipline → HQ-1 (with H-2). Grammar = TTR-M amendment (queued sweep). · Rejected: α admin-UI config (invisible authority); δ policy-in-world; β policy-as-TTR-truth (survives only as this sugar).
- **2026-07-09 · [H-8 = γ+β] · Q-1 ANSWERED / LF-8 RESOLVED: standalone security = integrity (trusted-principal stance, TTR-P Q8) + optional advisory lint against an exported/pinned bundle; ENFORCEMENT is inherently "operate" ⇒ the Platform's.** The E-3-β bypass line ratified: *credential-bounded, not policy-bounded* (platform-governed credentials resolve platform-side only; standalone bypass = credentials you already held). · Rejected: α zero-concept; lint-as-enforcement.
- **2026-07-09 · 🟢 WORKSTREAM H IS CONVERGED.** All eight forks resolved (see `07-security-options.md`). Discharged along the way: **FQ-5** (H-2) · **B's advisory-policy IOU** (H-3) · **C-5-ii/iii** (H-4/H-2) · **EQ-2's determinism widening** (H-6) · **E's bypass line** (H-8) · **Q-1/LF-8** (H-8). Riders out: HQ-2 → observability item; HQ-3 → G; HQ-4 → D-5 ⑥ work item; HQ-5 → H-7 β-step; security-block grammar → amendment sweep; bundle-hash field → F-6-β schema (planning). F-5-δ stays parked.
- **2026-07-09 · [H-7 = α-then-β] · Data plane: Argos transplants with its HOCON store VERBATIM (RLS/DENY/MASK = v1 by construction); a follow-up arc rekeys the store onto PDP bundles** (one policy discipline; data-plane decisions cite the bundle hash; security-block sugar feeds the same bundles). HQ-5 = the β-step's migration work item. · Rejected: rekey-at-transplant (migration inside a move); RLS-out-of-v1 (more work, hero life 2 dies).
- **2026-07-09 · [H-6 = β] · Trust = checksums/lock (existing) + publisher-signature verification for emit plugins (verify-if-signed v1, require-signed = policy knob) + the ttrp-conform DETERMINISM KIT (double-compile byte-compare; third-party certification — discharges the EQ-2 widening) + PDP-signed policy bundles. Two trust roots: IdP (identity) · publisher keys (artifacts).** γ SLSA post-v1, demand-driven. · Rejected: α-only; γ-now.
- **2026-07-09 · [H-5 = β+γ composed] · Secrets: SECRET-STORE SPI over external stores (K8s Secrets default, Vault/cloud bindings) + DISPATCH-TIME ENV INJECTION as delivery** (F-lite's `TTR_CONN_*` contract verbatim — parity both modes); refs resolve at dispatch, injection manifest-scoped per island/transfer; **never-at-rest invariant** (no material in envelope/run store/logs/artifacts — canary-tripwire-tested in CI); **no-secret-API rule** (no endpoint returns material; resolution = a dispatch side effect); store-unreachable = pre-flight island failure (same shape as unset env in bash-land); secret-zero = deployment config. Full dive in `07-security-options.md`. · Rejected: α native store (build-a-vault; rotation drift); γ-alone (no storage answer); δ per-worker sealed (governance unenforceable).
- **2026-07-09 · [H-4 = γ from α-transplant] · The whois-descendant = directory + PDP serving signed, content-hashed policy bundles; PEPs evaluate LOCALLY** (security server down ≠ hall down; fail-closed at bundle expiry); **run records cite the bundle hash in force** (F-6-β gains the field); security-block pipeline homes in the PDP bundle build (sugar fragments + hand Rego, deny-overrides, sign). Fills C-5-ii. · Rejected: transplant-alone (under-serves doors); metadata-server organ (coupling); policy-through-the-seam (revocation latency).
- **2026-07-09 · [H-3 = α] · v1 PEP map = minimal spine: deploy/run authz at the doors + transplanted Argos data plane + coarse catalog visibility; Designer writes ride git perms (HQ-3 reserved for G); compile-time policy = ADVISORY-ONLY — B's last owed confirmation DISCHARGED** (B-3-α forces it). β full-ACLs = growth shape. · Rejected: β day one; γ data-plane-only (deploy/compute unguarded).
- **2026-07-09 · [H-2 = α/ii/β] · Identity: IdP-verified JWT at every door via one shared ingress module** (bearer-only · enrichment-never-authority · fail-closed); machine callers = client-credentials service principals, same IdP; **scheduled runs execute as an explicit envelope-named service principal — required, v1 (Bora), discharging FQ-5**; deployers need a grant to *use* a principal. **HQ-1: classifications are the security block's native grant vocabulary** (org mapping binds classifications→roles); verbatim roles legal, fail-closed-validated at bundle build. · Rejected: gateway (asserted identity); platform accounts (second truth); deploying-user principal (offboarding ghosts).

### J / naming & conventions (full slate in map §J)

- **2026-07-09 · [J-1 = β] · Register rule: platform-NATIVE services = Slavic/Tatra names; TRANSPLANTS keep Greek** — the naming archaeology mirrors C-1 β-spine+α-leaves. · Rejected: Greek-continues; descriptive-only; rename-everything.
- **2026-07-09 · [J-2..J-5] · The slate:** `tatrman-platform` + **`tatry`** (infra) · **VELES** metadata server · **PERUN** PDP (the Perun-and-Veles oath pairing: knowledge vs authority) · **RADEGAST** executor (Bora's pick; Svarog/Svetovid alternates on file) · **ZORYA** triggers · Theseus/Argos/Kyklop/Charon/Cyclopes keep names · doors stay contract vocabulary · editions **Tatrman / Tatrman Platform** (no community/lite — P1) · license = Tatrman Platform License (text = D-5 ②) · **coordinates: `org.tatrman:*` MIT, `cz.tatrman:*` Platform (Bora — both domains owned; the group id makes the D-2 license boundary physical in Maven)** · `ttr-emit-*` / `ttr-connect-*` · "Designer Extensions".
- **2026-07-09 · 🟢 WORKSTREAM J IS CONVERGED — THE DESIGN EFFORT IS FULLY CONVERGED (A–K all 🟢).** Remaining: Q-6 acceptance statement (Bora) · `design.md` + `detailed-design.md` · `/planning`.

### Consolidation sweep (2026-07-09 — S-items batch-ratified, Bora)

- **2026-07-09 · [S1] · TTR-P/TTR-M amendment sweep queued as ONE batch** (record in TTR-P docs + contracts changelog, the `.ttrl` discipline): F-4-i runtime params · F-4-iv on-failure islands · FQ-4 Tatrman-the-executor manifest · E-5 manifest-contract graduation + lineage section · **T6 type-manifest ownership** (emit plugins ship executor-type manifests) · **K's `extends`-platform-world surface** (T4-adjacent) · **H-1 `security`-block grammar** (TTR-M).
- **2026-07-09 · [S2–S5] · Confirmed as recorded:** CQ-4 = spine-extraction work item (mapping layer first) · pinakes verify-then-place in ② · metis revisit = worker-SPI landing · kantheon-repo arcs (⑥ + doc sweep) = post-design work there.
- **2026-07-09 · [S6] · The post-split doc sweep includes THIS repo:** CLAUDE.md's "never talks to those services" → "the MIT toolchain is a client of the platform's published snapshot contract" (`ttr fetch`, `MetadataServerSource`).
- **2026-07-09 · [S7] · Audit + observability ride the F-6-β discipline:** PEPs/executor *emit* events (authority decisions w/ bundle hash, run transitions, island logs by reference), the metadata server *ingests and serves queries*; log payloads live in the executor's run store. One event spine, no new architecture. **Closes HQ-2 and the parked observability item's shape** (sizing/retention = planning). Parking-lot entry updated.
- **2026-07-09 · [S8] · Q-6 (v1 acceptance statement) remains OPEN — Bora's, before `/planning`;** strawman on offer.

### De-dirty pass (2026-07-09 — C/E/F return to 🟢)

- **2026-07-09 · [CQ-5 = compiler-side] · Column lineage is derived AT COMPILE and emitted into the bundle manifest (E-5 lineage section) / compile record** — MIT (D-3); reviewable artifact content; delegated runs get lineage free; T6 honored. **Pinned: v1 column lineage is STATIC** (per compile); run events attach context and *cite* the manifest section (F-6 rider settled). · Rejected: server-side plan parsing at ingest (re-derivation).
- **2026-07-09 · [CQ-6 pinned] · Deployed bundles verify their T6 fingerprint; ad-hoc query-door plans validate against the metadata server's currently-served resolved world.** No new mechanism.
- **2026-07-09 · [FQ-6 = two staging scopes] · Attempt-scoped staging wiped per retry; RUN-SCOPED staging retained while parked-resumable under an envelope-declared retention policy; at expiry the run DEGRADES resumable→restart-only (P3-explicit refusal); Charon Evict cleans on success/abandonment/expiry.**
- **2026-07-09 · [FQ-7 dissolved] · The door executes MANIFESTS, not compilers: compatibility keys on the E-5 manifest schema version** (current + N-1 min; window = planning); deployed envelopes untouched by toolchain upgrades (hash-pinned); server compiles use the platform's pinned toolchain, recorded.
- **2026-07-09 · 🟢 C, E, F ALL RE-CONVERGED** (E's remaining item = the T6 amendment, queued sweep bookkeeping).

### I / external metadata & megaproviders (full options + resolutions in `09-external-metadata-options.md`)

- **2026-07-09 · [I-1 = α] · "PROPOSALS IN, PROJECTIONS OUT — never sync, never federate."** Inbound = human-ratified TTR-document proposals (drift detection automatic + advisory); outbound = C-2's derived-view exports; no object has two writers ⇒ conflict machinery never exists. **Resolves LF-7 — all eight load-bearing forks now closed.** · Rejected: sync (external mutation of canon, P3); federation (unpinnable — B-5-δ bans it from compile); OpenMetadata-as-backend (demoted by C-2).
- **2026-07-09 · [I-2 = α · I-3 = α · I-4 = α] · One connector frame (EQ-4 ✓: shared with E, widened to two output kinds) under the BQ-3 temperament split (schemas → reviewed proposals; stats → BQ-2 store direct); ONE-SHOT `ttr import-schema` = MIT, continuous harvest = Platform (A-α line); proposals are PR-SHAPED — robots write through git too (G-1-γ universalized), deterministic generation; megaproviders = connector families, IQ-1 (PowerBI `md` mapping) carved out to a TTR-M modeling arc.** · Rejected: private harvester organs; auto-commit canon; per-ecosystem special integrations.
- **2026-07-09 · [I-5 = OpenMetadata] · Anchor external system = OPENMETADATA (Bora)**: v1 = its import+export connector pair; **DataHub = first follow-on, consciously not widened into v1** (EQ-3's second-target argument noted and declined here — one mapping, not three deliverables). · Rejected as anchors: Amundsen (momentum); Collibra/Purview (commercial-connector tier).
- **2026-07-09 · 🟢 WORKSTREAM I IS CONVERGED.** Riders: IQ-1 → TTR-M planning; IQ-2/IQ-4 → connector mapping spec (planning); proposals panel → G v1.x.

### G / Designer evolution (full options + resolutions in `08-designer-options.md`)

- **2026-07-09 · [G-1 = γ] · WRITES-THROUGH-GIT: edit session = branch, save = commit (via the `WorkspaceEdit` path), publish = repo-policy-decides (git's own governance, not an invented one); multi-user = git semantics + advisory presence; conflicts = review flows.** E-2-γ/K/H-3 presumptions confirmed — nothing re-renders. Live co-editing recorded as the *within-session* future (two users, one branch workspace — composes later). **Timing (Bora): read-only first, edits second — designed now, built second.** · Rejected: CRDT-as-write-model (research project over a text projection; kills text-is-canonical); locking (advisory theater beside git); never-writes (contradicts FI-4; registration needs writes).
- **2026-07-09 · [G-2 = γ · G-3 = γ] · Edit idioms split honestly (graph ops for structure, embedded text for content — one WorkspaceEdit path); sessions hybrid (stateless reads for the majority; "edit" materializes a server-side branch workspace on demand; previews ride the query door under the session principal).** · Rejected: ops-only (vocabulary lags the language); text-only (abandons the graphical point); browser-only workspaces (fragile); sessions-for-readers (waste).
- **2026-07-09 · [G-4 = α + GQ-2 in] · v1 = READER-FIRST: catalog · model graph views · runs · COLUMN LINEAGE · registration wizard (one wizard, kind-parameterized — GQ-3) · READ-ONLY TTR-P PROGRAM GRAPHS (from the E-5 manifest — Bora: "graph of the TTR-P for sure"); edit mode graduates from behind a flag.** · Rejected: workbench-v1 (scope); registration-only (wastes the frontend).
- **2026-07-09 · [G-5 ratified] · Standalone statements on the record: the IDE view-only path is PERMANENT (not transitional); standalone has NO data preview (preview = execution = "operate" — stated as a decision, review input #2); `.ttrl` schema shared with TTR-P C1, never forked (GQ-1 coordination).**
- **2026-07-09 · 🟢 WORKSTREAM G IS CONVERGED.** Discharged: **HQ-3** (git perms = write enforcement; H's reserved PEP unused in v1) + both review inputs. GQ-4 (edit-session quotas) = named ops item.

### K / world composition (options + resolution in the map §K)

- **2026-07-09 · [K = β + α-as-mechanics] · Project worlds DECLARE the platform world they extend (visible project text; T4-adjacent grammar → amendment sweep); the metadata server's resolver composes behind the declaration; the lock pins the platform world by content hash** (BQ-3's "the platform's view of the world changed" lock diff made literal). Pins: **contradiction = compile error** (platform entries authoritative; projects add/extend, never contradict); **platform world = its own git repo** (admin edits = commits; edit rights = H-3 policy object); **standalone parity** via file-exported copies (B-1 two-bindings); **project roster = server config**, not world content. **Resolves Q-5**; unblocks G registration UX + C-2's multi-repo serving model. · Rejected: α-alone (invisible composition, P3); γ THE-world (structural standalone/connected asymmetry, P1); δ copies (N truths).
- **2026-07-09 · 🟢 WORKSTREAM K IS CONVERGED** (same-day open-and-close; single load-bearing fork).

### Review incorporation (2026-07-09)

- **2026-07-09 · [REV-1] · review-260708 RATIFIED (Bora): all findings accepted and incorporated.** C/E/F → 🟠 converged-but-dirty (dirt listed per doc); workstream **K** (world composition) opened; Q-5/Q-6 logged; observability + Charon-quota-hole parked *named*; TTR-P amendment mini-sweep queued (F-4-i params, F-4-iv error-flow, FQ-4 executor manifest, E-5 contract graduation, **+ the silent T6 type-manifest-ownership amendment** — plugins ship executor-type manifests). · Why: independent-review protocol; every finding ground-truth-checked. · No finding rejected.
- **2026-07-09 · [F-2 amended] · The δ pointer-deploy variant cites `{lock hash + compile record}` (recompile-from-recorded-stats), never the lock alone.** Stats aren't in the lock; a lock-only pointer floats stats and deploys a plan the author never reviewed — B-2-δ's rejected failure returning. **Resolves Q-7.** · Rejected: lock-only pointer ("sound by construction" was overstated as recorded).
- **2026-07-09 · [C-2 amended] · COLUMN-LEVEL LINEAGE IS V1 (Bora's call).** The C-2-γ lineage organ serves column grain, derived from compiled plans/expressions (derivability established, TTR-P review §4.4); export = OpenLineage column-lineage facet. Overrides review §3.8.5's "pin in planning" with a decision. **Opens CQ-5** (derivation point: compiler-emitted into manifest/compile record — D-3/T6 lean — vs server-side plan parsing at ingest); riders: F-6 event contract carries/references column lineage; E-5 manifest gains a lineage section if CQ-5 lands compiler-side. · Rejected: object-grain-only v1 (a different, weaker product promise).
- **2026-07-09 · [EQ-2 widened] · Plugin-determinism VERIFICATION added to EQ-2's scope** (double-compile byte-compare kit + certification requirement; BQ-4 pins identity, not behavior). Rides with H's trust fork.
- **2026-07-09 · [K opened] · World composition = its own workstream** (map §K: K-α merge-at-assembly · K-β explicit import/extends · K-γ platform-world-is-THE-world · K-δ rejected-shaped per-project copies). The platform-managed world is assumed by E-2-γ/C-6/hero-life-2 and designed nowhere; multi-project instance truth must be one truth by decision. → **Q-5**; schedule before G converges registration UX.


### Naming amendment (2026-07-10 — ecosystem strategy session)

- **2026-07-10 · [J-v2 — supersedes J-1/J-2's register rule] · FUNCTIONS NAME CONTRACTS, MODULES, AND REPOS (`ttr-<function>`, proto `org.tatrman.<function>.v1`); persona names restricted to a CLOSED survivor list — Veles (metadata), Perun (PDP), Charon (transfer); Kantheon-internal: Golem (+`shem`), Pythia, Iris — and personas NEVER appear on the wire (proto packages, Maven/npm coordinates, published chart names).** Renames: Theseus→ttr-query · Proteus→ttr-translate · Argos→ttr-validate · Kyklop→ttr-dispatch · Arges/Brontes/Steropes→ttr-worker-{postgres,mssql,polars} · Echo→ttr-fuzzy · Kadmos→ttr-nlp · Prometheus→ttr-llm-gateway (proto `llm.v1` — dissolves the llmgateway/prometheus proto collision) · whois→ttr-identity · Ariadne→Veles (proto `meta.v1`) · Radegast/Zorya retired pre-birth → ttr-run / ttr-schedule. Product naming: the open spine ships as **"Tatrman Server"**; brand architecture fixed at four names (Tatrman · Tatrman Server · Tatrman Platform · Kantheon = persona space). Execution order: rename BEFORE the publish gates — full ledger + sweep plan in [`naming-260710.md`](./naming-260710.md). · Why: personas proved a comprehension tax (Argos/Arges) and leaked into contracts (llmgateway/prometheus); ecosystem strategy session 2026-07-10. · Rejected: full persona register (original J-1 β); zero personas (Veles/Perun/Charon carry real semantics — what is known, what is allowed, what moves between worlds — and sentiment has value).

### Ecosystem redraw amendment batch (2026-07-10 — strategy session + re-open session)

> STRAT-1..9 were ratified in the 2026-07-10 ecosystem strategy session (recorded in `../../next-steps-260710.md` §2, formalized here); RO-1..14 were ratified by Bora in the re-open session the same day.

- **2026-07-10 · [STRAT-1 · core vs satellites] · The ecosystem splits into the CORE ("prepare your data for AI consumption" — the piloted read spine: TTR-M + metadata serving + translate + validate/RLS + dispatch + workers + MCP surface + resolver + grounding + reference agents) and three PARKED SATELLITES:** (a) TTR-P processing family + graphical write designer + optimizer arcs; (b) entry/budgeting (the PF frontends effort); (c) **the operate tier** (ttr-run/ttr-schedule, Charon movement, Perun PDP, envelopes, bundles-as-deployed-units, harvest scheduling, the event spine). Satellites are parked **by sequence, not by doubt**; each re-opens on named evidence (first program workload / first planning workload / first operated estate). · Product+OSS-core top-down strategy. · Rejected: build-the-platform-first (the PL plan's implicit sequencing).
- **2026-07-10 · [STRAT-2 — supersedes A-1] · The edition rule "compile vs operate" is replaced by "INTEROPERATE vs ADMINISTRATE":** everything an adopter needs to prove the promise end-to-end is OPEN — the whole read spine moves to the open side, including the query door (ttr-query), the validator **with RLS** (ttr-validate), the dispatcher, the engine workers, Veles, the MCP tools, resolver, grounding, and the reference agents. Commercial remains: the operate tier, enterprise policy administration (Perun-the-organization; the open side keeps the enforcement point), continuous harvest/export connectors, Designer ops/deploy extensions, advanced/vertical agents. · "Policy-honest by construction" is the headline claim — enforcement cannot be paywalled without killing it; rule of thumb (Bora): **"there must be a meaningful OSS core."** · Rejected: A-1's compile/operate line (paywalls the proof of the promise).
- **2026-07-10 · [STRAT-3 — amends FI-2/J-5] · MIT → APACHE-2.0 for everything open** (patent grant; costs nothing). `cz.tatrman:*` = commercial tier unchanged; **`org.tatrman` now means Apache-2.0** and its scope grows per STRAT-2. Mechanics (LICENSE swap, headers, NOTICE, doc sweep) = a core work item. · Rejected: staying MIT (no patent grant for language/infra work).
- **2026-07-10 · [STRAT-4 — extends J-v2] · The open spine ships as "TATRMAN SERVER"** (one product name, one chart, many services). Brand architecture fixed at four names: **Tatrman · Tatrman Server · Tatrman Platform (parked) · Kantheon**. Editions vocabulary extended accordingly. · Rejected: per-service product names; "community edition" vocabulary (P1 stands).
- **2026-07-10 · [STRAT-5 — supersedes Q-6 as the near-term bar] · The ratified Q-6 acceptance statement is OPERATE-TIER scope — parked with satellite (c);** it remains Tatrman Platform's bar when that tier wakes. The near-term bar is the **Tatrman Server v1 acceptance statement** (ratified trimmed → RO-3). · Rejected: keeping Q-6 as the near-term bar (gates the debut on parked machinery).
- **2026-07-10 · [STRAT-6 — amends D-5] · Strangler re-sequenced by the redraw:** ①② stand (metadata/translator extraction; Veles v1). ③'s hall content is **done in kantheon** (per `kantheon/docs/architecture/fork/extraction-inventory-260710.md`) and is open-spine work — the program door (ttr-run) parks with the operate tier. ④ (Charon) and ⑦ (ttr-schedule) park with satellite (c). ⑤ splits: **ttr-validate = spine (open, done)**; Perun-the-org = operate (parked). ⑥ (kantheon query-door adoption) remains, inside the new critical path. **The strangler's "move out of kantheon into tatrman-platform" premise is inverted for the spine: the spine stays in its lineage and moves to `tatrman-server` (RO-1); the November "strangler" is ai-platform's repoint (STRAT-9).** · Rejected: executing D-5 ③–⑦ as planned.
- **2026-07-10 · [STRAT-7] · Golem ships as TWO open reference implementations:** Python + LangGraph (pedagogical, aggressively documented) and Kotlin + Koog (Kantheon product lineage) — two frameworks, one MCP contract; purpose: prove the MCP surface is the contract. Shared conformance conversation suite. (Python provenance = OQ-3; second Golem is a stretch clause per RO-3.) · Rejected: single reference (doesn't prove the surface).
- **2026-07-10 · [STRAT-8 · promotions] · Two formerly minor items become strategic arcs:** **`ttr import-schema`** (the standard's brownfield front door — promoted from I-2's one-shot-CLI footnote to a named arc; the E-R first-cut derivation is the valuable half) and **the TTR-M → Power BI / OSI semantic projection** (promoted from parked IQ-1 to the channel-bridge product — "modeling pays twice"). · Rejected: leaving both as footnotes.
- **2026-07-10 · [STRAT-9 · November refactor = the strangler, merged] · The DFP contract obligation (engagement ends Nov 2026) is discharged by INVERTING extraction: ai-platform is refactored to CONSUME published `org.tatrman` artifacts** (never by moving code out of it). **DFP = deployment instance #0.** Execution plan = the extraction inventory (committed, kantheon). · Rejected: extracting from ai-platform.
- **2026-07-10 · [RO-1 — resolves ecosystem OQ-1; amends D-1] · The open spine lives in a NEW `tatrman-server` REPO** (Apache-2.0, `org.tatrman`): spine services move from kantheon via the same history-preserving discipline, **with the N1 naming sweep folded into the move (rename-on-arrival — one migration, not two)**; kantheon keeps agents + the commercial/experimental surface; `tatrman-platform` remains reserved for the operate tier. D-2 ("license boundary = repo boundary") survives per repo across the three-repo public topology. · Rejected: opening kantheon wholesale (drags agents into the public standard; breaks D-2); spine-into-tatrman-monorepo (mixes toolchain with services; breaks the repo grammar).
- **2026-07-10 · [RO-2 · plan shape] · The PL plan set (`../implementation/`: architecture, contracts, plan, 24 task lists) is FROZEN as the operate-tier reference** for satellite (c)'s wake-up — amended only by tier-disposition banners, never rewritten. **The core gets its own thin documents at `docs/ecosystem/server/`** (architecture.md + plan.md) referencing this decision log and the extraction inventory. This control room remains ground truth for design decisions across both tiers. · Rejected: amending the PL plan in place (entangles parked reference with live sequencing); docs/ecosystem/platform/implementation/ or docs/ecosystem/ as the core-doc home (folder names mislead).
- **2026-07-10 · [RO-3 — resolves ecosystem OQ-7; the near-term acceptance bar] · The Tatrman Server v1 acceptance statement, ratified TRIMMED:** *"Tatrman Server v1 is done when, on a fresh single-chart deployment by someone who is not us, from public artifacts and public docs alone: a TTR-M model bootstrapped by `ttr import-schema` from an existing database and hand-refined in the IDE is served by Veles; a question asked through the MCP surface by any MCP agent — the reference Golem or a third-party one — returns a governed answer with row-level security applied in the plan and full provenance attached; the maker loop closes (edit model → commit → refresh → the agent understands); and the reference Golem passes the conformance conversation suite. All Apache-2.0."* **Named stretch clauses (on the critical path, non-gating):** the same model projects to a Power BI semantic model (STRAT-8 arc); a second reference Golem (Python + LangGraph) passes the same suite (rides OQ-3). · Why trimmed: the PBI projection is a whole promoted arc and the Python Golem's provenance is unresolved — neither may hold v1 hostage. · Rejected: strawman verbatim; deferring the bar to planning.
- **2026-07-10 · [RO-4 — reframes FI-3] · The connected-mode server is OPEN: the mode seam is now an open↔open contract.** FI-3's identity "connected = the (commercial) Tatrman Platform" is superseded — Veles and the whole B contract sit on the Apache side; the license seam no longer coincides with the mode seam ("interoperate vs administrate" cuts orthogonally to standalone/connected). FI-1's two first-class modes, B-1..BQ-4, and B-4 seam legality are untouched. · Rejected: none — this is the redraw's direct consequence, recorded so §2's framing reads correctly.
- **2026-07-10 · [RO-5 — amends C-2] · Veles = the LIVE, EXTRACTED service** (Ariadne renamed per J-v2 — "Ariadne was only the fork-transplant name"), grown toward C-2-γ's four-consumer design; **"Ariadne stays kantheon's facade" is dissolved.** The B-contract organs (snapshot-archive serving, per-object stats) are the *unbuilt* part; lineage organ + export connectors ride their tiers (export = operate/continuous, one-shot projections = core direction). · Rejected: building a second, platform-native Veles beside the live one (two bodies — C-2-α's disease, returning).
- **2026-07-10 · [RO-6 — redraws D-3's chain] · Dependency chain: `tatrman → tatrman-server → {tatrman-platform, kantheon}`** — the operate tier and the agents both consume the Server's published artifacts; **no build-time edge between `tatrman-platform` and `kantheon` in either direction**; runtime plugins on published SPIs stay legal (the P2 clarification unchanged, e.g. kantheon's LLM-Guard validator plugin). D-3's ownership rule keeps its form, with the strategy session's note made explicit: **ownership ≠ license tier — keep both columns.** · Rejected: keeping the linear three-tier chain (platform is no longer between tatrman and kantheon).
- **2026-07-10 · [RO-7 — splits H by tier; answers the open-policy gap] · Core keeps the ENFORCEMENT half:** the H-2 ingress module (bearer-only · enrichment-never-authority · fail-closed — live at the pilot edge), the ttr-validate data plane **incl. RLS** (H-7-α HOCON store verbatim), and `ttr-identity` (**H-4's "whois → Perun" is superseded**: whois's descendant is open-roster identity mapping/enrichment; Perun = the future PDP, parked). **Open-side policy administration (Bora): HOCON-IN-GIT** — the live validator policy store, versioned and git-governed ("robots write through git" covers it); the H-1 `security`-block sugar targets it in a follow-up open arc (generator → validator config, unsigned, git-reviewed); **Perun adds directory sync, bundle signing, and fleet administration when the operate tier wakes** (H-4/H-7-β park with it). · Rejected: sugar-generator in Server v1 (critical-path weight before November); minimal open bundle-builder (new machinery the pilot doesn't have).
- **2026-07-10 · [RO-8 — adds to the D-3 inventory] · The MCP SURFACE is a NAMED, TATRMAN-OWNED (Apache) CONTRACT:** the tool schemas of `ttr-meta-mcp` / `ttr-query-mcp` (+ `ttr-fuzzy-mcp`, `ttr-grounding-mcp`), per-user identity pass-through semantics, and the **conformance conversation suite** as its executable test artifact (STRAT-7's proving harness). It is the ecosystem's consumption contract and was missing from the contract inventory. · Rejected: leaving it implicit in service protos (the debut's most load-bearing surface would be undocumented).
- **2026-07-10 · [RO-9 — amends G-4's tiering] · Designer (Bora): the VIEWER (models incl. `.ttrl` files) ships in Tatrman Server v1** — in the chart, not an acceptance clause; **the modeler WRITER (edit mode) = Server v2**; **the TTR-P designer (program graphs + graphical write designer) = Platform / satellite (a)**. Runs/lineage panels stay Designer Extensions (operate). G-1..G-3 write-model decisions untouched (they wake with the writer). · Rejected: Designer in the acceptance bar (hard adapter dependency); Designer out of v1 entirely (the visual half of "modeling pays").
- **2026-07-10 · [RO-10 — tiers E-2-γ] · Server v1 keeps the LIVE CONFIG mechanics for engine/connection registration** (worker connections, shem environment declarations); **E-2-γ "registration = world content" remains the ratified convergence direction**, adopted when the operate tier wakes or when the Designer registration wizard needs it. Explicit tiered adoption, not drift. · Rejected: world-content registration in v1 (grammar + resolver + migration on the pre-November path).
- **2026-07-10 · [RO-11 — splits the S1 batch] · The "one batch" S1 amendment sweep is superseded by the tier split; each tier records its sub-batch when it wakes:** core-now = none blocking (the `security`-block grammar rides RO-7's follow-up arc; K `extends`-platform-world rides RO-10's adoption); satellite (a) = F-4-i params · F-4-iv on-failure · FQ-4 executor manifest · E-5 manifest-v2 graduation · T6 type-manifest ownership. · Rejected: executing S1 as one batch now (amends parked grammar for parked machinery).
- **2026-07-10 · [RO-12 — tiers D-4] · `tatry` parks with the operate tier; the Server's single chart lives in `tatrman-server`** (D-4's charts-beside-services pattern applied per repo; ship-as-umbrella-chart is the Server's packaging from day one). Deployment-instance vocabulary: **DFP/ai-platform = instance #0**; `tatry` (when it wakes) = the operate-tier instance repo. · Rejected: bootstrapping tatry for the Server (an infra repo for a product that ships as a chart).
- **2026-07-10 · [RO-13 — disposes contracts.md v1.1 ⚑ flags per ecosystem OQ-8] · Split review:** core-relevant flags (**snapshot archive §2, `ttr.lock` §3, stats schema §4, plan protos**) reviewed **before the publish gates** (a core-planning session item); operate-tier flags (**envelope §13, event spine §14, door API §15, policy bundles §19, sizing ledger §20**) park with satellite (c) and are ratified at its wake-up (the former PL-P0.S2 slot). · Rejected: one review pass over all flags now.
- **2026-07-10 · [RO-14 · B-contract timing] · Schemas pin now (with RO-13's core review); Veles's snapshot-archive serving = core work (connected mode is open↔open per RO-4); the per-object stats store/endpoints are BUILT WITH SATELLITE (a)** — the optimizer is their only consumer, and B-2's defined degradation (absent stats → static cost model) makes the deferral safe by construction. · Rejected: building the stats organs for v1 (serving data nothing consumes).

- **2026-07-10 · [RO-15 — resolves ecosystem OQ-5] · THE STEWARD ENTITY = COLLITE** (est. 2009, 100% Bora-owned, no other active business, registry clean, software within registered scope). Collite wears all five hats: trademark owner · public-infrastructure owner (GitHub org, `tatrman.org`/`.com`/`.cz` — domains currently personal, transfer before the publish gates) · contribution counterparty (OQ-12) · commercial licensor (`cz.tatrman`, Tatrman Platform License) · the entity diligence reads. · Why: functionally a dedicated vehicle, with seventeen years of registry history for free. · Rejected: new entity (setup + admin for no added benefit given Collite's cleanliness); personal ownership (key-person risk — the exact question a steward answers); foundation (premature; **named as the graduation path** if the standard takes).
- **2026-07-10 · [RO-16 — shapes ecosystem OQ-6] · TRADEMARK: WORD MARK "TATRMAN", EUTM DIRECT, APPLICANT COLLITE, CLASSES 9 + 42** (class 41 education/training under consideration — Data Academy, partner enablement). Clearance check stays Bora's (~1 h: TMview, "tatrman" + `TATR-*` in 9/42 — the TATRA-adjacency scan) and gates filing; file **before Aricoma diligence reads the repo**. · Rejected: CZ-first-then-EUTM-priority (a second deadline for no gain; CEE beachhead wants EUTM anyway); figurative mark first (word mark is the stronger, styling-independent right).
- **2026-07-10 · [RO-17 — resolves ecosystem OQ-9] · PUBLIC HOSTING SLATE:** GitHub org = **`tatrman`** — the name is Bora's own dormant 2020 user account (verified this session); recover access (Google-SSO collision → GH Support if needed), then **transform user → organization**; interim home = the Collite org (repos already there). **Maven Central = the public registry** for `org.tatrman` (namespace verified via tatrman.org DNS ⇒ rides the RO-15 domain transfer); **GitHub Packages = staging/pre-release only** — it requires auth even for public Maven downloads, disqualifying it for a "stranger from public artifacts alone" bar (this reinterprets SV-P1's gate 1 as the staging step). TS side: npm org + VS Code Marketplace / Open VSX publisher. Images + the Server chart: **GHCR** (OCI, anonymous pulls). Docs: **docs-as-code** — static generator in-repo, CI-built, served under `tatrman.org` (generator pick = SV-P4 start). **The publisher PGP key for Central signing = a Collite-held asset and the H-6 trust root.** · Rejected: GH Packages as the public registry (auth wall); fallback org names (the mark, domain, and org should all say tatrman).
- **2026-07-10 · [RO-18 — resolves ecosystem OQ-2 mechanics + OQ-12] · LICENSE HEADERS = SPDX ONE-LINERS** (`SPDX-License-Identifier: Apache-2.0` + copyright line; full text once in LICENSE; NOTICE minimal: "Tatrman — Copyright 2026 Collite" + accrued attributions). **CONTRIBUTIONS = DCO, not CLA.** Why DCO: Apache-2.0 inbound=outbound already lets the commercial tier consume contributions — the only unique power a CLA adds is unilateral relicensing of the open code, and *visibly not holding that power* is worth more to a standard than holding it; friction matters at the margin where contributors are won. "Robots write through git" extension: **agent-generated patches carry a human `Signed-off-by` — the human takes DCO responsibility.** · Rejected: full 11-line boilerplate per file (noise); CLA (friction + the HashiCorp-shaped power a credible standard must not want).
- **2026-07-10 · [RO-19 — resolves ecosystem OQ-3; ratifies the OQ-4 approach] · THE DFP CONVERSATION = FOUR ESCALATING ASKS** to the single decision-maker (the owner): ① bless the November repoint frame (ai-platform = instance #0 consuming the open standard — their win: maintained OSS under their platform, no bespoke lock-in) → ② evidence pack (analyst-IDE shot preferred) → ③ contributions, as a short copyright-holder letter to Collite: grounding (chrono/geo/money), optionally the Python Golem, **and a synthetic/anonymized derived conversation corpus for the conformance suite (new ask — the pilot's real Czech traffic is the best seed, but only as a derivation)** → ④ founding-adopter framing (public reference; can wait if short). **OQ-3: the Python + LangGraph Golem = TEXTBOOK REWRITE as the default** — a teaching artifact written fresh against the MCP contract with the conformance suite as its test bed; a DFP contribution is an accelerant/source-to-crib, never a dependency (it is already a non-gating stretch clause per RO-3). · Rejected: gating the stretch on DFP; converting engagement code into a textbook.
- **2026-07-10 · [RO-20 — closes ecosystem OQ-11's proto half; amends the N2 ledger step] · VERIFIED IN-REPO: `plan.v1` / `transdsl.v1` / `dfdsl.v1` HAVE LANDED in tatrman** (`packages/kotlin/ttr-plan-proto`, functional packages, README + wire round-trip tests present); **kantheon carries no copies** — clean artifact consumption, no inlining. **FINDING: the artifact also ships `org.tatrman.proteus.v1` (translator enums `Language`/`SqlDialect`, forked from ai-platform 2026-06-13), byte-identical duplicate in kantheon `shared/proto` — a persona package on the wire, violating J-v2; the N2 step ("already functional — unchanged") missed it. FIX BEFORE GATE 2: rename → `org.tatrman.translate.v1` in the artifact; kantheon deletes its duplicate and consumes the artifact.** Remaining OQ-11 half: the worker.proto delta vs ai-platform (kantheon's `worker.v1` = 12.6 KB, package clean; ai-platform repo not reachable this session). · Why recorded: rename-before-publish is the invariant — caught pre-gate it is a 30-minute change, post-publish a permanent wart.
- **2026-07-10 · [RO-21 — closes ecosystem OQ-11 in full] · PROTO-FAMILY DRIFT VERIFIED BY DIRECT DIFF (ai-platform repo connected):** (i) **worker.proto delta = kantheon is an additive superset** (`ImportDataFrame` streamed Arrow IPC, `DropWorkspaceEntry` + messages; nothing removed/renumbered) — repoint = adopt kantheon's proto. (ii) **SYSTEMIC: every spine proto (worker, kyklop, argos, theseus, proteus, ariadne, charon, kadmos) imports `org/tatrman/kantheon/*`** (chiefly `kantheon.common.v1.ResponseMessage`) — open wire contracts may not depend on the intelligence suite's namespace (RO-6 inverted at the wire); **N1/move gains a step: relocate shared message types → `org.tatrman.common.v1` in `tatrman-server`**, kantheon imports it. (iii) **plan.v1 drifted both ways: TableHint (ai-platform, per-scan NOLOCK-style hints, field 3) ADOPTED into `ttr-plan-proto` before the gate-2 freeze (Bora)** — un-reserve field 3, match ai-platform numbering so the repoint is a pure package swap; tatrman's `UnionNode`/`OverExpression`/`tenant_id` flow to ai-platform at repoint. (iv) parameters/transdsl/dfdsl identical modulo whitespace. (v) **llm.v1 convergence takes kantheon's superset** (`EmbedText`/embeddings) under a functional service name. · Rejected: dropping hints (silent behavior change for instance #0 before November); deferring TableHint to the RO-13 review (unfixable after gate 2 if forgotten).
- **2026-07-10 · [RO-22 — closes S3-T6] · `infra/health` + `infra/backstage` MOVE to `tatrman-server`** (Bora: "nothing to paywall there — and backstage is OSS anyway"). Names unchanged (already functional). Kantheon consumes them as deployed services and keeps registering its components in the same backstage instance. · Rejected: stay-kantheon + thin server copies (two copies of infra plumbing for a licensing distinction that doesn't exist).
- **2026-07-10 · [RO-23 — splits STRAT-1's satellite (b); tiers the PF frontends design] · The PF effort's two surfaces split across the tiers (Bora):** the **ANALYSIS plane** — semantic projection md→Cube/OSI + lossiness ledger · the R3 semantic layer service · the thin native viewer · the Superset reference pairing · the open kit packages (`@tatrman/door-client`, `md-client`, `ttrl`, `format`, `tokens`) — = **TATRMAN SERVER (open, Apache-2.0)**, sequenced as **named post-v1 Server arcs** (nothing enters the RO-3 bar; the first leg = the PBI/OSI projection, which is **already RO-3's stretch clause and ≡ STRAT-8's promoted arc — one arc**, PF `design.md` §4 is its design source). The **ENTRY plane** — `tatrman-entry`, `@tatrman/entry-client`, `@tatrman/grid-core`, entry journal / reservation registry / check-in / entry record / round contracts — = **TATRMAN PLATFORM (commercial)**, waking on satellite (b)'s trigger (first planning workload); structurally right, since entry check-in runs the `<cube>-entry-apply` program through the operate tier's program door (PF DQ-1/2). **Pinned consequences:** (i) semantic-layer policy source in the Server tier = the open validator policy store (RO-7 HOCON-in-git); PF BQ-2's "generated from Perun bundles" = the operate-tier leg — same generator, two sources; (ii) the B-author panel is open but rides satellite (a) (TTR-P family); (iii) the lineage-aware drill's legs light up as their tiers wake (static column lineage = satellite (a) manifests; runs = operate; entry commits = entry plane); (iv) PF corpus reads: "MIT" → Apache-2.0 (STRAT-3), `PL A-1` license line → STRAT-2, one-way arrow → RO-6. · Rejected: whole satellite stays parked (the analysis half is core-promise adjacent — "modeling pays twice"); entry-in-open (it is the monetization surface and rides paid operate machinery anyway).
- **2026-07-10 · [RO-24 — closes design-gaps item 5 (`next-steps-260710-design-gaps.md`)] · ARTIFACT VERSION SCHEME = α + γ's chart rule (Bora):** all publishables leave `0.0.1-LOCAL` for a **0.9.x line** (continuing above the already-public 0.8.4 of `ttr-{parser,writer,semantics}`) and jump to **1.0.0 together at the debut's acceptance run (SV-P6)**; after 1.0.0, artifacts version independently (plain semver on their own merits); **the product version is carried by the umbrella chart's `appVersion`** — artifacts ≠ product, permanently. Pre-debut churn stays cheap by construction (0.x semver permits breaking minors), and **version-before-publish joins rename-before-publish as an SV-P1 gate invariant.** · Rejected: β `1.0.0-RC.n` immediately (every pre-debut fix costs an RC re-release; RC qualifiers trip dependency tooling); γ pure independent semver from day one (loses the synchronized 1.0.0 debut moment the RO-3 story wants).
- **2026-07-10 · [RO-25 — closes design-gaps item 4; discharges RO-8's pin; closes NEW-2] · THE MCP SURFACE CONTRACT PINNED → `docs/ecosystem/server/design/mcp-surface.md` (Bora, four forks):** ① **capability ids go functional in the S4 sweep** (`theseus.query:v1`→`query.run:v1`, `ariadne.*`→`meta.*`, `echo.match`→`fuzzy.match`; `grounding.*` reserved for SV-P3) — the MCP surface is the most public wire of all, J-v2 applies in full; tool short names already functional, unchanged. ② **Identity = the OBO-only contract**: agents MUST forward the end user's bearer; fail-closed rejections (`missing_user_identity` · service-token · `identity_conflict`) are contract behavior; the door guarantees roles reach the validator unchanged + provenance back (`pipelineWarnings` always present); deployments MUST verify the token before pipeline entry (mechanism free); `X-User-Id`/`user_id` shortcuts = deployment-internal, outside the contract. ③ **Tool-schema compat = J-v2 verbatim**: `:v1` ids, additive-only within v1, breaking → `:v2` alongside. ④ **Conformance conversation suite = declarative YAML fixtures** (question → expected calls → governed result shape → provenance assertions; engine-agnostic; never asserts LLM wording), **tiered: hand-authored core (~25–40 fixtures incl. RLS/mask/identity-gate/truncation/refusal cases) passes 100% to claim conformance; DFP-derived synthetic extended tier (RO-19 ask ③) scored, non-gating** — the suite exists without DFP by construction; home = tatrman repo (RO-8); core authoring = SV-P4 with the reference Golem. · Rejected: ids-stay/defer-naming (persona strings in the permanent public contract); trusted-network mode in-contract (invites agents onto the weak path); pluggable identity SPI (unassertable by a suite; vague security story); server-level or per-tool-semver versioning (lockstep bumps / precision MCP discovery can't act on); golden transcripts (brittle) and per-framework test code (not engine-agnostic); flat-percentage or all-must-pass bars (a failed security fixture must not 'conform' / no home for a growing corpus).
- **2026-07-10 · [RO-26 — closes design-gaps item 1; the STRAT-8 arc's design] · `ttr import-schema` DESIGNED (Bora, fork-by-fork walk) → `docs/features/import-schema/design/` (compact control room + F1–F5 catalogue; feature-level decisions logged there):** relation derivation = **FK + deterministic name/type cascade + data-profiling probes, evidence-graded** (declared/verified/named-only/contradicted), LLM assist behind an optional flag gated by the probes · shaping = **junction collapse silent, header/detail folds proposed via checklist** · no-FK brownfield = **per-estate conventions file** (frozen PL contracts §12 skeleton; shipped profiles; deterministic: same DB + same conventions ⇒ same bytes) **+ probe verification** · output = **layered ownership**: db model machine-owned with PR-shaped drift proposals, er first cut = scaffold born once, human-owned, drift only flagged · interactivity = **CLI + review-checklist artifact** (judgment ledger, markdown + machine twin, IDE-rendered, rides the PR). Later arcs named: LLM assist, view-mining, teach-in, Designer import panel. Task-planning at SV-P4 start (probe determinism rule, conventions-file home, scale bounds land there). · Rejected (full whys in the feature log): FK-only/1:1-mirror chain (reproduces "modeling that doesn't pay" — fails the hero), silent folds, fixed built-in rulebook, one-shot-overwrite, wizard mode.
- **2026-07-10 · [RO-27 — closes design-gaps item 2; discharges RO-17's open generator pick] · PUBLIC DOCS/DX ARCHITECTURE PINNED → `docs/ecosystem/server/design/docs-dx.md` (Bora, five parts):** ① IA = **four goal-shaped tracks** (*Get running · Model · Connect · Operate*), genre discipline (tutorial/how-to/reference/explanation unmixed) applied within tracks as a writing rule; ② generator = **MkDocs Material + mike** (boring-and-proven fits the least-practiced, bar-weighted deliverable; Astro Starlight = the named revisit if a live `.ttrl` viewer in docs becomes a need); ③ site source lives **in the tatrman repo** (`docs-site/`), one site at tatrman.org, Server/Kantheon pages co-located by convention; ④ versioning = **latest-only until 1.0.0, then per-minor snapshots** labeled by the product version (RO-24's chart `appVersion`); ⑤ **Data Academy = separate future property** consuming/linking the Model track. Quickstart skeleton pinned = the RO-3 sentence as seven verified steps (install chart → `ttr import-schema` (RO-26) → commit model → serve via Veles → connect MCP client with OBO → governed answer **with visible provenance** → next tracks); SV-P4's docs deliverable now has shape (scaffold+quickstart first week). · Rejected: genre-first or product-first top nav (the stranger navigates by job, not by our taxonomy); separate site repo / CI aggregation (machinery before community); versioned-from-day-one (noise); Academy woven in (couples the bar to an unscoped effort).
- **2026-07-11 · [RO-28 — closes the SV-P0 phase review; disposes the sv-p0-review-input ⚑ set] · SV-P0 CLOSED + IMAGE PATH = FLAT `ghcr.io/collite/<name>` (Bora):** all six stages verified done (pilot PINNED at olymp `12796ac` + 17 digests; wire gates CLEAN both repos; Dockerfile blind spot fixed + gate enforcing 2026-07-11); phase-DONE 5/5. Moved-service images publish under **flat `ghcr.io/collite/<functional-name>`** at SV-P1 gate 3 — kantheon precedent, functional `ttr-*` names, no basename collisions post-move. Housekeeping: kantheon `reviews.md` kept; default-branch flip = Bora's checklist. SV-P1 (S0–S4) + SV-P2 (S1–S3) task lists generated the same day (`server/implementation/tasks/`), SV-P2 alongside by Bora's explicit call (pre-flights all resolved, execution-only). · Rejected: repo-scoped `ghcr.io/collite/tatrman-server/<name>` (diverges from kantheon precedent; buys nothing while names are functional).

---

## 8. Parking lot

| Item | Why parked | Revisit when |
|---|---|---|
| Monetization / commercial licensing model of the Platform | Product/business call, not design | Platform repo bootstrap (D-5 ②) forces a license file |
| Standalone persona: OSS-adopter-forever vs on-ramp-to-Platform | Product call; A-α rule holds either way | First external OSS adopter or GTM planning |
| TTR-B / NL surfaces on the Platform (agent authoring via Kantheon) | Depends on Kantheon-side agent design (C4-e seam) | D settles the split; Kantheon repo plans its half |
| Multi-tenant SaaS variant of the Platform | Single-org deployment is the design anchor first | C converges |
| Backfill semantics (schedule catch-up, windowed reruns) — FQ-1 | Out of v1 (Bora, 2026-07-08); interacts with params + external events | First real demand; likely with F-4-v-γ / E |
| Identity-priced quota tokens (F-5-δ) | Governance-flavored scheduling; needs H's policy machinery | H converges, or multi-tenant unparks |
| ~~Observability & alerting (island logs, run notifications, metrics/tracing) — review-260708 §3.8.7~~ **SHAPED 2026-07-09 (S7): rides the F-6-β event spine with audit (HQ-2)**; sizing/retention/notification channels = planning | Was: no workstream owned it | Planning |
| Charon transfers outside F-5 quota arbitration — review-260708 §3.8.1 | Known v1 hole; executor→Charon is direct, not Kyklop-dispatched | F-5 revisit / C-4-β condition |

---

## 9. Open questions (rolling)

- ~~**Q-1 (A/H):** Does *any* security concept exist in standalone mode (e.g., policy lint against an exported policy file), or is security 100% Platform? (LF-8)~~ **RESOLVED 2026-07-09 → H-8: integrity + optional advisory lint; enforcement = Platform.**
- ~~**Q-2 (C):** Is Argos actually the OPA carrier in kantheon?~~ **VERIFIED 2026-07-08 (kantheon repo): NO — "the OPA thing" is `infra/whois`,** the user/role directory + **OPA bundle server** (Keycloak/ERP sync → own Postgres; serves `UserRecord` lookups + OPA policy bundles `roles.tar.gz`). **Argos dropped OPA at the Stage 3.2 fold** — it is the PlanNode-level *validator* (RLS predicate injection, column DENY/MASK, TopN, strict coercion, admin bypass) with an **in-process HOCON policy store**, sitting Proteus→**Argos**→Kyklop; identity resolves at the theseus-mcp edge (Keycloak JWT → `PipelineContext.auth_roles`), whois = optional fail-closed role *enrichment*. Two split complications flagged for C/H: (1) **Argos carries an LLM Guard (DF-V04)** — an intelligence tendril inside the deterministic validator; (2) identity resolution lives at an *agent-side* edge (theseus-mcp). FI-4's "security server bound to metadata" therefore maps to a whois-descendant + the Argos policy engine + policy content bound to metadata objects — H's business.
- ~~**Q-3 (B/F):** The F-lite bundle records a semantic world fingerprint; in connected mode, is the *deployed* unit the same bundle, or a richer platform artifact?~~ **RESOLVED 2026-07-08 → the verbatim bundle wrapped in a platform deployment envelope (F-2 = β); deploy-as-pointer recorded as a later envelope variant.**
- **FQ-5 (F→H):** a scheduled run executes as *whom*? (Deploying user / envelope-named service principal / …) The envelope reserves the field; H answers.
- ~~**Q-5 (→ workstream K, opened 2026-07-09):** **World composition** — where does the *platform-managed world* live, and how does a project world compose with it? Multi-project instance truth must be one truth, by decision not inference.~~ **RESOLVED 2026-07-09 → K = β explicit reference + α-as-mechanics** (map §K resolution; decision log).
- ~~**Q-6 (A/planning · from review-260708 §3.5):** the platform-v1 **acceptance statement** — which hero lives, at what maturity, define "done"?~~ **RESOLVED 2026-07-09 (Bora ratified); 2026-07-10: SUPERSEDED as the near-term bar by STRAT-5/RO-3 — remains the operate-tier (Tatrman Platform) bar:** *"Platform v1 is done when, on a fresh `tatry`-deployed instance: (life 2) the hero program deploys via envelope, runs nightly under a service principal on Arges+Steropes with Charon transfers, RLS enforced, runs and column lineage visible in the Designer; (life 3) the same program delegates through Airflow 3 with lineage harvested back and exported to OpenMetadata, and the Kestra emit plugin passes conformance; (life 1) unchanged and green throughout — the MIT toolchain never regresses."*
- ~~**Q-7 (F/B · from review-260708 §3.2):** ratify the F-2-δ correction — the pointer-deploy envelope variant must cite `{lock hash + compile record}` (recompile-from-recorded-stats), not the lock alone, or B-2-δ's reviewability failure returns.~~ **RESOLVED 2026-07-09 → ratified; F-2 amended in `05-scheduler-options.md`.**
- ~~**Q-4 (D):** does the Designer frontend cross the license boundary?~~ **RESOLVED 2026-07-08 → no: frontend stays MIT in tatrman, backend-selectable; platform-only panels ship as platform-side extensions on an MIT-defined extension surface (D-2/Q-4-a).**

---

## 10. Session index

| Date | Gear | What happened | Artifacts |
|---|---|---|---|
| 2026-07-08 | Framing + Divergence (B) | Effort opened; FI-1..4 fixed, GI-1..4 recorded; workstreams A–J; LF-1..8; P1–P3 proposed; hero "one program, three lives"; B divergence → mode-seam options | this doc, `01-design-space-map.md`, `02-mode-seam-options.md` |
| 2026-07-08 | Convergence (B core) + dives | **B-1 β · B-2 β · B-3 α · B-5 δ · B-6 γ · BQ-1 archive decided** (LF-1, LF-5 resolved); BQ-2 + BQ-3 dives written (leans: per-object stats keying + inline recording; lock-for-canon / max-age-for-stats); BQ-4 folded into lock; BQ-5 opened | `02-mode-seam-options.md` (resolutions + dives), decision log |
| 2026-07-08 | Convergence (B closed) | **BQ-2 = γ(+δ) and BQ-3 layered (+BQ-4) ratified → 🟢 B CONVERGED.** Seam-legality rule standing (data + diagnostics; never identity/side effects); confirmations owed by C (server contract) and H (advisory policy); BQ-5 → F/D | decision log, dashboard |
| 2026-07-08 | Verification + Divergence (C) | **Q-2 verified in kantheon repo** (whois = OPA bundle server; Argos = validator w/ in-process HOCON policies + LLM Guard tendril; identity at theseus-mcp edge); full service inventory with placement verdicts; C forks C-1..C-6 catalogued (roster philosophy, metadata server, execution spine, Charon, security placement); **C-6 discharges B's IOU (server contract confirmed cheap)**; CQ-1..CQ-4 opened (CQ-3 = P2 runtime-plugin ratification) | `03-service-architecture-options.md`, Q-2 in §9 |
| 2026-07-08 | Convergence (C closed) | **C-1 β-spine+α-leaves · C-2 γ new metadata server (+lineage, +exports) · C-3 two doors/one hall · C-4 Charon home · C-5-i validator SPI ratified → 🟢 C CONVERGED** (LF-2 resolved; P2 clarified to build-time; riders distributed to H/D/F) | decision log, dashboard |
| 2026-07-08 | Divergence (D) | D-1..D-6 catalogued (new-repo vs monorepo vs kantheon-rename; license=repo boundary + Q-4 Designer placement; "toolchain-touched ⇒ tatrman-owned" contract rule; olymp-twin infra with ship-as-chart direction; strangler sequencing answering CQ-1; grey-zone sweep table — metis flagged "worker in disguise", pinakes unverified); DQ-1..3 opened | `04-repo-topology-options.md` |
| 2026-07-08 | Convergence (D closed) | **All six D leans + DQ-1..3 ratified → 🟢 D CONVERGED** (LF-3, Q-4, CQ-1 resolved; Gradle-only; sweep-on-arrival; tatrman-semantics obsolete; J naming demands queued) | decision log, dashboard |
| 2026-07-08 | Convergence (A) + handoff | **A-α "compile vs operate" rule confirmed → 🟢 A CONVERGED** (grey zones placed: optimizer/designer-server/conform/frontend all MIT; plugin mechanism MIT); **P1–P3 ratified**; persona + monetization parked; session handoff written | decision log, `next-steps-260708.md` |
| 2026-07-08 | Divergence (F) | F forks F-1..F-8 catalogued (scheduler shape incl. map's F-α..δ; deployed unit → **envelope-wrapping-bundle lean** answering Q-3; execution model incl. C-3-δ reconciler; F-proper debt repaid item-by-item — params/retries/resume/on-failure/events in the lean package, **FF stays reserved**; two-door quotas → priority-at-dispatch lean; CQ-2 → executor-owns-writes/metadata-serves-reads lean; **BQ-5 → dissolves into lock+compile-record (F-7-γ)**; C-4-β rider made conditional on F-3-γ); FQ-1..FQ-5 opened (FQ-5 scheduled-run principal → H) | `05-scheduler-options.md` |
| 2026-07-08 | Convergence (F closed) | **All eight F leans ratified (Bora) → 🟢 F CONVERGED** (LF-6's F half, Q-3, CQ-2, BQ-5 resolved; C-4-β closed without decision); FQ-1 backfill out of v1 → parking lot; FQ-2/FQ-4 → planning work items; FQ-3 declared a non-decision; FQ-5 → H; F-5-δ parked | decision log, `05-scheduler-options.md` (resolutions), dashboard |
| 2026-07-08 | Divergence (E) | E forks E-1..E-5 catalogued (emit mechanism — map's E-α..δ, **lean β JVM SPI proven by extracting the bash emitter, with δ as the platform's deploy-time adapter half**; registry hunch **confirmed via E-2-γ: registration = world content, B-T6's instance layer, metadata server = the registry**; delegation shape pressure-tests F-1-γ — **verdict: "frontend" holds for door-calling shapes; native-DAG emit is a compile-side product, not a frontend**; harvest → run-results-only lean, connector SPI shared with I; manifest-graph contract → documented-stable MIT lean); support-package synthesis answers LF-4 in shape; EQ-1..EQ-4 opened (EQ-3 anchor = Bora's call) | `06-orchestration-options.md` |
| 2026-07-08 | Convergence (E core) + EQ-3 dive | **E-1..E-5 ratified (Bora) → LF-4 RESOLVED; E 🟡** (E-3-γ clarified: in force day one — α-1/β are γ's contents, not its predecessors); **EQ-3 anchor field researched** (web, July 2026): five tiers, ~27 contenders (moderns · infra engines · cloud-managed · enterprise WLA · weird); findings: data-defined vs code-defined emit split; WLA tier = the commercial-plugin market; **lean Airflow 3 anchor · Kestra fast-follow · Dagster third**; EQ-1/EQ-2 → planning; EQ-4 → I | decision log, `06-orchestration-options.md` (resolutions + EQ-3 dive) |
| 2026-07-08 | Convergence (E closed) | **EQ-3 = both (Bora): v1 executor targets = {bash, Airflow 3, Kestra}; Dagster first post-v1 → 🟢 E CONVERGED** (pick-one-anchor consciously widened — the SPI-proving second target enters v1); riders distributed (I: connector frame + EQ-4; H: secrets/trust; G: registration UX). **E+F 🟢 → independent review session due next** (per process note) | decision log, dashboard |
| 2026-07-08 | **Independent review** | Fresh re-read of the whole corpus + ground-truth spot-checks (ttr-metadata `Source.kt`, TTR-P F-lite/B-T6 — citations verified). **All examined decisions confirmed.** Three concentrated exposures: **world composition** (platform-managed world × project worlds — assumed everywhere, designed nowhere → Q-5); **H rider pile-up + secrets unshaped** (recommend H next); **v1 scope accreted without an acceptance bar** (→ Q-6). Corrections/gaps: F-2-δ provenance precision (→ Q-7), plugin-determinism verification (→ EQ-2), resume-vs-staging lifecycle (→ F work items), TTR-P amendment mini-sweep due (incl. the silent T6 type-manifest-ownership amendment), observability parked-named | `review-260708.md`, §8/§9 updates |
| 2026-07-09 | Review incorporation + Divergence (H) | **review-260708 ratified (Bora); all findings incorporated**: F-2-δ amended (Q-7 ✓) · **column-level lineage = v1** (C-2 amended; CQ-5/CQ-6 opened) · EQ-2 widened (determinism kit) · FQ-6/FQ-7 opened · **C/E/F → 🟠 dirty** · **K opened** (world composition, Q-5) · TTR-P amendment sweep queued · G inputs recorded in map. Then **H divergence opened** → `07-security-options.md` (forks H-1..H-8, the full rider inbox) | this doc, `review-260708.md` (annotated), `01`/`03`/`05`/`06` amendments, `07-security-options.md`, `next-steps-260709.md` |
| 2026-07-09 | Convergence (H closed) | **All eight H forks ratified (Bora) → 🟢 H CONVERGED**: H-1 γ + **the TTR `security`-block sugar (Bora's addition — sugar generates policies; owner-vs-org line; deny-overrides; fingerprint-neutral)** · H-2 α ingress + **explicit envelope service principal (FQ-5 ✓)**, HQ-1 classifications-native · H-3 α minimal-spine, **compile advisory-only (B's ledger clear)** · H-4 γ PDP, PEPs evaluate locally, run records cite bundle hash · H-5 β+γ secrets (full dive written), never-at-rest + no-secret-API · H-6 β + determinism kit (**EQ-2 widening ✓**) · H-7 α-then-β, **RLS = v1** · H-8 γ+β (**Q-1/LF-8 ✓**, E-3-β bypass line ratified). Riders: HQ-2→observability, HQ-3→G, HQ-4→⑥, HQ-5→β-step, security-block grammar→amendment sweep | decision log, `07-security-options.md`, dashboard |
| 2026-07-09 | Convergence (K) | **K converged same-day (Bora): β explicit reference + α-as-mechanics** — project worlds declare the platform world; resolver composes; lock pins by hash; contradiction = compile error; platform world = own git repo; roster = server config. **Q-5 resolved**; G registration UX + C-2 multi-repo serving unblocked; T4-adjacent grammar → amendment sweep | map §K resolution, decision log, dashboard |
| 2026-07-09 | Divergence (G) | **G divergence catalogue written** → `08-designer-options.md`: G-1 write model (α CRDT · β locking · **γ writes-through-git lean** · δ never-writes floor; γ-i publish = repo-policy-decides) · G-2 edit idioms (γ graph-ops + embedded-text split) · G-3 sessions (γ stateless-reads/session-ful-edits) · G-4 v1 panels (α reader-first + flagged edit mode) · G-5 standalone statements (IDE viewer permanent; preview asymmetry as decision; `.ttrl` shared schema). GQ-1..4 opened | `08-designer-options.md` |
| 2026-07-09 | Convergence (G closed) | **All G leans ratified (Bora) → 🟢 G CONVERGED**, with timing pinned (**read-only first, edits second — designed now, built second**) and **GQ-2 decided in: read-only TTR-P program graphs ship in the v1 reader set**. G-1 γ writes-through-git · G-2 γ split idioms · G-3 γ hybrid sessions · G-4 α reader-first · G-5 ratified. **HQ-3 + both review inputs discharged** | decision log, `08-designer-options.md`, dashboard |
| 2026-07-09 | Divergence (I) | **I divergence catalogue written** → `09-external-metadata-options.md`: I-1 truth direction (LF-7 — **lean: α proposals-in/projections-out = two one-way flows, never sync/federate**; federation banned from compile by B-5-δ) · I-2 harvest (EQ-4 — lean: one connector frame; **temperament split: schemas→reviewed proposals, stats→BQ-2 store direct; one-shot introspection CLI = MIT, continuous harvest = platform**) · I-3 proposal mechanics (lean: PR-shaped — robots write through git too; deterministic generation) · I-4 megaproviders-as-connectors (**IQ-1 PowerBI `md` mapping carved out to TTR-M**) · I-5 anchor (lean: OpenMetadata, DataHub second; IQ-3 research dive on offer). IQ-1..4 opened | `09-external-metadata-options.md` |
| 2026-07-09 | Convergence (I closed) | **All I leans ratified (Bora) → 🟢 I CONVERGED; LF-7 resolved — ALL EIGHT LOAD-BEARING FORKS NOW CLOSED.** I-1 α proposals-in/projections-out · I-2/I-3/I-4 α (EQ-4 ✓; one-shot-CLI MIT line; PR proposals; IQ-1 → TTR-M) · **I-5 anchor = OpenMetadata, DataHub follow-on (widening consciously declined)**. Remaining: J + de-dirty C/E/F + consolidation sweep + Q-6 acceptance statement → `design.md`/`detailed-design.md` → `/planning` | decision log, `09-external-metadata-options.md`, dashboard |
| 2026-07-09 | De-dirty pass | **CQ-5 (compiler-side static column lineage) · CQ-6 (pinned) · FQ-6 (two staging scopes, retention, degrade-to-restart) · FQ-7 (dissolved — manifest-schema-version compatibility) all ratified (Bora) → C, E, F RE-CONVERGED 🟢.** Board: **A–K all 🟢 except J.** Remaining: consolidation sweep · J · Q-6 → `design.md` + `detailed-design.md` → `/planning` | decision log, `03`/`05`/`06` status sections, dashboard |
| 2026-07-09 | Consolidation sweep | **S1–S8 batch-ratified (Bora):** amendment sweep queued as one batch · S2–S5 confirmed · S6 CLAUDE.md line · **S7 audit+observability on the F-6-β event spine (HQ-2 closed, parking-lot item shaped)** · S8 Q-6 stays open (Bora). **Remaining: J · Q-6 · `design.md` + `detailed-design.md` · `/planning`** | decision log §sweep, parking lot |
| 2026-07-09 | Convergence (J closed) | **J ratified (Bora, with two amendments: RADEGAST for the executor; `cz.tatrman:*` platform coordinates) → 🟢 J CONVERGED — THE EFFORT IS FULLY CONVERGED (A–K).** Register rule β (new=Slavic, transplants=Greek); Veles·Perun·Radegast·Zorya; `tatry`; editions without "community"; the group id makes the license boundary physical. **Remaining: Q-6 (Bora) · final deliverables · `/planning`** | map §J, decision log, dashboard |
| 2026-07-09 | Wrap-up (Q-6 + handoff) | **Q-6 RATIFIED (Bora) — the v1 acceptance statement is on the record (§9).** Handoff written for the final-deliverables session (`design.md` + `detailed-design.md`, then `/planning`) | §9, `next-steps-260709.md` (final handoff) |
| 2026-07-09 | **Final deliverables** | **`design.md` + `detailed-design.md` written per the handoff checklist** (Q-6 verbatim · J-named service map w/ one-line contracts · D-3 contract inventory w/ owners · strangler ①–⑦ w/ H/G/I placements · standing rules · planning work items · parked/S1/kantheon-arc ledger); consistency-reviewed against the decision log. **THE DESIGN EFFORT IS CLOSED — next step: `/planning` on `design.md`** | `./design.md`, `./detailed-design.md`, status header |
| 2026-07-10 | **Re-open: ecosystem redraw** | **STRAT-1..9 formalized + RO-1..14 ratified (Bora)** — core/satellite split absorbed: Tatrman Server = the near-term build (repo = `tatrman-server`, RO-1; bar = RO-3 trimmed statement; H/G/E-2 tier splits RO-7/9/10; Veles provenance RO-5; P2 chain RO-6; MCP contract RO-8; S1/⚑/B-contract dispositions RO-11/13/14). PL plan set frozen as operate reference (RO-2); superseded-markers passed over `design.md`/`detailed-design.md`; **core docs written: `docs/ecosystem/server/design/architecture.md` + `docs/ecosystem/server/implementation/plan.md`** | this doc §7 batch, `./design.md`, `../plan/*` banners, `docs/ecosystem/server/*` |
| 2026-07-10 | **OQ walk (ecosystem register)** | **RO-15..20 ratified (Bora):** OQ-5 → Collite steward · OQ-6 shaped (word mark, EUTM, 9+42, 41 open) · OQ-9 → hosting slate (org `tatrman` via account recovery; Central public / GH Packages staging; GHCR; docs-as-code) · OQ-2/OQ-12 → SPDX + **DCO** · OQ-3/OQ-4 → four-ask DFP approach, textbook Python Golem · **OQ-11 proto half closed by direct repo check — `proteus.v1` persona-on-wire finding, rename → `translate.v1` before gate 2**; **worker.proto + full family diffed same day after ai-platform connected → RO-21** (kantheon superset; kantheon-common import finding → `org.tatrman.common.v1` relocation; TableHint adopted into plan.v1; llm.v1 = kantheon superset). SV-P0 `/planning` opened | §7 RO-15..21, `docs/ecosystem/server/implementation/plan.md` register, `../../stewardship-checklist-260710.md`, naming ledger §4 N2 |
| 2026-07-10 | **SV-P0 planning + PF tier split** | **SV-P0 `/planning` pass complete** → `docs/ecosystem/server/design/contracts.md` + `docs/ecosystem/server/implementation/tasks/` (S1–S6, TDD-ordered, verify blocks). **RO-22** health/backstage → server. **RO-23** PF frontends tiered: analysis plane → Server (post-v1 arcs; projection ≡ STRAT-8 arc; policy source = RO-7 open store), entry plane (`tatrman-entry`) → Platform (satellite (b) = entry only now). PF corpus banner + PF control-room amendment written | `docs/ecosystem/server/{contracts.md,tasks/*}`, §7 RO-22/23, `docs/ecosystem/platform/design/frontends/design.md` banner, PF control room §7 |
| 2026-07-10 | **Docs restructure** | All ecosystem-level docs consolidated under `docs/ecosystem/` (Bora): general docs at the root; product subfolders `tatrman/` · `server/` · `platform/`, each with `design/` + `implementation/{plan.md, tasks/}`; the PF corpus → `platform/design/frontends/`; toolchain design history stays in `docs/features/`. All links refreshed (incl. two pre-existing broken `ttr-p` references, fixed to `features/ttr-p/`). **Target versions labeled: Tatrman / Server / Platform = 1.0.0; the frontends surfaces = 1.1.0.** Decision-log wording untouched — RO-23's "post-v1" reads "1.1.0" | `docs/ecosystem/` tree, `README.md` map |
| 2026-07-10 | **Design-gaps session (RO-24..27)** | All five handover items closed. **RO-24** version scheme (0.9.x → 1.0.0 at debut; product = chart `appVersion`) · **RO-25** MCP surface contract pinned + conformance suite shaped (NEW-2 ✓) → `server/design/mcp-surface.md` · **RO-26** `ttr import-schema` designed — full diverge + fork-by-fork converge → `docs/features/import-schema/design/` (own compact control room + F1–F5 log) · **RO-27** docs/DX architecture (four tracks, MkDocs Material, tatrman-repo site, quickstart skeleton) → `server/design/docs-dx.md` · resolver rewrite **options catalogued** (NEW-1 still open, converges at SV-P3 planning) → `server/design/resolver-rewrite.md`. S4 task list gained T5b (capability-manifest renames); both plans' registers updated | §7 RO-24..27, `server/design/{mcp-surface,docs-dx,resolver-rewrite}.md`, `features/import-schema/design/*`, plans/registers, `../../next-steps-260710-execution.md` |
| 2026-07-11 | **SV-P0 phase review + SV-P1/P2 planning (RO-28)** | SV-P0 executed & CLOSED (6/6 stages; pilot pinned; review-input ⚑ dispositioned — flat GHCR path, Dockerfiles pre-fixed, reviews.md kept). **SV-P1 lists S0–S4** (RO-13 review + Central namespace pre-flight · tatrman 0.9.x/translate.v1 · server libs · images + olymp repoint · Maven Central) + **SV-P2 lists S1–S3** (SPDX/NOTICE/MIT sweep · governance file set · external-reader pass) generated; calendar proposal in the task-management doc awaits ratification (S0·T4) | §7 RO-28, `server/implementation/tasks/*`, `sv-p0-review-input.md` §Dispositions |
