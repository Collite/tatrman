# 3.3 — `modeler.toml` SQL config + loader

**Repo:** modeler. **Effort:** ~0.5 day. **Package:** `@modeler/semantics`.
**Pre-flight:** Read contracts §5 + §5.1. This config closes the multi-part name
mapping (DESIGN §12.8) and supplies per-dialect defaults; the resolver (3.4)
depends on it.

---

- [ ] **3.3.1 — Spec first (red).** `src/__tests__/sql-config.test.ts`: a sample
      `modeler.toml` `[sql]` block (contracts §5) loads into `SqlConfig`;
      `default-dialect`, the `namespace-map` array, and `defaults.<dialect>` parse
      correctly. Red — loader absent.

- [ ] **3.3.2 — Extend the manifest loader.** Find the existing `modeler.toml`
      reader (search `modeler.toml` / manifest in `@modeler/semantics`). Add the
      `[sql]` section parse into the `SqlConfig` DTO (contracts §5). Reuse the
      existing TOML dependency; do not add a new one.

- [ ] **3.3.3 — Validate bijectivity.** The `namespace-map` must be **bijective**
      (contracts §5 / E7): no two rows share a `namespace`, and no two share a
      `(database, schema)` pair. On violation emit a project-level diagnostic
      (point at `modeler.toml`). Unit-test both collision directions.

- [ ] **3.3.4 — Build the two lookup directions.** `SqlConfig` helpers:
      `namespaceFor(database, schema): string | undefined` (SQL→TTR, for
      resolution) and `sqlNameFor(namespace): { database, schema } | undefined`
      (TTR→SQL, for Phase 4 completion/qualified-insert). Unit-test both.

- [ ] **3.3.5 — Defaults + missing-config behaviour.** `defaultsFor(dialect)`
      returns the `(database, schema)` for under-qualified names. If the `[sql]`
      section is absent: default dialect `tsql`, empty namespace map (every
      multi-part name unresolved → a *soft* diagnostic, not a crash). Test the
      absent-config path explicitly.

- [ ] **3.3.6 — Green; lint; commit.**
      ```bash
      pnpm --filter @modeler/semantics test && pnpm --filter @modeler/semantics lint
      ```
      Commit: `Section embedded-sql: modeler.toml [sql] config + loader`.
