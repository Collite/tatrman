# S0.3 — Bundle-size + host-split evaluation

**Repo:** modeler (scratch `packages/sql-spike`). **Effort:** ~0.5 day.
**Gate:** the generated **lexer** bundle fits the browser-Worker budget (so the
Designer can highlight); **parser** bundle sizes are measured so the
lexer-only-in-browser split (E6 / DESIGN §12.5) is confirmed or revised with real
numbers.

**Pre-flight:** S0.1 PASS (generated artifacts available).

---

- [ ] **S0.3.1 — Agree the Worker budget.** Find the current `server-browser.ts`
      bundle size (`pnpm --filter @modeler/lsp build` → inspect the esbuild output
      for the browser entry). Record today's size and set a budget headroom with
      the owner (e.g. "+X KB gz acceptable for SQL highlighting").

- [ ] **S0.3.2 — Measure lexer-only bundles.** With esbuild, bundle a tiny entry
      that imports only `TSqlLexer` + `PostgreSQLLexer` (+ `antlr4ng`). Record
      minified and gzipped sizes, tsql and postgres separately and combined.

- [ ] **S0.3.3 — Measure parser bundles.** Same, importing the full
      `TSqlParser` / `PostgreSQLParser`. Record sizes. The expectation (to confirm)
      is parsers are several× larger than lexers — the rationale for keeping them
      out of the Worker.

- [ ] **S0.3.4 — Shared-runtime check.** `antlr4ng` is already in the browser
      bundle (TTR parser runs there). Confirm the SQL lexers reuse that runtime and
      don't duplicate it (measure marginal cost = lexer classes only, not runtime).

- [ ] **S0.3.5 — Decide & record.** Append `spike-report.md` § "S0.3": today's
      Worker size, lexer marginal KB (gz), parser marginal KB (gz), and the
      verdict — **lexer-only-in-browser confirmed**, or (if lexers alone blow the
      budget) the revised plan (e.g. lazy-load lexers, or no SQL highlighting in
      the Designer). This is the evidence behind architecture §5.

- [ ] **S0.3.6 — Phase 0 close-out.** With S0.1–S0.3 all recorded, write the
      `spike-report.md` top summary (three gate verdicts + the go/no-go for the
      ANTLR approach). Then **delete `packages/sql-spike`** except `spike-report.md`
      and `vendor/PINNED.md`, which move to
      `docs/grammar-master/embedded-sql/spike-report.md` for the record. Tick the
      Phase 0 gate row in [`INDEX.md`](INDEX.md). If go: author the Phase 2 task
      lists. If no-go: open the E8 (`node-sql-parser`) re-plan.
