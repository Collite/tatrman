# 3.1 — `SqlRefModel` contract + adapter tests (red)

**Repo:** modeler. **Effort:** ~0.5 day. **Package:** `@modeler/sql`. **TDD —
this stage writes the failing extraction tests Phase 3.2 makes green.**
**Pre-flight:** Phase 2 merged. Read contracts §4 (`SqlRefModel`) and DESIGN
§12.3/§12.4.

---

- [ ] **3.1.1 — Land the types.** Add the contracts §4 interfaces (`Span`,
      `SqlTableRef`, `SqlColumnRef`, `SqlCte`, `SqlParamRef`, `SqlScope`,
      `SqlRefModel`) to `src/refmodel.ts`. Declare the adapter signature
      `extract(tree, dialect): SqlRefModel` (impl in 3.2). Types only — no logic.

- [ ] **3.1.2 — Fixtures: the common constructs (DESIGN §12.3 "80%").** Build a
      fixture table of small queries with hand-written expected `SqlRefModel`s:
      simple `FROM`, `JOIN` + alias, multi-part name (`dbo.Orders`), CTE, derived
      table (subquery alias), qualified vs bare column. **tsql and postgres
      variants** of each (the adapters differ).

- [ ] **3.1.3 — Extraction spec (red).** `src/__tests__/extract-tsql.test.ts` and
      `extract-postgres.test.ts`: parse each fixture, call `extract`, assert the
      `tables` (with `name` parts + `alias` + `origin`), `columns` (with
      `qualifier`), `ctes`, and `rootScope.tables`. Red — `extract` is unimplemented.

- [ ] **3.1.4 — Error-tolerance spec (red).** A fixture with a partly-broken query
      (valid `SELECT … FROM users`, then garbage) must still return the `users`
      table and record the garbage in `parseErrors` — **never throw** (DESIGN
      §12.3). This pins the "best-effort" contract.

- [ ] **3.1.5 — Scope/alias spec (red).** A `JOIN` of two tables + a bare column
      present in both → the resolver later flags ambiguity, but at extraction the
      `rootScope` must list **both** tables/aliases so the resolver can decide.
      Assert the scope tree (CTE/derived scopes nested under `rootScope`).

- [ ] **3.1.6 — Confirm red; commit.** `pnpm --filter @modeler/sql test` fails on
      the missing `extract` impl (not on spec typos). Commit:
      `Section embedded-sql: SqlRefModel types + extraction specs (red)`.
