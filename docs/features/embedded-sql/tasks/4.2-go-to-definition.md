# 4.2 — Go-to-definition (SQL ref → TTR `db` def)

**Repo:** modeler. **Effort:** ~0.5 day. **Package:** `@modeler/lsp`. **Desktop
only.** **Pre-flight:** 4.1 done (shared hit-test + cached `SqlRefModel`).

---

- [x] **4.2.1 — Spec (red).** Integration test: "go to definition" on `users` (and
      on `u.email`) inside a SQL block jumps to the TTR `db.<ns>.table.users`
      definition (and the column def) — assert the returned `Location` range
      matches the symbol's `SourceLocation`.

- [x] **4.2.2 — Reuse hit-test + resolver.** From the request position, find the
      covering `SqlTableRef`/`SqlColumnRef` (4.1.2), resolve to the TTR symbol
      (3.4), and return its definition `Location`. Column → the column def; table →
      the table def. CTE/derived refs → the in-SQL defining occurrence (the CTE
      name span), not the symbol table.

- [x] **4.2.3 — Multi-result + unresolved.** Ambiguous bare column → return all
      candidate column defs (LSP allows `Location[]`). Unresolved ref → no result
      (no error). Test both.

- [x] **4.2.4 — Wire the provider; desktop guard.** Register the
      definition handler for positions inside SQL blocks; guard so the browser
      worker (no parser) never runs it.

- [x] **4.2.5 — Green; lint; commit.**
      ```bash
      pnpm --filter @modeler/lsp --filter @modeler/integration-tests test
      pnpm --filter @modeler/lsp lint
      ```
      Commit: `Section embedded-sql: SQL go-to-definition`.
