# 3.5 — `parameters` cross-check

**Repo:** modeler. **Effort:** ~0.5 day. **Package:** `@modeler/semantics`.
**Pre-flight:** 3.4 done. The `query` def declares typed `parameters`; this stage
checks the SQL's actual placeholders/bind-params against them. Inputs: the masked
`{param}` spans (Phase 2) + native bind params from `SqlRefModel.params`.

---

- [ ] **3.5.1 — Diagnostic codes + spec (red).** `sql-undeclared-param` (used in
      SQL, not in `parameters`) and `sql-unused-param` (declared, never used —
      warning). Spec against fixtures: `{nazev}` used but not declared → error on
      the placeholder span; declared `@since` never used → warning on the param def.

- [ ] **3.5.2 — Collect SQL param usages.** Union the masked placeholder names
      (`maskPlaceholders` result, threaded through the lexer pass) with
      `SqlRefModel.params` (`@p`/`$1`/`:name`/`?`). Normalise per dialect
      (contracts §4 comment + DESIGN §12.8): T-SQL `@name`, Postgres `$n`/`:name`,
      DuckDB `$name`/`?`. Positional `?`/`$1` map by index to declared order.

- [ ] **3.5.3 — Compare to declared `parameters`.** Read the query's `parameters`
      from the TTR AST. Emit `sql-undeclared-param` for SQL params with no
      declaration; `sql-unused-param` for declarations never referenced. Fold names
      per dialect rules where relevant (§6.2).

- [ ] **3.5.4 — Wire + integration test.** Run during the semantics pass; map
      spans to file positions; desktop-only guard. Integration test: a `.ttr`
      query whose SQL uses an undeclared `{param}` reports the diagnostic at the
      placeholder.

- [ ] **3.5.5 — Corpus sanity; lint.** Run over the S0.2 corpus; confirm no
      spurious undeclared/unused params on real queries (or record exceptions in
      `LAZY-PATCH.md`).
      ```bash
      pnpm --filter @modeler/semantics --filter @modeler/integration-tests test
      pnpm --filter @modeler/semantics lint
      ```

- [ ] **3.5.6 — Phase 3 DoD; commit.** Confirm the Phase 3 DoD
      ([`../plan.md`](../plan.md)): diagnostics fire correctly on the corpus, FP
      rate acceptable. Tick Phase 3 in [`INDEX.md`](INDEX.md). Commit:
      `Section embedded-sql: parameters cross-check (Phase 3 done)`.
