# 1.5 — `ttr-writer` round-trip + publish

**Repo:** modeler. **Effort:** ~0.5 day. **Modules:**
`packages/kotlin/ttr-writer`, release tooling.

**Pre-flight:**
- 1.4 done (Kotlin parser produces `TaggedBlockValue`).
- Read contracts §8 (versioning), DESIGN §10, and the root `PUBLISHING.md`
  semver rules (§7).

---

- [ ] **1.5.1 — Writer round-trip spec (red first).** Add a Kotest case: parse a
      `query { sourceText: """sql␊SELECT 1␊""" }`, render via `TtrRenderer`, re-parse,
      assert structural equality of the `TaggedBlockValue` (modulo `SourceLocation`).
      Red until 1.5.2.

- [ ] **1.5.2 — Render `TaggedBlockValue`.** Implement rendering: emit
      `"""<tag>␊<value>␊"""` with the project's standard block indentation
      (formatter convention DESIGN §7 — opener on its own line, uniform indent).
      Ensure the rendered form re-parses to the same `tag`/`value` (the round-trip
      spec). Untagged triple-strings render unchanged.

- [ ] **1.5.3 — Exhaustive-`when` sweep.** Build `ttr-writer`; fix every
      `when (value)` the compiler flags for the new variant. Confirm no `else ->`
      masks a missing branch.
      ```bash
      ./gradlew :packages:kotlin:ttr-writer:test :packages:kotlin:ttr-writer:ktlintCheck
      ```

- [ ] **1.5.4 — Full build + conformance.** Green across the board:
      ```bash
      pnpm -r build && pnpm -r test
      ./gradlew build
      pnpm --filter @modeler/conformance diff
      ```

- [ ] **1.5.5 — CHANGELOG + version.** Add a CHANGELOG entry: "adds
      `PropertyValue.TaggedBlockValue` (embedded-language blocks); breaking for
      exhaustive `when` consumers; `language:` on `query` soft-deprecated." Choose
      the version per PUBLISHING.md §7 (additive node + sealed variant → minor while
      < 1.0.0, but flag the source-break in the notes). Smoke-publish to Maven Local:
      ```bash
      ./gradlew -Pversion=<v> :packages:kotlin:ttr-parser:publishToMavenLocal \
                              :packages:kotlin:ttr-writer:publishToMavenLocal
      ```

- [ ] **1.5.6 — Publish + record ai-platform follow-up.** Tag-publish per
      PUBLISHING.md (`kotlin/v<x.y.z>`). Open a tracking note (NOT in this repo's
      scope to implement) for the ai-platform side: bump `tatrman-modeler`, add the
      `TaggedBlockValue` branch where `sourceText` is consumed, read
      `language`/`dialect` from the node (DESIGN §10). Tick Phase 1 in
      [`INDEX.md`](INDEX.md). Commit: `Section embedded-sql: writer + publish <v>`.
