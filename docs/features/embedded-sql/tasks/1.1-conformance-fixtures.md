# 1.1 — Conformance fixtures first (red)

**Repo:** modeler. **Effort:** ~0.5 day. **TDD — this stage writes the failing
tests that the rest of Phase 1 makes green.**

**Pre-flight:**
- Read DESIGN §9 (golden cases C1–C11) and §4 (value extraction).
- Read contracts §2.3 and §6.1.
- Locate the conformance harness: `@modeler/conformance` (TS dump vs Kotlin dump;
  see how grammar-master fixtures are wired — search `conformance` package).

---

- [ ] **1.1.1 — Add the fixture `.ttr` files.** For each of C1–C11, add a minimal
      `.ttr` fixture exercising a `query { sourceText: <block> }`. Use the exact
      source forms in DESIGN §9 (mind the `␊`/`·` legend → real newlines/spaces).
      Place alongside the existing conformance fixtures so both TS and Kotlin dumps
      pick them up.

- [ ] **1.1.2 — Encode expected values.** For each fixture, record the expected
      `tag`, `language`, `dialect`, and `value` (DESIGN §9 table) in the
      conformance expectation format. Pay special attention to:
      C5 (untagged keeps trailing `␊`), C7 (`"""sql"""` one-line = plain string),
      C11 (internal blank line kept).

- [ ] **1.1.3 — Add a TS unit spec (red).** In `packages/parser/src/__tests__/`,
      add `tagged-block.test.ts` asserting `walkEmbeddedBlock` produces the
      `TaggedBlockValue` of §2 for C1, C3, C4, C7, C9, C11. It must fail to
      **compile** (no `TaggedBlockValue` type / no `walkEmbeddedBlock` yet) — a red
      that proves the production type is missing, not a spec bug.

- [ ] **1.1.4 — Add a Kotlin spec (red).** Mirror 1.1.3 in
      `packages/kotlin/ttr-parser` (Kotest) for the same cases, referencing the
      Kotlin `TaggedBlockValue` (contracts §2.2). Red against the missing variant.

- [ ] **1.1.5 — Add the diagnostics specs (red).** Add specs for: unknown tag
      (C8 → diagnostic on `tagSource`), `language:`↔tag mismatch, and the
      `language:` deprecation warning (DESIGN §5/§6). These live where semantic
      diagnostics are tested (search existing diagnostic tests in `@modeler/semantics`).

- [ ] **1.1.6 — Confirm red, commit.** Run `pnpm --filter @modeler/parser test`
      and the Kotlin spec; confirm they fail for the **right** reason (missing
      type/variant, not a typo). Commit: `Section embedded-sql: Phase 1 fixtures
      (red)`.
