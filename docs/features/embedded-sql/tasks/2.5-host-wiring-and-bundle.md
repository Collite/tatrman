# 2.5 — VS Code + Designer wiring; bundle + broken-SQL guards

**Repo:** modeler. **Effort:** ~1 day. **Packages:** `@modeler/lsp`,
`@modeler/vscode-ext`, `@modeler/designer`. **Pre-flight:** 2.4 done. Read
architecture §5 and SPIKE S0.3 (the +155 KB budget). This stage delivers the
Phase 2 DoD: SQL colours in **both** hosts, within the bundle budget.

---

- [ ] **2.5.1 — Desktop bundle (`server-stdio`).** Confirm the Node LSP imports
      the full `@modeler/sql` (`.`) and that `pnpm --filter @modeler/lsp build`
      bundles the lexers (parsers will be needed in Phase 3). Launch VS Code F5
      (Extension Dev Host), open a `.ttr` with a `sql` block, **see colours**.
      Register the semantic-token legend client-side if the ext declares it.

- [ ] **2.5.2 — Browser bundle (`server-browser`) — lexer-only.** Point the
      Worker entry at `@modeler/sql/lexers` (NOT `.`) so parsers are excluded
      (E11). Rebuild and confirm the Designer renders SQL highlighting (it consumes
      the same LSP semantic tokens over the worker transport).

- [ ] **2.5.3 — Bundle-size guard test.** Add a CI check that fails if
      `dist/server-browser.js` gzipped exceeds the agreed budget (today 316 KB +
      155 KB headroom ≈ **480 KB gz** ceiling; set the exact number with the
      owner). This locks in the S0.3 decision and catches an accidental parser
      import into the Worker (which would add ~839 KB).

- [ ] **2.5.4 — Broken-SQL + placeholder end-to-end.** In VS Code, eyeball: a
      malformed query still colours the tokens it lexed; a `{param}` shows as a
      parameter colour. (Automated coverage is in 2.4; this is the host-level
      sanity pass — capture a screenshot for the PR.)

- [ ] **2.5.5 — TextMate fallback (optional, time-boxed).** If LSP semantic-token
      load latency is visible on open, add a minimal `source.sql` injection in
      `ttr.tmLanguage.json` for `"""sql`/`"""tsql`/`"""postgres` openers (DESIGN
      §"cheap path") for instant pre-LSP colour. Regenerate via
      `node packages/vscode-ext/scripts/generate-tm-grammar.ts`. Skip if not needed.

- [ ] **2.5.6 — Full green; Phase 2 DoD; commit.**
      ```bash
      pnpm -r build && pnpm -r test && pnpm -r lint
      ```
      Tick Phase 2 in [`INDEX.md`](INDEX.md). Confirm the Phase 2 DoD in
      [`../plan.md`](../plan.md): colours in VS Code + Designer, broken SQL still
      colours, `{param}` as parameter, positions verified, Worker within budget.
      Commit: `Section embedded-sql: host wiring + bundle guard (Phase 2 done)`.
