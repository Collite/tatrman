# 1.3 — TS walker: `TaggedBlockValue` + tag registry

**Repo:** modeler. **Effort:** ~1 day. **Files:**
`packages/parser/src/ast.ts`, `packages/parser/src/walker.ts`,
`packages/grammar/src/` (tag registry), `packages/semantics/src/` (diagnostics).

**Pre-flight:**
- Read DESIGN §3, §4, §5, §6 and contracts §2, §3.
- Study the existing `walkStringLiteralForm` and `dedent` in `walker.ts`
  (lines ~496–533) — you extend, not replace, them.

---

- [ ] **1.3.1 — Add the `TaggedBlockValue` type + union member.** In `ast.ts`, add
      the interface from contracts §2.1 and add it to the `sourceText` /
      `definitionSql` value union (`PropertyValue`). Add `SqlDialect` and (if not
      present) `LanguageKind`. ESLint forbids `any` — type precisely.

- [ ] **1.3.2 — Build the tag registry.** In `@modeler/grammar` add
      `tag-registry.ts` exporting `TAG_REGISTRY` and a `resolveTag(tag, defaultDialect?)`
      helper (contracts §3). Include `sql`, `ms-sql`/`tsql`/`mssql`,
      `postgres`/`postgresql`/`pg`, `duckdb`, plus the non-SQL tags
      (`transform`/`dataframe`/`relnode`). Unknown tag → `resolveTag` returns
      `undefined` (caller emits the diagnostic).

- [ ] **1.3.3 — Implement `walkEmbeddedBlock`.** In `walker.ts`, add a function for
      the `embeddedBlock` context:
      - `STRING_LITERAL` / `TRIPLE_STRING_LITERAL` → delegate to the existing
        `walkStringLiteralForm` (unchanged behaviour, incl. C5 trailing newline).
      - `TAGGED_BLOCK_LITERAL` → tag-peel (DESIGN §4 steps 1–4) → `dedent` (existing)
        → strip exactly one trailing `\r?\n` (step 6). Compute `tag`, resolve via
        the registry to `language`/`dialect`, and compute `tagSource`,
        `valueSource`, and `indentWidth` (= the common prefix length `dedent`
        removed; expose it from `dedent` or recompute identically).
      Wire `sourceTextProperty` / `definitionSqlProperty` walking to call it.

- [ ] **1.3.4 — Source-location math.** `tagSource` = token start `+3` (after `"""`)
      spanning the tag length. `valueSource` start = first char after the opener
      newline; honour the ANTLR `SourceLocation` convention (CLAUDE.md invariant:
      `endColumn = stopToken.column + stopTokenLength`, line 1-indexed, column
      0-indexed). Add a focused test asserting `tagSource` underlines exactly the
      tag in a multi-line fixture.

- [ ] **1.3.5 — Diagnostics (semantics).** In `@modeler/semantics`, emit:
      unknown-tag (anchored on `tagSource`), `language:`↔tag mismatch, and the
      `language:` soft-deprecation warning (DESIGN §5/§6). Infer `language` from the
      tag when the property is absent. These make the 1.1.5 specs green.

- [ ] **1.3.6 — Green the TS specs; lint; commit.**
      ```bash
      pnpm --filter @modeler/parser test
      pnpm --filter @modeler/semantics test
      pnpm --filter @modeler/parser --filter @modeler/semantics lint
      ```
      C1/C3/C4/C7/C9/C11 (TS side) and the diagnostics specs pass. Commit:
      `Section embedded-sql: TS TaggedBlockValue walker + tag registry`.
