# RG-P4 — TTR-M lexicon surface (workstream A — parallel grammar track)

> Pre-flight: **grammar 4.2 (semantics-block) merged**; a grammar-master change window scheduled (spec-version bump — the lexicon arc is the "grammar 4.3-class feature" of Q-13). DoD: [`../plan.md`](../plan.md) §RG-P4. Contracts: [`../contracts.md`](../contracts.md) §7. Check each box the moment its task is done.
>
> **Repo note (unlike P1–P3/P5–P6):** this phase lives in the **tatrman** repo — `TTR.g4` is canonical there (`packages/grammar/src/TTR.g4`), with the TS parser/semantics/LSP under `packages/*` and the Kotlin/Python parsers generated from the same `.g4`. Follow the grammar-regeneration procedure in the repo CLAUDE.md exactly (antlr-ng prebuild → TM grammar regen → commit only `TTR.g4` + tmLanguage; `generated/` is gitignored). Cross-target drift is caught by the conformance harness. The in-flight **search-block feature proceeds independently** — its flag relocation is this arc's compatible end state; do not touch its scope.

## S1 · Lexicon grammar + inline sugar + desugar {#s1}

Verify: `pnpm --filter @tatrman/parser test` + `pnpm --filter @tatrman/semantics test` green; `./gradlew :packages:kotlin:ttr-parser:test` green; the grammar conformance suite green across TS/Kotlin/Python parsers.

- [x] **T1 (test first — the grammar corpus).** Add parse fixtures in `packages/parser/src/__tests__/lexicon.test.ts` (+ mirrored Kotest cases): a canonical `model lexicon` file with `def term trzba { for: md.measure.net, forms: ["tržba","tržby","obrat","utržit"] }`, a `def pattern` with `match:`, a `def example` with `text:`; targets across all three layers (`er.*`, `db.*`, `md.*`); a locale-bearing unit header (the `db … schema` precedent applied — exact spelling per T2's grammar decision); rejection cases (unknown def kind in a lexicon model, `for:` ref missing). These fail until T2.
- [x] **T2.** The `TTR.g4` delta: the `lexicon` model code + the `term`/`pattern`/`example` def kinds + the unit-level locale parameter + the inline `lexicon{}` block attachable to data-bearing defs (er/db kinds AND `measure`/`dimension`/`cubelet` — RS-10 makes md kinds legal carriers). Regenerate per the CLAUDE.md procedure (parser prebuild, TM grammar). Keep the entry schema **minimal** — `for` + `forms`/`match`/`text` only; leave a grammar comment naming the additive-growth fields (`pos`, `weight` — RS-10's rejected-β-now, schema grows on parity evidence).
- [x] **T3 (test first — desugar, RS-9 T1).** `packages/semantics/src/__tests__/lexicon-desugar.test.ts`: an inline `lexicon { terms: [...] }` on `def measure net` desugars to canonical `term` entries targeting `md.measure.net`, **identical in every field** to hand-written canonical entries (the hero equivalence: inline vs canonical `tržba/obrat/utržit` produce the same resolved entry set); the **conflict rule copied from binding's discipline** — inline + canonical entries for the same target **merge** (union of forms) with a diagnostic on exact-duplicate forms (verify binding's exact rule in `er2db` handling first and mirror it; cite the source file in the test comment); inline entries default to the **deployment/base locale** (RS-11's rule).
- [x] **T4.** Implement desugar + conflict handling in `@tatrman/semantics` (the symbol-table/validator layer — the parser stays mechanical per the repo invariant); locale-on-unit resolution; `for:` cross-references resolved through the existing reference resolver with proper "unresolved reference" diagnostics.
- [x] **T5 (test first).** Referential-integrity cases: a `term` targeting a non-existent `md.measure.x` → named diagnostic with location; a lexicon in package A targeting package B's def follows the existing import/visibility rules (no special-casing).
- [x] **T6.** LSP surface minimum: hover on a lexicon entry shows the resolved target; go-to-definition from `for:` works (both fall out of the resolver if T4 registered symbols properly — assert, don't rebuild). Formatter handles the new constructs (block style per house conventions).
- [ ] **T7.** Grammar conformance suite green across all three generated parsers; **(TS side done: dumper + `out-ts/62,63.json` goldens committed; Python `walker.py`/`dump.py` + Kotlin `TtrWalker.kt`/`Definition.kt`/`ConformanceDump.kt` parity PENDING)** check the S1 box; commit `RG-P4.S1: lexicon model grammar, inline sugar, desugar` (grammar-master commit conventions).

## S2 · Search-block slim + legacy migration + consumer propagation {#s2}

Verify: full tatrman `pnpm -r test` green; a lexicon model round-trips into the snapshot in canonical form; deprecation warnings fire on every legacy vocabulary sub-property.

- [ ] **T1 (test first — the RS-32 migration table).** Data-driven migration tests: entity `aliases: [...]` → `term` entries · `search { aliases, keywords }` → `term` entries · `search { patterns }` → `pattern` entries · `search { examples }` → `example` entries — each producing the same canonical shape the S1 desugar produces (one pipeline, two sources). Deprecation diagnostics (warning severity, named ids) on every standalone legacy form.
- [ ] **T2.** Implement the migration path: the semantics layer treats legacy forms as a second sugar source desugaring into canonical entries (so consumers never see legacy shapes), emitting the deprecation warnings; `search{}` validation narrows to `searchable`/`fuzzy` + (deprecated) legacy passthrough. Provide the mechanical migration codemod (`ttr migrate-lexicon`-class script or LSP quick-fix) that rewrites legacy forms to lexicon entries in-place — trivia-preserving per the CST layer.
- [ ] **T3 (the descriptions consumer check — RS-32's deferred verification).** Grep/inspect every consumer of `search.descriptions` (snapshot builder, meta doors, get_model search hints, the fuzzy loader): if none reads it distinctly from `description`, execute the fold (migration + deprecation like T1); if one does, record the exception in the contracts changelog and keep it — **the default is the fold**; either way the outcome is written down.
- [x] **T4 (test first — RS-9 T3 propagation).** Snapshot tests: a model with canonical + inline + legacy vocabulary serializes into the snapshot as **canonical entries only** (one shape, per-locale units preserved, target refs resolved); `valueLabels` per-value alias widening (A4-β: `1: { label: {...}, aliases: ["živý"] }`) parses and rides the snapshot beside them.
- [ ] **T5.** Implement snapshot/metadata propagation (the canonical-only rule) + the A4-β `valueLabels` widening (grammar touch is part of this arc's window). This is the shape RG-P2's `SnapshotVocabularySource` stub was written against — cross-check the stub's fixture format and align it (name the alignment in the PR so the RG-P2 side swaps stub→real cleanly).
- [ ] **T6 (Q-15 resolved as a decision).** The vocabulary-coverage lint: implement a model-lint warning ("data-bearing kind X has no lexicon coverage for deployment locale Y") in the linter's existing rule framework **or** record the decision not to — one paragraph in the contracts changelog either way; if implemented, hero-class fixtures (a measure with no cs terms warns; with terms doesn't).
- [ ] **T7.** Full repo suite green; the hero round-trip (declare `tržba/obrat/utržit` inline → snapshot carries canonical entries → RG-P2's loader fixture format matches); check the S2 box + the RG-P4 review box after the phase-exit `/review`; commit `RG-P4.S2: search-block slim, legacy migration, canonical propagation`.
