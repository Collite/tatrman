# RG-P3 — Grounding extraction + kernel (workstream D)

> Pre-flight: RG-P0 DONE (fold lib + proto renames); can overlap RG-P1/P2 (independent of B/C). **Plus, inserted 2026-07-13: [`S0 — G2 closure`](./tasks-p3-s0-meta-semantics.md) (the `meta.v1` semantics projection + the `GroundingMetadataClient` seam) completes before S1's discovery wiring resumes** — S1's moved discovery code lands on the S0 port, and grounding tests never hand-build metadata protos. DoD: [`../plan.md`](../plan.md) §RG-P3. Contracts: [`../contracts.md`](../contracts.md) §3. Check each box the moment its task is done.
>
> **Nature of this phase:** mostly an **inheritance**, not a build — the DFP `[~]` stages are code-complete with test suites (chrono 70 · geo 67 · money 57 · mcp 9 · +18); RS-21 says inherit as-is and re-open only what the move breaks (the fix-at-rename list). The genuinely new work: the `ttr-grounding-core` kernel, the geo posture, Q-18, and the kind-named tools. Source: `ai-platform/services/{chrono,geo,money}` + the thin `grounding-mcp` (port 7153) → `tatrman-server/services/*`. Inherited invariants are NOT re-opened: recipe contract, `reference_datetime`-from-request, rules-first/LLM-fallback-off, semantic discovery over `semantics{}` **(now read through the S0 seam from `meta.v1`, not the ai-platform metadata protos)**.

## S1 · Extraction + `ttr-grounding-core` kernel + invariants {#s1}

Verify: `./gradlew :services:chrono:test :services:geo:test :services:money:test :services:ttr-grounding-mcp:test :libs:ttr-grounding-core:test` green (inherited counts preserved: ≥ 70+67+57+9); no per-service `RecipeBuilder`/`PlanExpr`/`SqlRenderer`/`Diacritics` copies remain (grep check).

- [x] **T1.** Move the three services + `grounding-mcp` from ai-platform into `tatrman-server/services/*` under the J-v2 names, wired to the RG-P0-renamed `org.tatrman.grounding.v1` proto (verify the `ResponseMessage` import wart fix landed in P0 — if the wart survived the rename, fix it now, it is on the RS-21 fix-at-rename list). **Metadata coupling goes through the S0 port** (`GroundingMetadataClient` domain-type interface + the `MetaV1` adapter; test fakes build domain objects, never protos). Inherit every test suite verbatim; the suites are the acceptance evidence — a failing inherited test means the move broke something, never that the test needs updating. — DONE: chrono (`c750369`), money (`4ec5a69`), geo (`15aab3b`), grounding-mcp (`b578d1f`). Each service's inherited discovery folds onto its own domain-typed port (`SemanticDiscovery`/`MoneyDiscovery`/`GeoDiscovery`) with a single `MetaV1*Discovery` adapter (client-side role filtering — meta.v1 has no server-side semantic filters) + a domain `FakeMetadataClient` + a `MetaV1*DiscoveryComponentSpec` proving the real projection against in-process Veles. ResponseMessage wart confirmed fixed (imports `org.tatrman.common.v1`). Combined verify green: **chrono 76 · money 66 · geo 79 · mcp 9 = 230 tests, 0 failures, 0 skipped**; ktlint clean; grep confirms meta.v1 reads only in each adapter. Two follow-ups for review: geo's `PostgresBoundaryStoreSpec` (Testcontainers) sits in `test` not `componentTest` (repo convention) and needs Docker Hub / Ryuk-disabled; geo grounding fixture `fixture-semantics-geo/poi.ttrm` is crafted (POI + `nameAttribute`).
- [x] **T2 (test first — kernel characterization).** Before extracting the kernel, capture golden `sql_preview` outputs: for each service, run its existing recipe tests and snapshot `(recipe → sql_preview)` pairs into `libs/ttr-grounding-core/src/test/resources/sql-preview-golden.jsonl` (≥ 15 per service, covering ValueBinding/FilterRecipe/JoinRecipe). Write `KernelCharacterizationTest` asserting the not-yet-existing kernel reproduces them byte-identically. — DONE (server `5db01f2`): `shared/libs/kotlin/ttr-grounding-core` created with the consolidated `PlanExpr` (union of the three services' builders) + `SqlRenderer` (chrono/geo byte-identical, +money `mul`). `sql-preview-golden.jsonl` = 45 curated cases (15/service, each of ValueBinding/FilterRecipe/JoinRecipe), each carrying the case's `plan.v1` Expression JSON + rendered SQL; `KernelCharacterizationTest` re-renders the built tree AND re-parses the stored JSON, asserting both byte-identical (47 tests green, ktlint clean). Behaviour-equivalence to the pre-extraction services is enforced by T3 keeping each inherited service suite green after repointing (below).
- [x] **T3.** Extract **`ttr-grounding-core`**: consolidate the triplicated `RecipeBuilder` scaffolding, `PlanExpr`, `SqlRenderer` into one Kotlin lib; the fold comes from the S-2 shared lib (delete the per-service `Diacritics` copies — this is the S-2 grounding call-site swap); the **`sql_preview`-derived-not-duplicated invariant** enforced once: the only way to produce a preview is rendering the `plan.v1` Expression tree the recipe carries (make the hand-built-string path unrepresentable — no public string-assembly API). All three services depend on the kernel; T2's characterization test goes green. — DONE (server `02a10d3`): deleted the 9 per-service `recipe/{PlanExpr,SqlRenderer}.kt` copies; chrono/money/geo now import `org.tatrman.grounding.core.{PlanExpr,SqlRenderer}`. Per-service `Diacritics.kt` deleted — recognizers/parsers fold through `org.tatrman.text.Normalization.fold` (ttr-text, the S-2 spec). Invariant enforced: `SqlRenderer` exposes only `render()` + `renderJoin()` (both Expression-derived); the hand-assembled JOIN string is centralised into `renderJoin`; `quote()` made **private** (no public string-assembly primitive). grep confirms services call only render/renderJoin and no PlanExpr/SqlRenderer/Diacritics copies remain. Behaviour preserved — every inherited suite green at its original count: **chrono 76 · money 66 · geo 79 · mcp 9 · kernel 49 = 279 tests, 0 failures**, ktlint clean. (The recipe *builders* stay per-service — they encode domain-specific targeting logic; only the shared PlanExpr/SqlRenderer/fold scaffolding was consolidated, matching the invariant's intent.)
- [x] **T4 (test first).** `NoClockReadsTest` (the D-T1 invariant, conformance-assertable): resolve „poslední fiskální čtvrtletí"-class inputs at two different injected `reference_datetime` values and assert the intervals differ accordingly; plus an arch-level check (Konsist or a grep-based test) that no service/kernel source calls `Instant.now()`/`LocalDate.now()`/`Clock.systemDefaultZone()` outside test code. — DONE (server `3792957`): `services/chrono/.../NoClockReadsTest.kt`. Behavioural — a relative span (`včera`/`minulý měsíc`, the „poslední fiskální čtvrtletí" class; fiscal-quarter itself lands in S2.T3/T4) grounded at two refs yields divergent intervals (2026-05-14 vs 2024-11-19). Architectural — a comment-stripped source scan of the five grounding main-source trees (chrono/money/geo/mcp + kernel, located via repo-root discovery) asserts zero wall-clock reads (`*.now()`, `Clock.system*`, `System.currentTimeMillis`); `System.nanoTime()` explicitly allowed (monotonic latency timer, per the services' own comments). chrono 79 green.
- [x] **T5.** Server-owned `GroundingContext` (D-T1): confirm the proto message (contracts §3) is the single shape all three services consume; document in KDoc that assembly (turn state → context) is agent-side; add the missing-`as_of` FX fail-loud test (`RG-GND-002`: time-versioned `fx_rate` table + no as-of ⇒ error, never a silent current-rate guess). — DONE (server `82677ad`): `GroundingContext` confirmed the single shape (GroundRequest field 5; chrono/money/geo all read `request.context`); proto message KDoc documents agent-side assembly + the no-ambient-reads invariant. RG-GND-002 both branches in `MoneyRecipeBuilderSpec`: current-rate no-ref ⇒ Ungroundable (reason names `reference_datetime`); transaction-date no-event_date (new `withFxTableNoEventDate` fixture) ⇒ Ungroundable (reason names `event_date`). money 67 green.
- [x] **T6 (D-T4 posture).** LLM-fallback tests: empty gateway URL (the default) ⇒ rules-only, recognizer-miss returns UNGROUNDABLE (not an LLM call); gateway configured ⇒ fallback fires only on rules-miss/low-confidence, result tagged `provenance.method=LLM` + structurally validated against the same `GroundingResult` schema. Both behaviors fixture-backed (they feed conformance assertions in RG-P6). — DONE (server `1e01d4b`): the posture was largely inherited across all three outcomes specs (no-fallback miss ⇒ UNGROUNDABLE; configured gateway ⇒ `source=LLM` on a miss; invalid-JSON LLM result ⇒ UNGROUNDABLE not a crash; rules-hit ⇒ `source=RULES`). Added the defining "fires ONLY on a miss" assertion to chrono/money/geo: a rules-hit with a `FakeLlmGateway` present stays `source=RULES` and the spy's `lastUserPrompt` is null (LLM never invoked). chrono 80 · money 68 · geo 80 green.
- [x] **T7.** S-3 on the inherited operator endpoints (any `/refresh`/reload hooks in the three services): admin-role-gated, unauthenticated refused — same pattern as RG-P2.S2.T6. Charts/values for the four workloads in `tatrman-server`. — DONE (server `ea10511`): **S-3 is a vacuous no-op** — the grounding services + MCP expose only public `/health`/`/ready`/`/status` GET probes; no `/refresh`/`/reload`/`/admin` mutating endpoints were inherited (unlike ttr-fuzzy), so nothing to gate (geo's boundary-cache priming is an install-time batch job, not a request-path hook). Charts: four env-agnostic Helm charts (`services/{chrono,geo,money,ttr-grounding-mcp}/k8s`) reusing the shared `ttr-service` library chart (mirror `services/veles/k8s`): chrono 7120/7220, geo 7121/7221 (Nominatim + PostGIS gated off, Q-19 no default Nominatim), money 7122/7222, ttr-grounding-mcp 7153 HTTP-only (backend gRPC wiring). All four `helm template` clean; Chart.lock + vendored tgz stay gitignored per repo convention.
- [x] **T8.** All suites green at inherited counts; grep confirms no duplicated kernel code; check the S1 box; commit `RG-P3.S1: grounding extraction, ttr-grounding-core kernel, invariants`. — DONE: full verify block green — **kernel 49 · chrono 80 · geo 80 · money 68 · ttr-grounding-mcp 9** (all ≥ the DFP inherited minima 70/67/57/9, plus the new kernel + T4/T5/T6 conformance tests); grep confirms zero duplicated `PlanExpr`/`SqlRenderer`/`Diacritics` copies (single kernel source). **RG-P3.S1 COMPLETE.** Remaining in RG-P3: S2 (geo posture RS-19, Q-18 fiscal quarter, kind-named MCP tools) + the phase-exit `/review`.

## S2 · Geo posture + kind-named tools + fiscal-quarter (Q-18) {#s2}

Verify: `./gradlew :services:geo:test :services:chrono:test :services:ttr-grounding-mcp:test` green; the 109+21 grounding eval corpus runs green as the extended-tier feed; a Nominatim-less deployment reports geo dark and fails loud, never wrong.

- [x] **T1 (test first — the RS-19 capability posture).** `GeoCapabilityTest`: (a) no Nominatim endpoint configured ⇒ `GetStatus` reports the geo capability off + `RG-GND-001`, and `Ground(LOCATION)` returns `UNAVAILABLE` fail-loud — never `UNGROUNDABLE`/"place doesn't exist"; (b) endpoint configured but 429/5xx ⇒ same `UNAVAILABLE` semantics (inherited, now asserted at the capability level); (c) **PostGIS surfacing (D-T2)**: `GetStatus.capabilities` reports `postgis` truthfully and the PG lowering path degrades documented-dark without it. — DONE (server `74fff75`): `GeoGroundingService` takes `nominatimConfigured`/`postgisAvailable` (wired in Application from `geo.nominatim.base-url` non-blank + boundary-store-is-PostGIS); GetStatus reports `nominatim`=ok|dark + `postgis`=ok|absent + RG-GND-001 message when dark. Behavioural half was already correct (ChainedPlaceResolver stops on geocoder `Unavailable` → gRPC UNAVAILABLE, never a false UNGROUNDABLE; NominatimClient `expectSuccess` maps 429/5xx to the throw). `GeoCapabilityTest` pins (a)/(b)/(c). geo 84 green.
- [x] **T2.** Boundary cache ON by default (flip the inherited 90-day Postgres cache default) + the **install-time priming step**: a small runner (CLI or K8s job) that warms the cache from (a) model-declared POIs and (b) distinct city values in member data (reuse the existing geocode + store code paths — the priming run is just a batch client). Test: after priming, a cached place resolves with Nominatim unreachable. — DONE (server `9e9bcd3`): cache is already ON by default (in-memory, 90-day TTL, get-before-geocode). Added `CachePrimer` (drives `PlaceResolver.resolve` over a name list — reuses the resolver's geocode+store side-effect, no duplicated geocoding; returns a `PrimingReport`) + `PrimingRunner` (K8s-Job/CLI main, non-request-path; wires NominatimClient + durable BoundaryStore from config, primes from `GEO_PRIME_PLACES`/args — the operator assembles model-POI names + distinct city values). `CachePrimerSpec`: after priming over WireMock, the place resolves from the cache with Nominatim UNREACHABLE while an un-primed place → Unavailable. geo 85 green.
- [x] **T3 (test first — Q-18, hero-parity-relevant).** `FiscalQuarterTest` in chrono: „poslední fiskální čtvrtletí" (+ „toto fiskální čtvrtletí", „minulé účetní čtvrtletí" variants) against a fixture period table with `yyyyQn`-class `period_code` ⇒ a `JoinRecipe` selecting the correct quarter row relative to the injected `reference_datetime`; plus the no-period-table case ⇒ the honest calendar-quarter `FilterRecipe` with the documented confidence. These fail if the recognizer only knows fiscal-*year* words. — DONE (server `7b488c5`): `FiscalQuarterTest` pins the 3 cs variants + en + a no-scope non-match + both recipe paths (yyyyQn table → JoinRecipe binding `2026Q1`; no table → datetime-bounds FilterRecipe).
- [x] **T4.** Implement the Q-18 extension where T3 exposes gaps: quarter-word recognition in the chrono rule table (inflected stems, diacritic-insensitive per the house style) + quarter-granularity period-table params in the semantics discovery (`period_code` format `yyyyQn` — discovery reads it via the S0 seam). Per D-T3 this is a small recognizer+params extension, not a re-architecture — if it grows beyond that, stop and flag. — DONE (server `7b488c5`): `fiscalQuarter` recognizer rule (quarter word + this/last scope, cs+en, folded stems) → PERIOD recognition w/ `yyyyQn` code + calendar interval relative to reference. `RecipeBuilder.reformatCode` passes `yyyyQn` through (would else mangle the 6-char code); table → JoinRecipe, no-table quarter → datetime-bounds FilterRecipe (NOT `period_start`, which the translator can't lower for yyyyQn). Stayed a small extension (~40 recognizer + ~15 recipe lines), no re-architecture. chrono 87 green.
- [x] **T5 (test first).** `KindNamedToolsTest` on `ttr-grounding-mcp`: the three tools `grounding.time:v1` / `grounding.geo:v1` / `grounding.money:v1` are registered (names ergonomic outside, each delegating to `Ground(kind=…)` on the generic proto inside — RS-28); every callback wrapped in `safeMcpTool` (timeout ⇒ well-formed `CallToolResult(isError=true)`, per EXAMPLES.md §3c); geo tool honors the capability posture (dark ⇒ a clean capability-off error, fixture-conditional). — DONE (server `ed46f3e`): `KindNamedToolsTest` pins the 3 capability ids loading from the manifests (category "grounding"), `safeMcpTool` timeout → `isError=true`, and the geo tool honouring the posture (a dark geocoder / gRPC UNAVAILABLE surfaces as a clean tool error). Kind delegation itself is pinned by the inherited ToolsTest.
- [x] **T6.** Implement the kind-named tools (Kotlin MCP SDK, `mcpStreamableHttp`, `McpJson`, the `ToolRegistry` pattern from `tools/query-mcp`); H-2 identity on the door per the house MCP base. Register the exact ids in the naming ledger (F-T1 feeds RG-P6). — DONE (server `ed46f3e`, per Bora's "full capabilities-mcp manifests" call): three `ToolCapability` YAML manifests (`grounding-{time,geo,money}.yaml`) with ids `grounding.{time,geo,money}:v1` + `ManifestLoader`/`registerWithCapabilities()` (mirrors `ttr-query-mcp`) wired into Application boot (warn-and-continue; no-op unless `CAPABILITIES_MCP_URL` set). Per contracts §2 the MCP tool names stay `ground_time`/`ground_geo`/`ground_money`; the kind-named ids are the registry/ledger identity. H-2 rides the house `installMcpKtorBase` door (mcp `security{}` block). Added jackson + capabilities-client deps.
- [x] **T7.** Q-19 documented: the chart ships **no default Nominatim endpoint**; values comment + docs give self-host guidance (public OSM Nominatim policy restricts production use) and name the RÚIAN artifact as the deferred CZ-deterministic arc. Wire the 109+21 eval corpus as a runnable (non-CI-gating yet — RG-P6 tiers it): `services/*/eval` runner green. — DONE (server `a23aa82`): geo `values.yaml` ships `nominatim.baseUrl=""` with an expanded Q-19 comment (public OSM not a valid prod endpoint; RÚIAN = deferred CZ-deterministic arc). `eval/grounding` moved into tatrman-server (109 bulk + 37 supplemental + 21 e2e); the no-cluster tier is green — `pytest tests/` = 18 passed (corpus validity + pure `report.py` core), no services needed; non-CI-gating (RG-P6 tiers it).
- [x] **T8.** All green incl. the corpus run; check the S2 box + the RG-P3 review box after the phase-exit `/review`; commit `RG-P3.S2: geo posture, fiscal quarter, kind-named grounding tools`. — DONE: full grounding verify green — **kernel 49 · chrono 87 · geo 85 · money 68 · ttr-grounding-mcp 12 = 301 tests, 0 failures** + eval corpus 18 no-cluster. **RG-P3.S2 COMPLETE.** ⚑ The RG-P3 review box stays unticked pending the phase-exit `/review` (Bora-triggered).
