# RG-P3 — Grounding extraction + kernel (workstream D)

> Pre-flight: RG-P0 DONE (fold lib + proto renames); can overlap RG-P1/P2 (independent of B/C). DoD: [`../plan.md`](../plan.md) §RG-P3. Contracts: [`../contracts.md`](../contracts.md) §3. Check each box the moment its task is done.
>
> **Nature of this phase:** mostly an **inheritance**, not a build — the DFP `[~]` stages are code-complete with test suites (chrono 70 · geo 67 · money 57 · mcp 9 · +18); RS-21 says inherit as-is and re-open only what the move breaks (the fix-at-rename list). The genuinely new work: the `ttr-grounding-core` kernel, the geo posture, Q-18, and the kind-named tools. Source: `ai-platform/services/{chrono,geo,money}` + the thin `grounding-mcp` (port 7153) → `tatrman-server/services/*`. Inherited invariants are NOT re-opened: recipe contract, `reference_datetime`-from-request, rules-first/LLM-fallback-off, semantic discovery over `semantics{}`.

## S1 · Extraction + `ttr-grounding-core` kernel + invariants {#s1}

Verify: `./gradlew :services:chrono:test :services:geo:test :services:money:test :services:ttr-grounding-mcp:test :libs:ttr-grounding-core:test` green (inherited counts preserved: ≥ 70+67+57+9); no per-service `RecipeBuilder`/`PlanExpr`/`SqlRenderer`/`Diacritics` copies remain (grep check).

- [ ] **T1.** Move the three services + `grounding-mcp` from ai-platform into `tatrman-server/services/*` under the J-v2 names, wired to the RG-P0-renamed `org.tatrman.grounding.v1` proto (verify the `ResponseMessage` import wart fix landed in P0 — if the wart survived the rename, fix it now, it is on the RS-21 fix-at-rename list). Inherit every test suite verbatim; the suites are the acceptance evidence — a failing inherited test means the move broke something, never that the test needs updating.
- [ ] **T2 (test first — kernel characterization).** Before extracting the kernel, capture golden `sql_preview` outputs: for each service, run its existing recipe tests and snapshot `(recipe → sql_preview)` pairs into `libs/ttr-grounding-core/src/test/resources/sql-preview-golden.jsonl` (≥ 15 per service, covering ValueBinding/FilterRecipe/JoinRecipe). Write `KernelCharacterizationTest` asserting the not-yet-existing kernel reproduces them byte-identically.
- [ ] **T3.** Extract **`ttr-grounding-core`**: consolidate the triplicated `RecipeBuilder` scaffolding, `PlanExpr`, `SqlRenderer` into one Kotlin lib; the fold comes from the S-2 shared lib (delete the per-service `Diacritics` copies — this is the S-2 grounding call-site swap); the **`sql_preview`-derived-not-duplicated invariant** enforced once: the only way to produce a preview is rendering the `plan.v1` Expression tree the recipe carries (make the hand-built-string path unrepresentable — no public string-assembly API). All three services depend on the kernel; T2's characterization test goes green.
- [ ] **T4 (test first).** `NoClockReadsTest` (the D-T1 invariant, conformance-assertable): resolve „poslední fiskální čtvrtletí"-class inputs at two different injected `reference_datetime` values and assert the intervals differ accordingly; plus an arch-level check (Konsist or a grep-based test) that no service/kernel source calls `Instant.now()`/`LocalDate.now()`/`Clock.systemDefaultZone()` outside test code.
- [ ] **T5.** Server-owned `GroundingContext` (D-T1): confirm the proto message (contracts §3) is the single shape all three services consume; document in KDoc that assembly (turn state → context) is agent-side; add the missing-`as_of` FX fail-loud test (`RG-GND-002`: time-versioned `fx_rate` table + no as-of ⇒ error, never a silent current-rate guess).
- [ ] **T6 (D-T4 posture).** LLM-fallback tests: empty gateway URL (the default) ⇒ rules-only, recognizer-miss returns UNGROUNDABLE (not an LLM call); gateway configured ⇒ fallback fires only on rules-miss/low-confidence, result tagged `provenance.method=LLM` + structurally validated against the same `GroundingResult` schema. Both behaviors fixture-backed (they feed conformance assertions in RG-P6).
- [ ] **T7.** S-3 on the inherited operator endpoints (any `/refresh`/reload hooks in the three services): admin-role-gated, unauthenticated refused — same pattern as RG-P2.S2.T6. Charts/values for the four workloads in `tatrman-server`.
- [ ] **T8.** All suites green at inherited counts; grep confirms no duplicated kernel code; check the S1 box; commit `RG-P3.S1: grounding extraction, ttr-grounding-core kernel, invariants`.

## S2 · Geo posture + kind-named tools + fiscal-quarter (Q-18) {#s2}

Verify: `./gradlew :services:geo:test :services:chrono:test :services:ttr-grounding-mcp:test` green; the 109+21 grounding eval corpus runs green as the extended-tier feed; a Nominatim-less deployment reports geo dark and fails loud, never wrong.

- [ ] **T1 (test first — the RS-19 capability posture).** `GeoCapabilityTest`: (a) no Nominatim endpoint configured ⇒ `GetStatus` reports the geo capability off + `RG-GND-001`, and `Ground(LOCATION)` returns `UNAVAILABLE` fail-loud — never `UNGROUNDABLE`/"place doesn't exist"; (b) endpoint configured but 429/5xx ⇒ same `UNAVAILABLE` semantics (inherited, now asserted at the capability level); (c) **PostGIS surfacing (D-T2)**: `GetStatus.capabilities` reports `postgis` truthfully and the PG lowering path degrades documented-dark without it.
- [ ] **T2.** Boundary cache ON by default (flip the inherited 90-day Postgres cache default) + the **install-time priming step**: a small runner (CLI or K8s job) that warms the cache from (a) model-declared POIs and (b) distinct city values in member data (reuse the existing geocode + store code paths — the priming run is just a batch client). Test: after priming, a cached place resolves with Nominatim unreachable.
- [ ] **T3 (test first — Q-18, hero-parity-relevant).** `FiscalQuarterTest` in chrono: „poslední fiskální čtvrtletí" (+ „toto fiskální čtvrtletí", „minulé účetní čtvrtletí" variants) against a fixture period table with `yyyyQn`-class `period_code` ⇒ a `JoinRecipe` selecting the correct quarter row relative to the injected `reference_datetime`; plus the no-period-table case ⇒ the honest calendar-quarter `FilterRecipe` with the documented confidence. These fail if the recognizer only knows fiscal-*year* words.
- [ ] **T4.** Implement the Q-18 extension where T3 exposes gaps: quarter-word recognition in the chrono rule table (inflected stems, diacritic-insensitive per the house style) + quarter-granularity period-table params in the semantics discovery (`period_code` format `yyyyQn`). Per D-T3 this is a small recognizer+params extension, not a re-architecture — if it grows beyond that, stop and flag.
- [ ] **T5 (test first).** `KindNamedToolsTest` on `ttr-grounding-mcp`: the three tools `grounding.time:v1` / `grounding.geo:v1` / `grounding.money:v1` are registered (names ergonomic outside, each delegating to `Ground(kind=…)` on the generic proto inside — RS-28); every callback wrapped in `safeMcpTool` (timeout ⇒ well-formed `CallToolResult(isError=true)`, per EXAMPLES.md §3c); geo tool honors the capability posture (dark ⇒ a clean capability-off error, fixture-conditional).
- [ ] **T6.** Implement the kind-named tools (Kotlin MCP SDK, `mcpStreamableHttp`, `McpJson`, the `ToolRegistry` pattern from `tools/query-mcp`); H-2 identity on the door per the house MCP base. Register the exact ids in the naming ledger (F-T1 feeds RG-P6).
- [ ] **T7.** Q-19 documented: the chart ships **no default Nominatim endpoint**; values comment + docs give self-host guidance (public OSM Nominatim policy restricts production use) and name the RÚIAN artifact as the deferred CZ-deterministic arc. Wire the 109+21 eval corpus as a runnable (non-CI-gating yet — RG-P6 tiers it): `services/*/eval` runner green.
- [ ] **T8.** All green incl. the corpus run; check the S2 box + the RG-P3 review box after the phase-exit `/review`; commit `RG-P3.S2: geo posture, fiscal quarter, kind-named grounding tools`.
