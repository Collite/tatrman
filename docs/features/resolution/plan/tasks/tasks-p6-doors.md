# RG-P6 — Exposure: doors + conformance (workstream F)

> Pre-flight: RG-P5 DONE (the core is real); RG-P3 DONE (grounding tools exist); the mcp-surface §2.2 rows scheduled. DoD: [`../plan.md`](../plan.md) §RG-P6 — **this phase's exit = the global DONE / SV-P3 parity bar.** Contracts: [`../contracts.md`](../contracts.md) §4–§6. Check each box the moment its task is done.
>
> **The organizing line, enforced here:** doors expose the deterministic; agents own the generative. The `resolve` door exposes RG-P5's core only — the Resolving Agent's LLM ladder is never a door (RS-27). Kotlin MCP SDK patterns per EXAMPLES.md §3 (`mcpStreamableHttp`, `McpJson`, `safeMcpTool`, the `query-mcp` registry precedent); H-2 identity = bearer-only, OBO, fail-closed.

## S1 · The `resolve` door + grounding tools on the surface {#s1}

Verify: `./gradlew :services:ttr-resolver:test` (door module) green; an MCP client round-trips resolve → clarification → resume through the door; an identity-less call is refused.

- [ ] **T1 (test first — the door contract).** `ResolveDoorTest` (MCP-client-driven, streamable HTTP): the `resolve.*:v1` tool (exact ids from the naming ledger — F-T1; register them first, cite the ledger entry) accepts a fresh question and returns bindings + universal spans + the `parse` passthrough + the `capabilities` echo (F-T3); a clarification response carries options + the resume token **as an opaque string** that round-trips through a second tool call and binds via the pin; **H-2 fail-closed**: a call with no bearer identity is refused before any resolution work; every callback is `safeMcpTool`-wrapped (a forced timeout yields `CallToolResult(isError=true)` with the errorCode shape, never a broken stream).
- [ ] **T2.** Implement the resolve door on `ttr-resolver` (the service is the door — RS-22 chose standalone partly for this): `installMcpKtorBase` + `mcpStreamableHttp` + the `ToolRegistry` pattern; `McpJson` as the only JSON instance on MCP routes; OBO identity threaded to the service call (the per-user identity matters — clarification options may reflect user-visible vocabulary, RS-17's posture documented at the door, F-T2).
- [ ] **T3 (test first — refusal-over-guess, the door's signature assertion).** Fixtures where the deterministic core *cannot* confidently bind (ambiguous member, below-threshold scores): the door returns clarification-or-refusal — **never a guessed binding**; assert no `Resolution` carries a binding below the bind threshold. These fixtures are written to the conformance `calls:` schema from day one (they seed S2.T2's core tier).
- [ ] **T4.** Author the **mcp-surface §2.2 rows** (in the tatrman repo's `docs/ecosystem/server/design/mcp-surface.md`): the `resolve.*:v1` door row (OBO, resume-token semantics, refusal-over-guess assertion, determinism promise) + the three `grounding.{time,geo,money}:v1` rows (capability/GetStatus surfacing, geo conditional-on-capability per RS-19) — additive under J-v2, no existing id changes; wording final per F's §6 deliverable note.
- [ ] **T5.** Finalize the grounding tools on the contracted surface: confirm the RG-P3 tool ids match the ledger + §2.2 rows exactly (a mismatch here is a rename-class bug — fix the ledger side only if the tools never shipped); capability echo verified against a dark-geo deployment.
- [ ] **T6.** Door docs: the determinism promise stated at the door (what `resolve.*:v1` guarantees and what it never does — no LLM, refusal-over-guess, provenance on every binding); RS-17 visible-by-declaration noted at the door. Check the S1 box; commit `RG-P6.S1: resolve door, surface rows, refusal-over-guess`.

## S2 · Three-tier conformance + fold audit + register updates {#s2}

Verify: the gating service-level tier runs green in CI with **no DFP dependency**; the fold audit passes across all call sites; every register update is committed; the global-DONE walk passes.

- [ ] **T1 (the gating service-level tier — the SV-P3 instrument).** Wire into CI as gating: the ENTITIES_ONLY corpus (RG-P5.S1.T6) + the grounding 109+21 eval set (RG-P3.S2.T7) + the Q-17 fuzzy match corpus (RG-P2.S1.T1) — one runnable target (`conformance-service-level`), self-contained in-repo, green required. This is the "parity demonstrated" gate; its inputs' provenance (spike numbers, corpus hashes) cited in the runner's README.
- [ ] **T2 (the E2E core tier — schema + seeds).** Define the `calls:` conversation-fixture schema (the assertion vocabulary the RO-25 fixture design reserved: expected tool-call sequence + arguments + outcome per turn) and author **3–5 seed fixtures**: the hero end-to-end, one refusal-over-guess case (from S1.T3), one clarification round-trip (token-resumed), one geo-dark degrade. Mark the tier's full authoring (~25–40 @100%) as **SV-P4 work with the reference Golem** — the schema + seeds land now so SV-P4 authors against a real harness, not a blank page.
- [ ] **T3 (the non-gating extended tier — the slot).** The pilot-corpus runner: same harness, `scored-not-gating` mode (reports pass-rate, never fails CI), reading an external corpus path (the anonymized pilot conversations arrive via RO-19 ask ③ — the slot exists even while the corpus doesn't; document the ask's status in the runner README).
- [ ] **T4 (the S-2 fold audit — closes the sweep's consolidation).** One test module importing the golden fold vectors (RG-P0.S3.T1) and asserting **byte-identical folding at every call site**: fuzzy (swapped P0) · the grounding kernel (swapped P3) · **meta.search — swap it here** (the last of the ≥5 copies; the metadata service's scorer folds via the shared lib from this commit on). Any site that can't import the lib directly gets a documented characterization bridge — no site keeps a private fold.
- [ ] **T5 (register updates — design.md §8, all five).** In the tatrman repo: (a) `docs/ecosystem/next-steps-260710-execution.md` item 4 → points at `docs/features/resolution/design/design.md` + this plan (RS-2's amendment executed); (b) mcp-surface §2.2 rows confirmed landed (S1.T4); (c) `docs/ecosystem/server/design/architecture.md` §3 roster → the split resolver line (deterministic core service + kantheon Resolving Agent) + the `ttr-grounding-core` roster entry; (d) `resolver-rewrite.md` header → superseded-by note (R1–R4/RQ-1..5 → RS-22..27/30 mapping); (e) the RO-13 review notes → two consumers on the snapshot channel.
- [ ] **T6 (the global-DONE walk).** Execute [`../plan.md`](../plan.md)'s global definition of DONE as a checklist run: the hero resolves end-to-end through the deterministic core, self-hosted + offline parse, grounded universal spans, batch-fuzzy-gated domain spans, intent agent-side; the gating tier green with no DFP dependency; S-1/S-4 provenance on every binding; the door asserts determinism + refusal-over-guess; **no path below the door line calls an LLM** (the RG-P5 arch test + a door-level trace check). Record the walk's evidence in the phase progress doc.
- [ ] **T7.** Check the S2 box + the RG-P6 review box after the phase-exit `/review` (= the SV-P3 parity bar); commit `RG-P6.S2: three-tier conformance, fold audit, register updates`.
