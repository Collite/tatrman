# RG-P2 — `ttr-fuzzy`: the vocabulary matcher (workstream B)

> Pre-flight: RG-P1 DONE (in-cluster `BatchLemmatize` exists — the lemma axis's enabler); RO-13 snapshot schema available **or** the live-metadata step-one adapter accepted (tracker pre-flight). DoD: [`../plan.md`](../plan.md) §RG-P2. Contracts: [`../contracts.md`](../contracts.md) §2. Check each box the moment its task is done.
>
> **Boundaries that must not move:** `fuzzy.match:v1` is PINNED (GI-2) — everything here is additive; the cascade semantics (first algorithm whose top-1 ≥ its minScore wins) are contract-observable and must not drift; explicit-but-unknown category returns EMPTY, never global (the leak-guard, `RG-FUZ-002`). Service home: `tatrman-server/services/ttr-fuzzy` (Kotlin/Ktor, gRPC 7203 / REST 7103). Fold via the shared S-2 lib (wired in RG-P0.S3.T3).

## S1 · Q-17 referee corpus + source-tagged categories + lemma axis ON {#s1}

Verify: `./gradlew :services:ttr-fuzzy:test` green; the Q-17 corpus file exists and is cited from the tests; the inflection cases demonstrably need the lemma axis (pass with it on, fail with it off).

- [x] **T1 (test first — author the Q-17 referee corpus).** `match-quality-corpus.jsonl`: 40 cases across diacritics/inflection/multi-word-order/typos + data-driven `MatchQualityCorpusTest` (FunSpec) over a fixture vocabulary. *(Done; all 40 green with the fixture lemma axis. Provenance header in the corpus. Fixture vocab + a deterministic fixture `Lemmatizer` live in the test — no ttr-nlp dependency.)*
- [x] **T2 (test first).** `SourceTagTest`: MEMBER category → `{source=MEMBER, id=pk, target_ref=absent}`; VOCABULARY category → `{source=VOCABULARY, target_ref=lexicon, id}`; both through the same cascade. *(Done; 3 cases green.)*
- [x] **T3.** Extend the model with the source dimension (`SourceTag{MEMBER, VOCABULARY}`, `target_ref`, `Provenance{producer, method, raw_score}` — S-4). Pinned `MatchRequest` untouched; additive. Update gRPC + REST mappers. *(Done: `Candidate.source/targetRef` + `Candidate.vocabulary()`, `FuzzyMatchResult` + `Provenance`, matcher propagation; proto additive `FuzzyMatch.{source,target_ref,provenance}` + `FuzzyMatchResponse.vocabulary_version` + `SourceTag`/`Provenance`; gRPC + REST (fuzzy-common DTO) mappers.)*
- [x] **T4.** Turn the lemma axis ON: repoint `NlpLemmatizer` to RG-P1's gRPC `BatchLemmatize` (batched); apply at load + query time; default `nlp.enabled=true`; degradable. *(Done: injectable `NlpBatchClient` + `GrpcNlpBatchClient` over `org.tatrman.nlp.v1.NlpService.BatchLemmatize`; `NlpLemmatizer` rewritten (one rpc for N tokens, positional, folded, degrade-on-failure/shape-mismatch); `application.conf` `nlp.enabled=true` + port 7271; `LemmatizerTest` rewritten with a fake client.)*
- [x] **T5 (the axis-matters proof).** `MatchQualityAxisProofTest`: inflection class ON vs OFF. *(Done — the honest form: with the axis every inflected case lands an EXACT lemma match (score ≥0.999); with it off the matcher keeps only the surface axis, so exact-count + total score strictly drop. My corpus's inflections are near-misses surface fuzzy also ranks top-1, so the proof is score-quality, not top-1 flip — still a real regression guard.)*
- [x] **T6 (characterization — the GI-2 guard).** *(Done: the full pre-existing suite stays green after the additive T3/T4 changes — 129 tests, cascade/minScore/diacritics/EMPTY-on-unknown all byte-identical. `fuzzy-mcp` + `fuzzy-common` consumers build clean.)*
- [x] **T7.** All S1 tests green; check the S1 box; commit `RG-P2.S1: …`. *(Done.)*

## S2 · Snapshot read + alias tables + BatchMatch + refresh/staleness {#s2}

Verify: `./gradlew :services:ttr-fuzzy:test` green; the hero's `Octavie` (MEMBER) and `pobočkách` (VOCABULARY) both resolve through one `BatchMatch` call, source-tagged; an unauthenticated `/refresh` is refused.

- [ ] **T1 (test first).** `BatchMatchTest`: `BatchMatch{spans:[{query, categories[], limit}], locale?}` returns positional `results[]`, each a full `MatchResponse` with `vocabulary_version`; N spans × M categories = ONE rpc; a span with an unknown explicit category gets EMPTY in its slot (leak guard preserved per-slot). Write against the not-yet-existing rpc.
- [ ] **T2.** Implement `BatchMatch` (contracts §2): fan spans across the existing match internals with a bounded parallelism (`coroutineScope { async }` per EXAMPLES.md §4's fan-out shape); one `vocabulary_version` read per call (consistent snapshot of the in-memory repo — take the reference once, before the fan-out).
- [ ] **T3 (test first).** `DeclaredVocabularyLoaderTest` against a **snapshot stub**: define `SnapshotVocabularySource` (interface: `fetch(): DeclaredVocabulary` + `hash(): String`) with a fixture impl serving lexicon terms (`tržba/obrat/utržit → md.measure.net`, `pobočka → er branch`) + `valueLabels`; assert they load into VOCABULARY-tagged categories keyed by target kind. **RO-13 pending:** the stub IS the seam — a live-metadata step-one adapter and the real snapshot reader both implement it later; note this in the interface KDoc (rule 6).
- [ ] **T4.** Implement the declared-vocabulary load path (snapshot stub → VOCABULARY categories, folded + lemmatized at load like member values) **and** alias-table ingestion (RS-12-γ): `MetadataLoaderSource` reads `semantics{kind: alias_table}` declarations and composes the additional `SELECT alias, pk` alongside the primary name column, merged into the owning member category. Reuse the `SqlComposer` identifier-validation discipline.
- [ ] **T5 (test first).** `RefreshDisciplineTest`: two clocks — (a) member data refreshes on interval with atomic swap (loader failure preserves previous cache — existing behavior, now asserted); (b) declared-vocabulary config reloads when the snapshot hash changes and NOT when it doesn't; (c) `vocabulary_version` = snapshot hash + per-category load timestamp, echoed on every response and in `GetStatus` (S-1 applied to B).
- [ ] **T6.** Implement the two-clock refresh + `GetStatus` (category discovery + staleness per contracts §2); **admin-gate `/refresh`** (S-3): H-2 identity + `admin` role check; test that an unauthenticated call is refused and an admin call reloads.
- [ ] **T7.** B-T4 loader report: PK-skipped declared columns (`no_pk`/`composite_pk`) surfaced as a structured report (`RG-FUZ-001` warnings) retrievable via `GetStatus` — estates learn which declared columns aren't actually searchable. Plus the documentation duties: RS-17 visible-by-declaration (modeling manual at `search{fuzzy}` + security docs), the Q-8 open-vs-harvest line (interval/on-refresh = open; continuous CDC = platform tier on the `LoaderSource` SPI), and the B-T3 memory envelope note (N replicas × full in-memory vocabulary; per-estate sizing from the Q-10.T5 cardinality curve).
- [ ] **T8.** Full suite green; the hero check passes (one `BatchMatch`: `octavie` → MEMBER product candidate via lemma, `pobočkách` → VOCABULARY branch-entity term); check the S2 box + the RG-P2 review box after the phase-exit `/review`; commit `RG-P2.S2: snapshot vocabulary, alias tables, BatchMatch, refresh discipline`.
