# RG-P5 — `ttr-resolver`: the deterministic core (workstream E)

> Pre-flight: RG-P2 DONE (`BatchMatch` + source-tagged vocabulary); **the Q-20 spike verdict read and applied** (see S1.T0); RG-P4's declared vocabulary reachable via the snapshot — or the RG-P2 stub if the grammar arc lags (rule 6). DoD: [`../plan.md`](../plan.md) §RG-P5. Contracts: [`../contracts.md`](../contracts.md) §4–§5. Check each box the moment its task is done.
>
> **The one rule that defines this phase: ZERO LLM in the service.** The module has **no llm-gateway dependency** — enforced by an arch test, not convention (S1.T5). The LLM escalation ladder is the kantheon Resolving Agent's job (Q-21, kantheon-side planning); this phase pins the contract it reuses. Home: `tatrman-server/services/ttr-resolver` (Kotlin). The live resolver (`ai-platform/agents/resolver`) is the *reference for porting mechanics* (thresholds, HMAC, registry) — not for the pipeline shape, which changes per RS-23.

## S1 · Proto reshape + the deterministic core (parse → gate → bind) {#s1}

Verify: `./gradlew :services:ttr-resolver:test` green; the ENTITIES_ONLY corpus passes at the service level; the arch test proves no LLM dependency; measured span-gating precision matches the Q-20 spike numbers.

- [ ] **T0 (conditional — the Q-20 gate).** Read `../spikes/q20-span-gating.md`. **GO** ⇒ proceed, cite the precision/recall numbers as this phase's acceptance baseline. **GO-WITH-FALLBACK** ⇒ implement the fallback the spike sketched (deterministic sibling-column co-scoring / code-pattern detection, or the narrow agent-side pre-filter that keeps the *service* LLM-free) as an explicit first work item — write its tests from the spike's failure cases before anything else. **NO-GO** ⇒ stop; the phase re-opens design (E2 fork) — do not build around it.
- [ ] **T1 (test first — the reshaped proto).** `ResolverProtoTest`: `org.tatrman.resolver.v1` per contracts §4 round-trips — **no** `function_specs`, **no** `ResolveMode` (the NORMAL machinery is gone); `EntityBinding` carries `BindingProvenance{vocabulary_source, algorithm, score, tier, snapshot_hash, model_versions[]}` (S-1 + S-4 + RS-15) and the `degraded` flag; `ResolveResponse` carries the `parse` passthrough (E-T1) and the `capabilities` echo. Write against the not-yet-reshaped proto.
- [ ] **T2.** Reshape the proto + regenerate; port the service skeleton (Ktor + gRPC per EXAMPLES.md §1; the state-machine-as-suspending-functions dispatch shape per EXAMPLES.md §4 — keep the sealed `NodeResult` pattern, drop the LLM nodes entirely). Delete the dead `resolutionCache` (E-T3 — don't port dead machinery); keep the consumer-side `nlpCache` pattern (RS-6/T3).
- [ ] **T3 (test first — span proposal).** `SpanProposalTest`: dep-parse noun heads (from a fixture `AnalyzeResponse` of the hero) + **n-gram windows including multi-word nominal phrases** (`pražských pobočkách` as one candidate — the live single-head TODO is fixed here, it is load-bearing for the hero) produce the expected candidate span set; universal-typed spans (NER-tagged) are excluded from domain candidates; a parse-less input (degraded language) still yields n-gram candidates (the R4-γ floor).
- [ ] **T4 (test first — gateSpans, the heart).** `GateSpansTest` against a fake `BatchMatch` loaded with the Q-20 harness vocabulary: all candidate spans → one `BatchMatch` call (never per-span RPCs — B-T1's point); thresholds ported from the live config (bind ≥ 0.5, ambiguity gap 0.05, exact-match 0.9999 — cite `agents/resolver` config as provenance); entity-identity dedup (same resolved id via two spans → one binding); ambiguity → clarification candidates (max 20 options, the live cap); the sibling-column and code-vs-name cases from the Q-20 report handled per its decided approach; MEMBER hits → `Domain{resolved_id}`, VOCABULARY hits → the target-ref'd binding.
- [ ] **T5.** Implement the core pipeline: `parse` (via `nlp.v1`, capability matrix read) → `extractUniversal` (NER → `UniversalEntityType`, port the label mapping incl. the CNEC leading-letter map with its brittleness comment) → `proposeDomainSpans` (T3) → `gateSpans` (T4) → assemble `Resolution | AwaitingClarification`. **Arch test:** `NoLlmDependencyTest` — the module's runtime classpath contains no llm-gateway client (assert via a Gradle dependency check or Konsist rule); this is the RS-23 boundary made mechanical.
- [ ] **T6 (the parity instrument).** Wire `ucetnictvi_entities_only.jsonl` (+ the `seed.jsonl` entity subset) as the **service-level gating suite**: bindings match expected entities at the live pass-rate gate (≥ 80%, the inherited eval threshold) AND the measured precision/recall matches the Q-20 spike numbers within tolerance (the spike is the baseline; regressions are bugs). Every binding in the run carries full provenance (S-1/S-4 asserted corpus-wide).
- [ ] **T7.** S1 green; check the S1 box; commit `RG-P5.S1: reshaped proto, deterministic core, zero-LLM arch test`.

## S2 · Snapshot registry + HMAC tokens + degrade {#s2}

Verify: `./gradlew :services:ttr-resolver:test` green; a clarification round-trips via a signed token; a tampered token is refused; an unsupported-language resolve returns a labeled floor.

- [ ] **T1 (test first — the one-channel registry).** `SnapshotRegistryTest`: the registry (entity types, categories, lexicon terms, locales, thresholds) builds from the **same** `SnapshotVocabularySource` seam RG-P2 defined (one channel, two consumers — RS-24); the `snapshot_hash` lands in every `BindingProvenance`; a hash change reloads the registry; **caller-supplied per-request `Registry` wins** over the snapshot default; the live-metadata step-one adapter satisfies the same interface (RO-13 pending, rule 6 — name the adapter, don't invent a coupling).
- [ ] **T2.** Implement the snapshot-fed registry + staleness echo; startup = dev-mode live-metadata read behind the same seam (E3-β as step one, per RS-24).
- [ ] **T3 (test first — RS-26).** `ResumeTokenTest` per contracts §5: sign/verify round-trip (`payload.options` = the exact offered set); resume with a `selected_option_id` matching a signed pin binds at confidence 1.0 with **no re-fuzzy** (assert the fuzzy client is not called); tampered payload / wrong key / unknown `key_id` → `RG-RES-002` refusal; key rotation (old `key_id` verifies during the rotation window); the token is opaque-string-safe for MCP transport (base64url, no framing chars).
- [ ] **T4.** Port the HMAC resume-token mechanism from the live resolver (proven incl. rotation — porting mechanics, not redesign); document the token schema in the proto/KDoc as contract-visible (contracts §5); key management noted for the chart (S-3 discipline).
- [ ] **T5 (test first — RS-25 degrade).** `DegradeTest`: capability matrix reports no cs NER ⇒ universal hints thin out but the domain path binds unaffected (span proposal is dep-parse/n-gram based); matrix reports an unsupported language ⇒ fold+fuzzy floor, every binding `degraded=true` + `RG-RES-001`; the `capabilities` echo in the response matches what actually backed the resolve (F-T3's honesty requirement, asserted at the service level before the door exists).
- [ ] **T6.** Implement capability-matrix consumption (a `GetStatus` read at startup + refresh, branch points in the pipeline) — the resolver is the first consumer that must *visibly* branch on the matrix (RS-25).
- [ ] **T7.** Full suite + the hero end-to-end at the service level: `Octavie` + `pobočkách` bind deterministically with zero LLM, provenance-carrying, `snapshot_hash` populated; a synthetic ambiguity clarifies and resumes via pin. Record in the phase progress doc that the **kantheon-side re-plumb (Q-21, Themis ⚑) is intentionally NOT here** — the contract consequences (proto slimming, reuse seam) are done, the agent work is kantheon planning. Check the S2 box + the RG-P5 review box after the phase-exit `/review`; commit `RG-P5.S2: snapshot registry, resume tokens, capability degrade`.
