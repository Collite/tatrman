# RG-P1 — `ttr-nlp` self-hosting (workstream C)

> Pre-flight: RG-P0 DONE (Q-10 sizing gates backend resource requests; Q-10 parity table = the S2 fixture seed; the fold lib exists). DoD: [`../plan.md`](../plan.md) §RG-P1. Contracts: [`../contracts.md`](../contracts.md) §1. Check each box the moment its task is done.
>
> **Shape of the work.** The front stays Python 3.13 / FastAPI + gRPC and becomes **engine-free** (contract + routing + langid; no torch, no models). Each model-bearing engine becomes its own backend image. The endpoint swap (Lindat → in-cluster) is *config* — the adapters already speak the protocol. **S-1 is a hard invariant:** no empty/default model anywhere; every response echoes engine + version.

## S1 · `org.tatrman.nlp.v1` gRPC + capability matrix + engine-free front {#s1}

Verify: `pytest services/ttr-nlp/tests/` green; `grpcurl` (or a test client) against a locally-run front returns an `Analyze` response with a populated `used[]`; `GetStatus` returns the capability matrix. Confirm current FastAPI + grpc-python APIs with `context7` before writing handlers.

- [x] **T1 (test first).** `test_contract_shapes.py`: assert the `org.tatrman.nlp.v1` messages (contracts §1) round-trip — `AnalyzeRequest{text, language?, ops[], mode}`, `AnalyzeResponse{tokens[], sentences[], entities[], detected_language, used[]}`, `BatchLemmatizeRequest/Response`, `StatusResponse{ready, capabilities[]}`. Include a test that **`used[]` is non-empty and no `EngineVersion.model` is blank on every `Analyze` response** (S-1 lives in config + response echo — `AnalyzeRequest` deliberately carries no model field; the route-level assertion is S1.T3). Write against the not-yet-generated proto. *(Done: `tests/test_contract_shapes.py`; S-1 invariant helper `nlp_service/contract.iter_s1_violations`.)*
- [x] **T2.** Author `proto/org/tatrman/nlp/v1/nlp.proto` (contracts §1): `NlpService{Analyze, BatchLemmatize, GetStatus}` + messages + `Op`/`Mode`/`Tier` enums + `EngineVersion`/`Capability`. Generate Python stubs; keep the REST mirror (camelCase) for dev/health only. (This consumes the RG-P0.S3 rename — the package is already `org.tatrman.nlp.v1`.) *(Done: service + messages added to the shared `nlp.proto` — also generates the Kotlin gRPC stubs the consumer migration needs. Python messages+grpc stubs generated at the service via grpcio-tools into `generated/` (gitignored, `scripts/gen_proto.py` + a conftest bootstrap); shared-proto dropped from ttr-nlp to avoid the `org.tatrman.nlp.v1` namespace collision.)*
- [x] **T3 (test first).** `test_routing.py`: given a config routing table, `EngineRegistry.route(lang, op)` returns the right engine; `cs` → morphodita (TOKENIZE/LEMMATIZE/POS_TAG), stanza (DEP_PARSE), nametag3 (NER); `en` → stanza (+ spaCy NER); unknown `(lang, op)` → the degrade-floor engine (tokenize/fold/langid) with an `RG-NLP-010` info. Assert **no route resolves to an empty model id** (each route names engine + model + version). *(Done: `Route` in `nlp_service/routing.py`; `RG-NLP-*` in `nlp_service/diagnostics.py`.)*
- [x] **T4.** Implement the engine-free front: `NlpEngine` Protocol (`supported_languages`, `supports(lang,op)`, `analyze`) but with **all** concrete engines as *HTTP-adapter clients* to backends (no in-process torch/models); `EngineRegistry` per-op-per-language routing from config; `Orchestrator.analyze` groups ops by routed engine, calls each backend once, merges/dedups/sorts; langid via lingua (the one thing that stays in-front, it's tiny). Strip any model files / torch from the front image. *(Done: Stanza/spaCy now `JsonBackendEngine` HTTP adapters (no `import stanza`/`spacy`); MorphoDiTa/NameTag3 native-protocol HTTP adapters sending explicit model ids. Torch/stanza/spaCy deps stay on the front image until they move to backend images in S2/S3.)*
- [x] **T5 (test first).** `test_capability_matrix.py`: `GetStatus` returns one `Capability{language, op, engine, model_version, tier}` per routed (lang, op); a Lindat-pointed route reports `tier=REMOTE_UNPINNED` (+ `RG-NLP-002`); a self-hosted route reports `SELF_HOSTED_PINNED`. Assert the matrix is built from config + backend `GetStatus`, not hardcoded. *(Done: `EngineRegistry.capability_matrix()`.)*
- [x] **T6.** Implement `GetStatus` (aggregate backend readiness + versions into the matrix) and the S-1 response-stamping: every `Analyze`/`BatchLemmatize` response populates `used[]` from the backend that served each op (engine + model + version); a backend that returns no version → `RG-NLP-003` error (never a silent empty model). *(Done: orchestrator stamps `used[]` per served op + validates via `iter_s1_violations`; gRPC `GetStatus` returns the matrix.)*
- [x] **T7.** All S1 tests green; check the S1 box; commit `RG-P1.S1: nlp.v1 contract, engine-free front, capability matrix`. *(Done: grpc.aio `NlpServicer` (`api/grpc_server.py`) + dual-server `main.py` (gRPC :7271 contract, REST :7270 mirror); in-process gRPC component test `tests/test_grpc_service.py`; **consumer migrated** — `ttr-nlp-mcp` `NlpClient` HTTP→gRPC (mirrors fuzzy-mcp), `application.conf`→:7271, `GrpcTargetConfigSpec`, build deps. Python 54 tests + ruff green; Kotlin consumer build+ktlint green. NOTE: the front's k8s gRPC port + backend workloads are wired in the S2/S3 chart tasks.)*

## S2 · MorphoDiTa + NameTag 3 backends (self-hosted, models baked) {#s2}

Verify: both backend images build; `docker run` each → healthy; the front, pointed at the in-cluster URLs, produces a `cs` parse of the hero sentence offline (no Lindat egress); the Q-10 parity fixtures (RG-P0.S1.T3) pass against the self-hosted backends.

- [x] **T1 (test first — parity fixtures from Q-10).** `test_morphodita_backend.py` + `test_nametag_backend.py`: load the Q-10 parity table (≥ 20 cases) as fixtures; assert the self-hosted backend's tokens/lemmas/POS (MorphoDiTa) and entity spans/labels (NameTag 3) match the recorded expected outputs. These fail until the backends exist. *(Done as `tests/component/` `@pytest.mark.component` (opt-in, `-m component`). The raw Q-10 `parity.json` lives in the unreachable ai-platform harness, so — rather than fabricate golden from idealized seed gold — the tests reproduce Q-10's METHOD: self-hosted ⇄ Lindat over the same pinned model on the hero + 21 seed inputs, `(form,lemma,xpos)` / `(text,cnec)` equality; plus the hero documented-lemma golden `fixtures/parity_hero.json`. **Hero validated live against the pinned model:** lemmas `utržili→utržit/Octavie→Octavia/pražských→pražský/pobočkách→pobočka`, NER `Octavie→op→MISC`.)*
- [x] **T2.** MorphoDiTa backend image: multi-stage Dockerfile that builds `src/rest_server` and **bakes** model `czech-morfflex2.0+pdtc`, digest-pinned; launch with an **explicit model id** (S-1). CPU-only. Expose the tag endpoint; health. *(Done: `backends/morphodita/Dockerfile` — builds `morphodita_server` from ufal/morphodita v1.11.3, bakes the pinned tagger (LINDAT URL verified), `--max_request_size=65536` for BatchLemmatize, explicit model id, tcpSocket/curl health. Targets linux/amd64 — avoids the arm64 SSE patch.)*
- [x] **T3.** NameTag 3 backend image: Dockerfile running `nametag3_server.py` with model `nametag3-czech-cnec2.0-240830` baked + digest-pinned, CPU-only torch; explicit model id on launch; health. Size from Q-10. *(Done: `backends/nametag3/Dockerfile` — ufal/nametag3 v3.2.0, CPU torch, RobeCzech PLM baked, `HF_HUB_OFFLINE=1` (offline), inference-only deps (drops keras/tensorboard/datasets/peft), generous readiness for the ~5 s cold start.)*
- [x] **T4.** Repoint the front's MorphoDiTa + NameTag adapters from `lindat.mff.cuni.cz` to the in-cluster backend URLs — **config only**. Keep Lindat as a selectable `REMOTE_UNPINNED` dev/eval endpoint (env-gated), surfaced in the matrix (`RG-NLP-002`). *(Done: `config.yaml` defaults to in-cluster `SELF_HOSTED_PINNED`; `apply_env_overrides` + `NLP_UFAL_ENDPOINT_MODE=lindat` toggle to Lindat REMOTE_UNPINNED, model stays pinned; `test_config_repoint.py`.)*
- [x] **T5 (test — the CNEC→MISC trap, cross-link E).** `test_cnec_mapping.py`: assert the NameTag adapter's CNEC-label handling. *(Done: `_cnec_to_universal` maps leading class letter — g→LOCATION, p→PERSON, i→ORGANIZATION, t→DATE, **else MISC (never dropped)**; the hero's `Octavie` (`op`, product) → MISC. Corrected two live-found bugs: the adapter must send `output=conll` (not `vertical` — different shape) and the hero's geo phrase is grounded in RG-P3, NOT NER-tagged. Also fixed a latent frozen-dataclass mutation in the multi-token entity path.)*
- [x] **T6.** Wire OTel to both backends and the front; add the backends to the chart/values with the Q-10-derived resource requests + readiness probes. *(Done: front auto-instruments httpx so front→backend calls are traced + context-propagated (the UFAL servers don't self-instrument); chart renders both backends as separate Deployments+Services (tcpSocket probes, Q-10 §5 requests) + the front's gRPC :7271 port/env. `helm template` verified.)*
- [ ] **T7.** All S2 tests green; the hero `cs` parse runs fully self-hosted + offline; check the S2 box; commit `RG-P1.S2: MorphoDiTa + NameTag 3 self-hosted backends`. *(Unit 66 + ruff green; hero output validated against the PINNED model (== self-hosted, Q-10 byte-identical). **Remaining: the literal offline container run** — `docker-compose.offline.yml` + the image builds; MorphoDiTa image building. Once both images run, `just test-py services/ttr-nlp -m component` closes the parity gate.)*

## S3 · Stanza + spaCy backends, batch lemmatize, degrade floor {#s3}

Verify: `pytest services/ttr-nlp/tests/` green including batch + degrade; the front image carries no torch/models; a bulk-lemmatize call sustains the Q-11 cardinality within the Q-10 sizing envelope.

- [ ] **T1 (test first).** `test_batch_lemmatize.py`: `BatchLemmatize{texts[], language}` returns positional `results[]` with `used[]` populated; assert it holds at **both hops** (front → MorphoDiTa backend) — i.e. the front batches to the backend, it does not loop per-string HTTP (the pilot's `nlp.enabled=false` cause). Size the test input to the Q-10.T5 curve.
- [ ] **T2.** Move Stanza (cs+en; tokenize/lemma/POS + cs DEP_PARSE + en NER) and spaCy (en tokenize/NER) out of the front into their own backend images (extend the existing bake pattern — Stanza cs/en + spaCy `en_core_web_md` already have a working baked-image precedent). The front's Stanza/spaCy adapters become HTTP clients like the others. Stanza is on the cs hot path (DEP_PARSE feeds the resolver's span proposal) — set its cold-start budget from Q-10.
- [ ] **T3.** Implement `BatchLemmatize` in the front (fan a batched request to the MorphoDiTa backend; echo model versions) and in the MorphoDiTa backend adapter (batched tag call). This is the throughput driver for fuzzy's RG-P2 lemma axis.
- [ ] **T4 (test first).** `test_degrade_floor.py`: an unsupported language (no morphology/NER backend for it) returns TOKENIZE + fold + DETECT_LANGUAGE only, with `RG-NLP-010` info and the matrix reporting the gap; the response's `used[]` still names what produced the tokens. No 500, no silence.
- [ ] **T5.** Implement the degrade floor in the `Orchestrator`: when a routed (lang, op) has no backend, drop that op to the floor, label it, and continue — consumers read the matrix and branch (the resolver is the first such consumer, RG-P5).
- [ ] **T6.** Confirm the front image is engine-free: assert (a build/CI check) no torch, no model files, no per-engine native deps in the front image; models live only in backend images. Update the chart so front and four backends deploy as separate workloads with independent scaling.
- [ ] **T7.** Full `ttr-nlp` suite green; the engine-free-front check passes; bulk-lemmatize sized; check the S3 box + the RG-P1 review box after the phase-exit `/review`; commit `RG-P1.S3: Stanza/spaCy backends, batch lemmatize, degrade floor`.
