# PL Design — Control Room

> The single dashboard for the **TTR Processing Language (PL)** design effort.
> Open this first every session. It tells us where we are, what's open, and what's decided.
> Companion docs: [`01-design-space-map.md`](./01-design-space-map.md) (the branches to explore), [`02-internal-model-options.md`](./02-internal-model-options.md) (workstream B), [`03-tooling-delivery-options.md`](./03-tooling-delivery-options.md) (workstream G), the brief [`../pl-brief.md`](../pl-brief.md).
>
> **Status:** Framing (Phase 0). Started 2026-06-30.

---

## 0. How we run this

This is a **multi-session, exploration-first design effort.** The explicit rule (Bora's): **diverge before we converge.** We enumerate alternatives broadly and resist committing to a design until the option space has been walked. Claude's job is to *hold the structure*, surface branches we haven't explored, and push back when we narrow too early.

**The cadence — three gears:**

| Gear | What happens | Output |
|---|---|---|
| **Framing** | Set scope, personas, success criteria, the load-bearing forks. | This doc + design-space map populated. |
| **Divergence (deep-dive)** | Pick one workstream. Enumerate *all* reasonable alternatives, with trade-offs, examples from the PoCs/prior art. **No decision.** | A `NN-<workstream>-options.md` doc: the option catalogue. |
| **Convergence** | Only once a workstream's options are well understood: weigh, decide, record. | A decision in the log + (later) an `architecture.md` / `contracts.md`. |

**Deep-dive protocol** (for each Divergence session):
1. State the question the workstream must answer.
2. Enumerate alternatives — aim for ≥3, including the "weird" one.
3. For each: what it buys, what it costs, which prior art/PoC shows it, which other forks it constrains.
4. Capture **open questions** and **cross-links** to other workstreams.
5. End with: are we done diverging here? What would we need to converge later?

**Anti-rush guardrails** (Claude enforces these):
- No internal-model lock-in until ≥2 surface languages have been sketched against candidate models.
- No surface-language syntax bikeshedding until that language's *target persona* and *coverage tier* are named.
- Every "let's just use X" gets a recorded alternative before it becomes a decision.
- "Deferred" is a valid, tracked outcome — see the Parking Lot.

---

## 1. Workstream dashboard

Status legend: ⚪ not started · 🔵 diverging · 🟡 options captured · 🟢 converged/decided · ⏸ parked

| # | Workstream | Status | Core question | Notes |
|---|---|---|---|---|
| **A** | Vision, scope & personas | 🟢 | Who is each surface language *for*, and what does "v1 done" mean? | **Converged 2026-06-30.** See decision log + principles. |
| **B** | Internal model (execution graph) | 🟢 | One graph with data/control/error flow — what are nodes & edges, and how does it relate to Calcite RelNode / Kantheon PlanNode? | **Converged 2026-07-02.** T1–T10 all closed for v1 (incl. T6 manifests, T4 world/variables, leftover sweep). Compiler-phase work items: T8 termination measure, node-fission rule, capability vocabularies. |
| **C** | Surface languages (×5) | 🟢 | Are the 5 languages isomorphic to one graph, or tiered subsets? One deep-dive each, after a cross-cutting "relationship" session. | **C0 converged 2026-07-02** (γ tiered-at-container) → `04-surfaces-options.md`. **C3 (canonical flow-DSL) converged 2026-07-03** → `05-canonical-dsl-options.md`. **C1 (graphical) converged 2026-07-03** → `10-graphical-options.md` (two-level view; skins; binary auto/manual layout; ζ identity; `.ttrl` schema; β edit vocabulary; `ttrp/*`; Display transport; Designer-server convergence-at-migration). **C2 (fragments) converged 2026-07-04** → `11-fragments-options.md` (full decomposition; TTR-SQL workhorse cut; TTR-pandas method-chain; document scope; err-only; untouchable interiors; own grammars). **C4 (NL/TTR-B) converged 2026-07-04** → `12-nl-options.md` (TTR-B = third fragment dialect; verbose expression skin; assist = `assistContext`+`validate` contracts; Q1 resolved; `.ttrb`/`"""ttrb`). **Workstream C complete.** |
| **D** | Data objects / models | 🟢 | How do PL programs reference TTR model objects (table/entity/cube), and which ops apply to which model? | **Converged 2026-07-03** → `06-model-binding-options.md` (γ tiers, v1=db+er; package-derived kinds; relation-joins; world = `schema world`, hosts models; `[pl]` project defaults; declared staging). **MD-sugar session still parked** (D-h). |
| **E** | Transpilation & targets | 🟢 | RelNode-via-Proteus vs direct emit; engine affinity; pandas/Polars/SQL/Kotlin-DF. | **Converged 2026-07-03** → `07-emit-options.md` (Proteus core → modeler lib; concrete artifacts, world-driven PlanNode emit; CTE-per-node; γ dataframe emit, v1 = PG+Polars; early er rewrite + provenance; Q9 + Q8 resolved; Transfer generalized). E-h finishes in F-lite. |
| **F** | Orchestration / scripting layer | 🟢 | The multi-step DAG gap. New service? Extend Theseus? Reuse Pythia's DAG executor? | **F-lite converged 2026-07-03** → `08-orchestration-options.md` (wave-parallel bash emit; FF dropped from v1 — Q10 dissolved; Arrow staging; env-var credentials; fail-fast; JSON manifest + semantic world fingerprint; E-h closed). **F proper (Kantheon orchestrator, events, FF, retries) = v2/post-v1.** |
| **G** | Tooling & delivery | 🟢 | Does PL ride the modeler stack, and where's the JVM-compiler ↔ TS-designer line? | **Converged 2026-07-02.** G-b → WS-LSP + repo-attached Designer server, Kotlin-only; G-a/e/g ratified. Component roster set (`03-tooling-delivery-options.md`). Open item "does TTR's designer converge onto the same server" **closed by C1-f (2026-07-03: converge with the TTR-M `.ttrl` migration arc)**; remaining: Designer-server auth (local v1). |
| **H** | Naming & conventions | 🟢 | Family name; rename Byx; where docs live; versioning. | **Converged 2026-07-03** → `09-naming-options.md`. **Tatrman = the product ("table transformation manager"); TTR-M / TTR-P / TTR-B; `.ttrp`, `.ttr.sql`/`.ttr.py`, family-wide `.ttrl` sidecar; `org.tatrman:ttr-translator`; `TTR_CONN_*`; repo fork → `Collite/tatrman`** (clone w/ history). Leftovers opportunistic (npm scope, bundle-dir name, versioning → consolidation). |
| **Z** | Optimizer | 🟡 | Calcite vs OptaPlanner vs custom; learn from Tatrman's slow brute-force. | **Opened AND grounded 2026-07-07** → `13-optimizer-options.md`. All forks leaned + GI-1…GI-20; **question board empty (ZQ1–ZQ10 closed)**; roadmap Z 1.0→3.0 sketched. To 🟢: 5 work items + decision-log entries. |

---

### Framing inputs (Bora, 2026-06-30)

These are scope commitments stated up front. Not yet "decisions" (no design converged), but they constrain the deep-dives:

- **FI-1 · Start with workstream A** (Vision & scope) before touching the internal model.
- **FI-2 · Multi-engine from v1.** v1 must demonstrate one program executing across ≥2 engines (e.g. SQL + Polars). **Cascade:** this pulls the **orchestration/scripting layer (F)** and **Charon data-movement** *into* v1 — they leave the parking lot. Single-engine-only is rejected as the v1 anchor.

## 2. What we already have (asset inventory)

The landscape is unusually rich — much of the target already exists as prior art. Mapped in detail in the design-space map; summary here so we don't reinvent:

- **Three historical surface-language PoCs** (in `../examples/`):
  - **RAE** (Groovy) — pandas/functional, variable-binding dataflow: `X = filter(Y, {expr})`, `aggr`, `cut`, `createValue`, `materialize`, `append`.
  - **Kyx** (Kotlin DSL) — Alteryx tool-flow: `Input{} + Filter{} + Join(a,b){}`, anchors `.True/.False`, targets Alteryx `.yxmd`.
  - **Byx** (NL-like) — sentence commands: `Load file "...". Select cols a,b. Summarize sum(amt) group by branch.`
- **TTR modeling language** (this repo) — declarative, multi-schema (`db`/`er`/`md`/`binding`/`cnc`), `def <kind> <id> { props }`. Extension hooks: `def procedure`/`def query` (already carry `language` + `sourceText`), `calc` maps + MD catalog. **Big open fork:** is PL a new model *code* or a separate language family?
- **Tatrman repo** — already implements the **exact execution-graph internal model the brief describes**: JGraphT DAG with dataflow + control-flow + error + lineage edge types, multi-engine *affinity* via static cost JSON, transpile to Calcite RelNode → SQL. Its optimizer is a brute-force state-space search → **slow** (combinatorial, no pruning/A*/cardinality). Closest existing prototype to workstream B.
- **Kantheon platform** — the execution substrate:
  - Unit of work = **Calcite PlanNode** (RelNode tree, protobuf); results stream as **Arrow IPC** with a cross-engine schema fingerprint.
  - Pipeline: **Theseus** (orchestrate) → **Proteus** (lang↔RelNode↔SQL) → **Argos** (validate/RLS) → **Kyklop** (dispatch) → **Workers** (Brontes/MSSQL, Steropes/Polars, Arges/PG).
  - **Charon** moves data (Materialize/Stage/Copy/Evict, Arrow, named connections).
  - **Two existing DSL protos** already in Kantheon: **TransDSL** (declarative query) and **DFDSL** ("Pandish", imperative op-chain) — both lower to RelNode. Candidate ancestors for workstreams C/B.
  - **Known gap:** no multi-step orchestration DAG — single-query only. Pythia has a custom DAG-executor PoC. This is workstream F.

---

## 3. Load-bearing forks (decide consciously, not by drift)

These are the questions whose answers cascade. Listed here so they stay visible; explored in the design-space map.

1. ~~**PL-as-TTR-model-code vs PL-as-separate-language-family.**~~ **RESOLVED 2026-06-30 → separate family that references TTR models (option b).** See decision log.
2. **Unified graph vs layered graph.** One graph carrying data + control + error flow together (Tatrman's approach), or a RelNode dataflow core wrapped by a separate orchestration/control layer (closer to Kantheon's split)?
3. **Reuse Kantheon's PlanNode/TransDSL/DFDSL as the internal model, or design fresh.** Adopt vs adapt vs greenfield.
4. **Surface coverage: isomorphic vs tiered.** Do all 5 languages express the full graph (round-trippable), or do some (NL, SQL) cover only a subset?
5. **"Text is canonical" for the graphical language too?** The modeler invariant says text is the source of truth and the designer issues structured edits. Does PL's graphical surface round-trip through text, or can the graph be primary?
6. **Build the orchestrator new vs extend Theseus vs adopt Pythia's DAG executor.**

---

## 3a. Design principles

Stable values that constrain every workstream. Cite by ID in decisions.

- **P1 · Small core, rich edges. (2026-06-30)** Prefer a *minimal* operation/node set; push expressiveness and ergonomics into the surface languages and the transpilers, not into the core operation count. The internal model and transpilers are what we pressure-test; the language stays simple. *(Origin: Bora, A1-bis.)* Constrains B1 (keep node taxonomy lean, RelNode-aligned), C (surfaces are sugar over the small core), E (transpilers carry the complexity).
- **P2 · No miracles. (2026-07-02)** Everything is **explicit, or deterministically derived from project defaults; if it cannot be derived from project defaults, it is an error.** No content-sniffing, no heuristic inference, no silent guessing — anywhere in the toolchain. *(Origin: Bora, confirming the bare-fragment caveats.)* Constrains C (dialect markers, bare-fragment desugaring), D (project/world defaults are the *only* implicit source), T8 (rewrites are deterministic), diagnostics (a miss is an error, not a guess).

## 4. Decision log

> Append-only. Format: `YYYY-MM-DD · [workstream] · Decision · Why · Alternatives rejected`.

- **2026-06-30 · [A2] · PL is a separate language family *within* the Tatrman (TTR) family, distinct from the modeling language, that references TTR models.** · Processing is imperative dataflow with control/error edges — a different beast from TTR's declarative `def {props}` metadata; but it stays in the family and resolves data objects against TTR semantics (mirrors how Proteus↔Ariadne work). · Rejected: (a) processing as a sixth `model` code inside TTR; (c) consuming only resolved-semantics with no family ties.
- **2026-06-30 · [A5] · Hero scenario = the cross-engine sales+accounts case.** Read accounts (SQL DB) + read sales (CSV/file) → join → summarize → branch on filter, with an error path; DB-side work runs in SQL, CSV-side in Polars, Charon moves data between. One program, two engines, one graph. Carried verbatim through every workstream. · Forces FI-2 (multi-engine) into the canonical example.
- **2026-06-30 · [A3] · No writes beyond materialization.** The only "write" primitive is *materialize + call Charon*; no arbitrary sink/DML in v1. · Keeps PL a transform language, not an ETL-sink language.
- **2026-06-30 · [A1] · v1 personas = data engineer + data analyst. Data scientist added later.** · Anchors scope on two users; defers the data-scientist surface affinity.
- **2026-06-30 · [A1-bis] · All 5 surface languages ship in v1 (brief wins over persona-tiering).** · Keeps the internal-model isomorphism test honest — more surfaces = stronger pressure on a surface-agnostic core. Pandas-like and NL-like are forward-investments. · Rejected: tiering surfaces to persona timing (3-surface v1). · **Couples to principle P1 below.**
- **2026-06-30 · [A3-bis] · Also out of v1 scope: streaming/CDC, incremental/refresh, user-defined functions.** · (with A3: no writes beyond materialize+Charon.)
- **2026-06-30 · [A4] · Success criteria for v1: the hero scenario, authored in ≥2 surface languages → compiles to one graph → executes across ≥2 engines on Kantheon workers → produces identical results.** · Bakes FI-2 into "done."

**Workstream A is converged (🟢).** Next: pick the first technical divergence (B internal model vs C0 surface relationship).

### B / internal model — converging (in progress; full options in `02-internal-model-options.md`)

- **2026-06-30 · [B-T1] · Internal model = a NODE SET + REWRITE RULES + per-engine CAPABILITY, not a fixed "elementary vs macro" split.** Transpile step 1 = normalize the graph to an engine's supported node subset by rewriting (T8). "≈ RelNodes" is a revisitable prior. Reinterprets P1: "small core" = whatever subset an engine supports, reached by rewriting. · Rejected: fixed two-tier elementary/macro as the primary frame (it's a special case).
- **2026-06-30 · [B-T2] · Connections use PORTS (term chosen over "anchors").** Semantics live in the port, not the edge. Ports are typed (data-bearing | control-bearing), **named, with a default port** (names matter in text, not in the graphical view). **Multicast yes** (one out-port → many edges); **no implicit union** (a data-in takes one edge; merging is an explicit `Union`). · Aligns with Tatrman ports / Kyx `.True/.False`+default.
- **2026-06-30 · [B-T2] · Routing nodes pinned:** `Filter` (1 out, SQL-style pass-through) · `Branch` (true/false split = Alteryx filter) · `Switch` (multi-out, per-output conditions; non-overlapping+else OR overlapping) · `Join`/`Union` (explicit named multi-in).
- **2026-06-30 · [B-T2] · Control vocabulary = FS (hard, workhorse) + SS (positive co-start) + FF (atomic co-finish). SF dropped. Mutex excluded (→ F).** Parallel-ok = absence of FS, not an edge.
- **2026-06-30 · [B-T2] · Error-flow is node-level, both modes:** signal-only and erroneous-rows. (Erroneous-rows → SQL will be hard; flagged for E/T8.)
- **2026-06-30 · [B-T2] · Port schemas fully static for now** (author-time-checked; reinforces T7).
- **2026-06-30 · [B-T2] · Control edges carry EVENTS — conceptual model only; v1 is static FS/SS/FF, v1 graph is ACYCLIC. Events + loops/iteration are v2+.** Model is designed to accommodate events; machinery deferred.
- **2026-06-30 · [B-T2] · Control constraints are HARD on their effect; the optimizer may rewrite the surrounding graph only while preserving that effect (never drop it).** "Reorderable hint" = parallelism (absence of FS) only. **→ T2 fully closed.**
- **2026-06-30 · [B-T9] · Charon MOVES only** (physical→physical) **and converts formats** (arrow→csv). It does **not** persist and does **not** load into memory. Persistence/"write" is a runtime (engine) op. *(Corrects an earlier wrong assumption that Charon persists.)*
- **2026-06-30 · [B-T9] · Movement/IO ops are DISTINCT nodes:** **Load** (physical→engine memory), **Store** (engine memory→physical; this is the engine "write"), **Transfer** (a Charon call; physical→physical; may convert), **Index**. **Engine-crossing = Store + Transfer + Load.** **Terminology locked: Load/Store** (over Read/Write) — names the memory boundary, frees "write" as the generic engine concept; accepted risk = ETL's "load = write-to-target" collision, held by convention (Load=input, Store=output).
- **2026-06-30 · [B-T9] · Materialize = optimization-only, temporary (temp-table), pass-through; it is a MACRO** that rewrites (T8) into **Store + (Index) + Load joined by FS.** Indexing optional in v1 but the model must be ready for it. · Fork 2 = distinct ops (Materialize not a primitive); Fork 1 = pass-through.
- **2026-06-30 · [B-T9] · Container node** (from Tatrman): groups ops, no processing value; has **ports mapped to internal node ports** (acts as a *function*/encapsulation); **in v1 bears the execution target** (SQL vs Python). **Collapse containers into nodes ⇒ the orchestrator graph** (F); transfers are defined between containers.
- **2026-06-30 · [B-T9 / closes old B2 "unified vs layered"] · No logical/physical split of programs.** One op graph; ops sit on a **physicality spectrum**; before execution, **iteratively rewrite less-physical → more-physical ops until "can this engine process this graph?" holds** (T8 fixpoint). Engine targets live on **containers**, not on the (engine-agnostic) transform ops. · Rejected: a separate logical→physical program lowering (Bora tried it — too many graph layers).
- **2026-06-30 · [B-T10] · PRIMITIVE-vs-MACRO is ENGINE-RELATIVE, not absolute.** One node set; each engine natively supports a subset; a node is "primitive" for engines that run it natively and "macro" for engines that must rewrite it (Branch: primitive in streaming/Alteryx, macro→Filter in SQL). Two rewrite kinds: **authoring sugar** (engine-independent — Select, Calc, HAVING) and **capability lowering** (engine-relative — Branch→Filter, Pivot→CASE). "Supported everywhere" = intersection of engine capabilities, not a privileged tier.
- **2026-07-01 · [G-b] · PL Designer = thin frontend backed by a repo-attached "Designer server"; the compiler/LSP is served over WebSocket (+ stdio for IDEs). NO KMP/JS — PL is Kotlin-only.** The Designer always needs a **model repository**, so requiring a server is acceptable: *"use the IDE, or run a server for the Designer."* The **Designer server** attaches directly to the model repo (no "upload a repo to draw it") and is the backend; the Designer is pure frontend. It **embeds the compiler front-half + the shared metadata component** (same component as G-c). · **Rejected: G-b-i (KMP front-half / backendless static designer)** — the repo-attachment requirement removes the reason to be backendless; also WASM / whole-compiler-to-JS. · **Consequences:** (1) **new component — Designer server** (JVM; repo-attached; serves Designer over WS-LSP + `pl/*`). (2) **Metadata component is JVM, not KMP** (all consumers — Ariadne, PL compiler, Designer server — are JVM). (3) **PL is Kotlin-only: no KMP-JS, no standalone TS parser, no Python target, no cross-target conformance harness** — single parser (biggest G simplification). (4) **One Kotlin PL LSP, transports = stdio (VS Code, IntelliJ) + WS (Designer server)** — restores modeler's *"one LSP across hosts"* invariant (mirrors TTR's stdio + browser-worker, now stdio + WS). (5) The Designer server is **editor infrastructure** (a repo-attached editing backend, like an LSP), **not platform runtime** — modeler's "no runtime coupling to kantheon" invariant holds. · **Open:** does TTR's *own* designer converge onto the same JVM Designer server (embedding `ttr-semantics`) or keep its TS browser-worker LSP?
- **2026-07-01 · [B-T3] · PL internal model = γ: our OWN model (and later our OWN optimizer), delegating the relational islands to Calcite.** PL owns an engine-agnostic superset core (control/error/movement/container/Branch/Switch/Pivot + relational); the **relational island** — Bora's term — lowers to `plan.v1.PlanNode` at the E boundary for SQL-family engines (→ Proteus → Calcite → SQL); Polars/pandas islands emit separately and bypass PlanNode. · **Rejected: α (adopt PlanNode — relational-only, can't carry PL's apparatus); β (superset/embed PlanNode — couples PL's core to Kantheon's `plan` wire version, betrays T9 engine-agnostic core).** · **Consequence: settles T5-a = adapt** (PL owns its `Expression` twin, lowers to `plan.v1.Expression` for SQL islands). Calcite reuse = **two-tier** (PL rewriter for non-relational lowering; Calcite/Proteus for the relational island + SQL emit). Canonical form = **text (G)**; PlanNode is a derived E-boundary interchange artifact.
- **2026-07-01 · [B-T5] · NULL semantics = canonical SQL three-valued logic.** `NULL = NULL` → NULL; unknown propagates (SQL-style). Engines that don't natively match (pandas/Polars) get codegen that *enforces* it. · **Forced by A4** — "identical results across ≥2 engines" cannot hold under engine-defined NULL. This is a hard constraint handed to E, not a preference. · Rejected: engine-defined NULL (breaks A4); a strictness knob (no v1 use case).
- **2026-07-01 · [B-T5] · Expression-level capability misses resolve at NODE granularity — never mid-expression.** When a function inside a value node (Project/Calc/Filter/…) has no rewrite into the container engine's supported function set, the **whole node** is the unit of re-placement: it moves to a capable engine (container reassignment + Store/Transfer/Load, per T9) — the same T8-d node-level split, triggered by an expression miss. The expression normalizer only decides *"can this function be rewritten into engine E's set?"*; a "no" escalates to the node-placement fixpoint (T8). Rewrite order: authoring-sugar expansion → function capability-lowering (where a rewrite exists) → whole-node re-placement (none exists). **Policy: split-with-warning by default, configurable as a project/user preference** (auto-split vs refuse). · Closes **T5-b-fork.** · Rejected: hard-error-only (too rigid); mid-expression engine split (nasty seam). Consequence: the expression layer never reasons about cross-engine seams.
- **2026-06-30 · [B-T10] · Transform node set (v1):** **Project** (primitive; sugar **Select**=select+rename, **Calc**=formula), **Filter**, **Branch**/**Switch** (real nodes, engine-relative), **Join**, **Aggregate** (HAVING=sugar→Aggregate+Filter), **Sort**, **Union**, **Values**, **Limit**, **Pivot** (v1, **static/declared-value typing**; native or CASE per dialect). **Window → v2.** Open: Distinct/Intersect/Except, semi/anti joins, Explode.
- **2026-07-02 · [B-T9/T8 · v1 placement] · v1 placement is AUTHOR-ASSIGNED: the author draws Containers and assigns each its execution target; the compiler re-places nodes only via the T5-b escalation (capability miss → whole-node re-placement, split-with-warning).** No auto-placement pass in v1 — rule-based or cost-based placement is Z/v2. Surfaces (C) must therefore express containers + targets. · Rejected: deterministic auto-placement (more machinery, weaker author control); hybrid gap-filling (implicit magic).
- **2026-07-02 · [G-a/G-e/G-g] · Ratified: G-a same monorepo (new PL packages beside `ttr-*`); G-e text canonical for PL incl. the graphical surface (layout/ports serialize into PL text, v1.1 layout-block precedent); G-g kantheon consumes compiled plans only in v1 (never the PL compiler; revisit β if runtime agent-authoring lands).** · Workstream **G is now fully converged 🟢.**
- **2026-07-02 · [B-T6] · Manifest content model = β: PARAMETERIZED DECLARATIVE ENTRIES.** Node entries carry structured constraints (`Join{types:[…]}`, `Aggregate{functions, distinct}`, `Pivot{native}`); functions referenced by catalogue id (T5-c interface), id-granularity in v1. Rare named-predicate escape hatch allowed. · Rejected: flat sets (too coarse — real misses live below the node kind); pure predicates (opaque, undiffable).
- **2026-07-02 · [B-T6/T4] · The WORLD is a TTR-family document (`def world …`) and a COMPILE TARGET.** The world is a compile-time description of the surroundings; PL compiles *against* it the way a compiler targets an architecture ("compile to x86"). **At runtime, the concrete environment (Kantheon or any other) must verify its COMPATIBILITY with the declared world** — runtime capability advertisement is *verification*, never the compile-time source of truth. Engine-*type* manifests stay a compiler-shipped data artifact (format open).
- **2026-07-02 · [B-T6] · Manifests are SEPARATE from rewrite rules (α).** Manifest = facts about an engine; rewrite rules = compiler knowledge; the normalizer joins them (native? → rewrite exists? → escalate, per T5-b order). Adding an engine = write a manifest, inherit all rewrites. Engine-specific emit stays in E codegen. · Rejected: manifests carrying/referencing rewrites (duplicates compiler knowledge into data).
- **2026-07-02 · [B-T6/T9/F · working stance, explicitly revisitable] · TWO LAYERS: DATA vs EXECUTION.** The **data layer** is where data flows through edges between ops; the **execution (orchestration) layer** is where *tasks* are orchestrated and data moves only via explicit **Transfer** or **Load/Store**. Engine taxonomy follows: **data engines** (process data: SQL, Polars, pandas, …) vs **execution engines** (run tasks/jobs: bash, Airflow-class DAG orchestrators, Kantheon). **Execution engines get capability manifests too** (which control vocabulary — FS/SS/FF — parallelism, retries, transactionality they support). Bora: *"not sure it is a good (and final) split, but we need some distinction — so this is it."* Consistency note: this does NOT reintroduce a program split (T9 one-graph holds) — the execution-layer graph is *derived* by container-collapse, not separately authored (v1; see F).
- **2026-07-02 · [B-T6 · confirms] · (1) Execution-layer graph is DERIVED-ONLY in v1** (container-collapse; directly-authored orchestration programs deferred). **(2) T6-b two-layer manifests confirmed:** engine-**type** manifest (ships with compiler) + environment-**instance** overlay in the world doc (instance `extends` type + deltas, e.g. Postgres extensions). Kind (Storage/data/execution) × layer (type/instance) — orthogonal axes.
- **2026-07-02 · [B-T6/E · invocation capabilities] · Execution-engine manifests declare INVOCATION capabilities:** "can execute bash", "can execute pg script", "can execute python 3.13", "can call REST via curl", … The compiled artifact must know **how each data island is delivered**: the same PG island can be emitted as inline psql scripting *or* as a REST call to Arges — chosen from the (data engine, execution engine) manifest pair. **v1 execution engine = bash** (a bash script invoking pg/python scripts); Airflow/Dagster/Prefect/cloud-native are later targets of the same mechanism. Names the E-boundary concept: **invocation binding** — per-container choice of delivery channel.
- **2026-07-02 · [B-T4 · Q7] · Variables = γ: SUGAR for named edges/instances, with SSA reassignment.** A variable names an out-edge/anonymous instance (a *textual* device for expressing edges — the graphical surface draws wires instead); names survive as labels (round-trip/diff stability). **Reassignment allowed in text** (`X = filter(X,…)`) and **desugared SSA-style** to fresh instances — the same mechanism as table instances (SSA-for-storage), appearing as SSA-for-names. The model stays pure dataflow; no symbol-table/scoping layer. · Rejected: α (no reassignment — makes the pandas-like surface alien); β (real binding construct — a second semantic layer buying nothing v1 needs).
- **2026-07-02 · [B-T4] · Variables name DATA only, never containers.** Containers (functions) get their own `def`-style declaration; no callable-valued variables (no higher-order constructs in v1).
- **2026-07-02 · [B-T4] · Program structure: ONE DOCUMENT = ONE PROGRAM = ONE GRAPH.** Statements are incremental graph construction; containers are the only sub-structure; imports/modules deferred. **`WorkspaceRef` reuse is RUNTIME-only** (compile-time equivalent = table instance in the internal world) — ratifying the earlier decision-shaped stance. **→ T4 closed** (Scan/TableScan-vs-Load reconciliation = drafted in the sweep).
- **2026-07-02 · [B-T5 · sweep] · Aggregate calls = distinct `AggregateCall` arm** (not unified with `FunctionCall`; matches Calcite scalar/agg split, keeps checker context-honest). **Subquery-valued expressions NOT in v1 IR** (semi/anti Join covers them; SQL-like surface may parse + desugar to graph). **Parameters = compile-time only in v1** (declared, defaulted, substituted at compile; no `ParameterRef`/runtime bind — runtime parameterization arrives with events/F).
- **2026-07-02 · [B-T7 · sweep] · Dynamic-schema exception = authored program outputs ONLY** (sinks by definition; nothing consumes them downstream). **Schema sourcing:** modeled objects from TTR models via `ttr-metadata`/world; ad-hoc files require declared schemas (world doc or inline); schema-on-read banned; details → D. **T7-c closed as superseded** (Pivot is static-declared per T10).
- **2026-07-02 · [B-T10 · sweep] · Distinct = authoring sugar → Aggregate** (native DISTINCT emit is E/Calcite's job). **Intersect/Except = real nodes, engine-relative** (native SQL; join-pattern lowering for dataframes). **Semi/anti = Join types** (engine-relative emit). **Explode/Unnest → v2** (needs nested types; v1 type system is flat scalars). pandas-Branch = manifest content, not a model decision. T8 termination-measure + node-fission = named compiler-phase work items.
- **2026-07-02 · 🟢 WORKSTREAM B IS CONVERGED.** All threads T1–T10 closed for v1; remaining B items are implementation work items, not design forks.
- **2026-07-02 · [C0-a/b/c/f/g] · Surface architecture = γ: TIERED AT THE CONTAINER BOUNDARY.** The **flow-DSL is the canonical language** (full coverage: containers, targets, control, movement, layout; the file format). **SQL-like and pandas-like are container-content dialects** (fragments embedded per-container; they express data-flow islands, never program structure). **The container is the mixing unit** (one document mixes dialects per-container). Graphical = view of the whole (G-e). **Prototype order: canonical flow-DSL → graphical → fragments → NL.** **A1-bis resolved re-scoped:** v1 ships 2 full surfaces + 2 fragment dialects + NL (below) — honest to the 5-surface intent, compatible with A4. · Rejected: isomorphic-all (5 full grammars + 5 round-trips); graph-primary (G-e).
- **2026-07-02 · [C0-d] · NL = BOTH: Byx stays as a strict controlled-grammar surface (deterministic, LLM-free — "bypassing LLMs for a simple instruction set has value"), AND a 2026-grade LLM-assisted authoring layer is additional on top** (NL + world + manifests → generated canonical PL, user reviews; also Q1's agent path). · Rejected: LLM-only (loses determinism); strict-only (ignores 2026).
- **2026-07-03 · [C3-a] · Canonical statement form = γ HYBRID: pipeline chains AND assignment, both first-class, freely mixed.** A variable names any edge (Q7-γ taken literally); a chain writes a run of edges; chains are legal in source position; the formatter carries the style rule (chain linear runs, name at fan-out/reassignment/multi-out). · Rejected: α chain-primary (fights Q7 reassignment; names creep back at every branch point); β assignment-only (no flow-shaped syntax; duplicates the TTR-pandas fragment's niche).
- **2026-07-03 · [C3-b] · Connector token = `->`.** One token serves statement chains, program-level port wiring (`acc_prep -> crunch.accounts`), and the explicit edge form; directional; extends to a control-arrow family if ever wanted. Only known collision is lambda syntax, absent from v1 expressions. · Rejected: `|>` (reads wrong as a wiring statement — two tokens needed); `+` (Kyx continuity, but non-directional + arithmetic collision); `.op()` chaining (blurs the canonical/TTR-pandas tier boundary C0-γ draws).
- **2026-07-03 · [C3-a-iii] · Op-argument surface = BOTH: named args canonical; a config block is allowed on wide ops (Aggregate, Pivot, Switch, multi-key Join); formatter picks per op width.** Blocks compose inside chains (`j -> aggregate { … } -> sort(…)`). · Rejected: args-only (Pivot/Switch become one-liner walls); blocks-only (trivial ops get heavy: `filter { amount > 0 }`).
- **2026-07-03 · [C3-e] · Control-edge syntax = KEYWORDS: `b after a` (FS) · `a with b` (SS) · `a finishes with b` (FF); usable top-level or grouped in an optional `control {}` block.** Cross-container data edges already imply FS at collapse; explicit control edges are for non-data deps only. · Rejected: arrow family `-FS->` (three new tokens for a rare construct); mandatory control-block (forbids stating a dep next to its container).
- **2026-07-03 · [C3-f] · TWO reserved error ports: `node.err` (signal, control-shaped) + `node.rejects` (erroneous rows, data-shaped).** Mode is textually visible; unconnected ⇒ fail-fast default (elision is P2-legal). · Rejected: one `err` with per-node mode (invisible mode; reader must know the node kind).
- **2026-07-03 · [E-a = α′, extended] · The PROTEUS TRANSLATION CORE MOVES TO MODELER as a published library (`org.tatrman` group, name → H); kantheon's Proteus consumes it as a thin wrapper — exactly the metadata/Ariadne pattern (Q6).** The PL compiler embeds it (offline, in-process: island → RelNode → payload). **Artifacts carry CONCRETE payloads; PlanNode emission is WORLD-DRIVEN:** when a container's execution target is Kantheon (per world/manifest), the invocation binding delivers `plan.v1` PlanNodes; otherwise dialect SQL. No standing export contract — the world decides per compile (T6 mechanism, γ folded into α′). **Cross-repo consequence:** a kantheon-side extraction arc (Proteus core → modeler lib), to be planned in the kantheon repo. · Rejected: α private unparse (extraction becomes refactor); γ unconditional plan export (contract with zero consumers); β runtime translation (ships half a compiler; unreviewable); consuming Proteus code *from* kantheon (inverted/circular coupling).
- **2026-07-03 · [E-c = γ + engine set] · Dataframe emit = STRAIGHT-LINE script + GENERATED INLINE PRELUDE (only the enforcement helpers the program needs — artifacts dependency-free, SSA names carried, mirrors canonical text).** **v1 data engines = {Postgres, Polars}; pandas engine → v1.x** (TTR-pandas *dialect* still ships v1 — dialect ≠ engine, C0). NULL/3VL enforcement lives in the prelude patterns, proven by the Q9 harness. · Rejected: runtime-harness lib (versioned dependency in every artifact); pure straight-line (enforcement repeated at every use site).
- **2026-07-03 · [Q9 · RESOLVED — A4 equivalence procedure] · Seven points confirmed: (1) Arrow schema fingerprint; (2) rows as MULTISET, order-sensitive only under terminal Sort (canonical-sort both sides to compare); (3) NULLS LAST emitted in every Sort; (4) decimal exact, float64 within DECLARED tolerance (default exact — no silent epsilon, P2); (5) UTC µs datetimes, truncation emitted at boundaries; (6) binary/UTF-8 collation, locale sort = declared v1 non-goal; (7) `pl-conform` harness over Arrow IPC exports** — doubles as the emit regression suite and the standalone-vs-Kantheon drift guard.
- **2026-07-03 · [Q8 · RESOLVED — v1 RLS stance] · TRUSTED PRINCIPAL + TRIPWIRE: artifacts run under the world's named-connection credentials; PL propagates no policy. World storage may declare `rls: true` → Store/Transfer OUT emits a compile WARNING; project knob `[pl] rls-egress = warn|error`. Row-filter propagation explicitly v2 (Argos territory).** · Rejected: refusal-by-default (fights the hero's own egress); stance-without-tripwire (invisible escape).
- **2026-07-03 · 🟢 WORKSTREAM E IS CONVERGED.** E-a…E-g closed; E-h (artifact bundle shape) deliberately finishes in F-lite; leftovers (v1 dialect = PG-only follows from the engine set; golden-test placement; world fingerprint in artifact; `pl explain`) in `07` §Open.
- **2026-07-03 · [E-b] · SQL emit shape = CTE-PER-NODE (preserved-shape applied): each island node becomes a named CTE; SSA variable names survive as CTE names; trivial islands emit flat SELECT (deterministic rule).** The SQL mirrors the authored graph — debuggable, diffable, lineage-readable. · Rejected: nested-subquery default unparse (lossy, names lost); optimize-at-emit (Z's job, violates preserved-shape).
- **2026-07-03 · [E-d = γ] · er rewrite is EARLY (T8 sugar stratum) WITH MANDATORY PROVENANCE: er refs desugar to db refs right after resolution; nodes/expressions carry origin (`CUST_TYPE ← erp.er.customer.customerType`); diagnostics, graphical view, and lineage render through provenance.** One tier through normalize/fission/placement/emit. · Rejected: early-no-provenance (analyst writes customerType, gets told about CUST_TYPE); late-at-emit (every emitter + checker handles two tiers; manifests match on physical names).
- **2026-07-03 · [E-g · AMENDS B-T9] · Transfer GENERALIZED: an abstract movement node whose delivery is an invocation binding from the (storage, executor) manifest pair — Charon is its Kantheon-world binding; native tools (psql \copy, file copy) bind it in bash-land.** B-T9's intent preserved (Transfer moves, never persists); its "Transfer = a Charon call" wording amended consciously. · Rejected: Charon-only (hard runtime dependency for standalone artifacts; no Charon CLI exists).
- **2026-07-03 · [D-b] · World names ride the SAME QNAME + IMPORT mechanism as model names (world defs are TTR defs), PLUS POSITION-TYPING: each syntactic position checks its kind (`target` = engine, `load`/`store` = storage or model object, `schema:` = schema).** Uniform naming, position-precise errors. · Rejected: flat unified namespace (late collisions, vague errors); sigil-marked world refs (noise in programs and fragments).
- **2026-07-03 · [D-c = δ] · Ad-hoc schemas live in BOTH homes, P2-ordered: world-declared (shared/stable, beside the storage) and program-declared (named def or inline literal).** Precedence: inline > named-in-program > world-declared; same-level conflict = error. Schema-on-read stays banned (T7). · Rejected: world-only (kills the quick-CSV analyst flow); program-only (drift across programs).
- **2026-07-03 · [D-d shape = α] · The world is a NEW TTR SCHEMA KIND (`schema world` beside db/er/md/binding/cnc): `def world` containing `def engine` / `def executor` / `def storage` (instance `extends` type manifest; storage `hosts:` model packages per D-d-i).** One toolchain; world files are model files; Designer-viewable for free. Storage grouping renamed off Tatrman's "TableContainer" (working: `namespace`, → H). · Rejected: standalone PL-side grammar (two grammars in one family).
- **2026-07-03 · [D-g · confirmed] · The PL compiler EMBEDS the modeler metadata component (Q6's D-side settled): model repo + world doc read DIRECTLY via paths in project defaults — offline, no service at compile time (T6 compile-target stance).** Kantheon workspace metadata = a population source for worlds only; Ariadne remains the runtime wrapper. The component boundary (what's in `ttr-metadata` vs the PL front-half) stays a G work item.
- **2026-07-03 · 🟢 WORKSTREAM D IS CONVERGED (core).** D-a…D-g closed; the MD-sugar session stays parked as its own session (D-h frames it); leftovers (world-extends-world, auth/named connections, world-doc placement, schema type vocabulary) in `06` §Open.
- **2026-07-03 · [D-a = γ] · ALL model tiers are referenceable by design; v1 ships db + er; md reserved for the MD-sugar session.** Ref-kind machinery (checker, fragments, graphical) carries both kinds from v1 — the two-tier test is the point. The hero scenario gains an er-flavored variant. · Rejected: physical-only (shrinks A2; forecloses MD's seat); logical-first (unmodeled tables need the hatch anyway); world-only (contradicts A2).
- **2026-07-03 · [D-a sub-1] · Ref kind is PACKAGE-DERIVED: full TTR qnames are unambiguous by construction (model package/schema path distinguishes tiers); short forms are import sugar (D-b-ii `import erp.er.*`).** No new syntax; ambiguity becomes a visible, local import choice. · Rejected: resolution-derived bare names (adding a model def can break programs); keyword-marked (`table`/`entity` noise, markers inside fragments).
- **2026-07-03 · [D-a sub-2] · v1 er semantics = NAMES + RELATION-JOINS: attribute→column mapping via er2db binding, plus `on: relation <name>` (modeled join conditions referenced explicitly — P2-clean).** Full logical layer (cardinality checks etc.) → v2. · Rejected: names-only (misses the analyst payoff that justifies the tier); full layer (v2-shaped machinery).
- **2026-07-03 · [D-d-i · model-to-world attachment] · The WORLD HOSTS MODELS: a world storage declares which model packages it hosts (`def storage erp_db { …, hosts: [erp] }`).** The world is the one environment truth; the same model attaches differently in dev/prod worlds — that variance is what worlds are for. · Rejected: project-defaults mapping (splits environment truth across files; world stops being self-describing); a `db2world` binding kind (model files would name deployment environments — layering violation).
- **2026-07-03 · [D-e-α] · Project defaults live in the PROJECT MANIFEST (`modeler.toml`-equivalent, a `[pl]` table).** Keys: world ref, bare-fragment target + shell, T5-b split policy, display default, (staging default if not world-declared). Precedence: document pin (`uses world`) > project defaults > nothing (P2 — no user dotfiles in the chain). · Rejected: TTR-family defaults doc (grammar for key-value config; second project file kind); inside-the-world (wrong layer — "which world" can't live in a world).
- **2026-07-03 · [D-f] · Staging = DECLARED default + FEASIBILITY check + explicit OVERRIDE.** One world storage marked `staging: true`; synthesis verifies both sides reach it via the world's read/write/move relations (else compile error); per-edge/container `via <storage>` override. Fully deterministic (P2). · Rejected: pure per-pair resolution (tie-break is a miracle); always-explicit `via` (re-verboses what C3-d-iv bought).
- **2026-07-03 · [Q11 · Display — semantics resolved] · `Display` = sink-only leaf node; dynamic schema allowed (the T7 output exception); multiple per program, named (`display` anonymous when single, `display(name)` otherwise); project default: a bare program's final result → display.** Transport (Arrow stream / Designer server / `pl/*` method) explicitly deferred → G/E. Q11's semantics half is closed.
- **2026-07-03 · [C3-g-ii · bare-fragment marker] · Extension is the normal dialect marker (e.g. `report.ttrsql`; names → H); a magic first-line comment (`-- pl: dialect=sql` / `# pl: dialect=pandas`) overrides and serves generic extensions.** Both explicit + deterministic (P2); the comment form keeps files valid for foreign tools. · Rejected: extension-only (forces renames on arrival); comment-only (marker invisible in listings).
- **2026-07-03 · [C3-h · world ref] · World comes from PROJECT DEFAULTS; a program MAY carry `uses world "…"` to pin/override.** Bare fragments need the project path anyway — one mechanism serves both file kinds (P2). · Rejected: always-in-document (two mechanisms).
- **2026-07-03 · [C3-a-iv · composition rules baseline confirmed] · (1) precedence `=` < `->` < call; (2) chains legal in source position (`join(left: load(f) -> filter(…), …)`), formatter discourages; (3) expression scope = input columns ONLY — variables never resolvable inside op expressions (no correlation); (4) multi-in ops qualify columns by port name (`left.x`, `right.y`).** Grammar details iterate in prototyping without reopening the fork.
- **2026-07-03 · [C3-h · layout, AMENDS G-e] · Layout lives in a per-document VIEW-STATE SIDECAR (same name, different suffix; suffix → H), not in the program file.** Scope = presentation/view state generally (positions now; collapsed containers, annotations, edge routing later). **G-e amended:** "layout serializes into PL text" now means canonical, versioned PL-family text *in the sibling file* — not the same file. Rationale: (1) PL's graphical surface is co-primary — in-file coordinates churn every program diff; (2) **bare-fragment programs force it** — their source text is never rewritten (C0), so their derived-container layout has nowhere else to live; (3) writer isolation — `pl/setLayout` rewrites the sidecar wholesale, no WorkspaceEdit surgery in program text. Not a D4 revert: D4 killed the *project-global* aggregate; "layout travels with the document" is preserved by the same-name pair. Costs accepted: pair integrity (LSP orphan/missing diagnostics + atomic renames → G work item); family divergence from TTR's in-file block (same block grammar, different hosting; TTR may migrate later). · Rejected: in-file layout block (diff churn; no bare-fragment answer); per-container blocks (coordinates sprinkled through code).
- **2026-07-03 · [C3-g] · Fragment embedding = TTR's TAGGED_BLOCK_LITERAL (`"""sql` … `"""`), TAG-ONLY: the fence tag IS the dialect marker; no `as <dialect>` header.** One embedding mechanism across the family (same token + tag-peeling as TTR `sourceText`); nests cleanly when PL is pasted into markdown. · Rejected: backticks (reverse markdown-nesting collision; second mechanism beside TTR's); `<<< >>>` heredoc (`>>>` = Python REPL prompt — pasted sessions terminate the block); braces+mode-switch (brace-counting breaks on pandas dicts); header+tag (redundant).
- **2026-07-03 · [C3-c] · Multi-input ops take NAMED inputs ONLY: `join(left: accounts, right: sales, on: …)`.** Always explicit which port — port semantics visible in text (B-T2 "semantics live in the port"); P2-flavored explicitness. · Rejected: positional (order silently carries left/right meaning); both-forms (two ways to write the same edge). · Open sub-point: n-ary homogeneous ops (Union) — `union(in1: …, in2: …)` vs a list form; settle with the formatter rules.
- **2026-07-03 · [C3-d-iii] · Containers are CLOSED functions: bodies reference only their own declared ports; all cross-container wiring lives at program level** (`acc_prep -> crunch.accounts`). Matches T9 container-as-function; graphical view maps 1:1. · Rejected: lexical reach (implicit synthesized ports); both-forms (unneeded second way in).
- **2026-07-03 · [C3-d-iv] · Movement is SYNTHESIZED: a cross-engine data edge lowers to Store+Transfer+Load as a T8 capability rewrite; staging location comes from project/world defaults (→ D: "default staging area").** Explicit Store/Transfer/Load remain available for control (staging choice, Index). Deterministic — P2-compatible; exactly what container-to-container wires in the graphical surface need. · Rejected: always-explicit (verbose, blocks plain graphical wiring); defer-to-later (movement is the hero scenario's spine).
- **2026-07-03 · [F-a = β] · Bash executor emit = WAVE PARALLELISM: topological waves, `&` + pid-checked `wait` per wave; FS = wave ordering, SS = same-wave co-launch; per-island log files.** This choice *is* bash's executor-type manifest content (declares FS + SS). · Rejected: strictly-sequential (makes SS dead vocabulary in all of v1 — bash is the only executor); scheduler-loop (artifact becomes a runtime, fights reviewable-text); emit-a-Makefile (re-imports a dependency after E-c; resume declined in F-e).
- **2026-07-03 · [F-b · Q10 DISSOLVED — AMENDS B-T2 v1 scope] · FF IS DROPPED FROM v1. v1 control vocabulary = FS + SS only.** FF stays in the conceptual model and returns in v2 with F proper's orchestrator (where staging+swap / compensation become real designs); `finishes with` stays reserved in the C3 grammar; a v1 program using FF = ordinary T6 capability compile error. · Rejected for v1: same-transactional-domain restriction (real but machinery for vocabulary nothing demands); staging+swap (near-atomic, storage-shaped prerequisites); compensation (saga = v2 orchestrator work); co-visibility redefinition (guts FF's meaning — plain data edges nearly subsume it).
- **2026-07-03 · [F-c] · v1 invocation-binding table ratified: pg×bash = `psql -v ON_ERROR_STOP=1 --no-psqlrc -f`; polars×bash = `python3` (interpreter + required packages declared in the executor-type manifest — E-c's "dependency-free" = no PL harness lib, not no polars); display×bash = headless file drop `out/<name>.<fmt>` + printed notice.** **F-c-i = β: Arrow IPC at EVERY staging boundary** — the staging format IS Q9's fingerprint format (runtime and `pl-conform` agree by construction); transfers emit as generated Python (ADBC/connectorx). **F-c-ii = α: credentials via `PL_CONN_<NAME>` env-var convention**; artifact secret-free; pre-flight fails exit-2 on missing. · Rejected: CSV staging (re-opens the type-fidelity war Q9 closed); per-pair format choice (defer fast paths); tool-native config (two mechanisms day one).
- **2026-07-03 · [F-d] · Failure semantics: fail-fast baseline** — `set -euo pipefail`, ON_ERROR_STOP, **`wait -n` early wave abort** (first failure kills remaining siblings), per-island logs + run.sh failure summary, exit contract 0 ok / 1 island failure / 2 pre-flight failure. **F-d-i = α: cross-container `err` (signal) = compile error in v1** — fail-fast is the only orchestration-level failure behavior; `rejects` unaffected (data-shaped, crosses engines as normal synthesized movement — the hero's error path lives there). On-failure islands → F proper. · Rejected: on-failure islands now (β), absorb markers (γ — try/catch semantics F proper should own).
- **2026-07-03 · [F-e = α] · No retries, no resume in v1: rerun = the whole artifact; `run.sh` wipes its own staging/out dirs at start** (idempotent restart, no stale-staging miracles). Retries/resume = executor-manifest-declared F-proper/v2 mechanisms. · Rejected: sentinel resume (stale-staging correctness questions P2 hates); per-island retries (orchestrator-shaped).
- **2026-07-03 · [F-f · closes E-h] · Artifact bundle ratified: directory of reviewable text** — `run.sh` + `manifest.json` + `islands/` + `transfers/` + `schemas/` + `plans/` (Kantheon worlds only); `logs/`/`staging/`/`out/` runtime-created; **sha256 per file in the manifest; no single-file variant in v1** (naming → H). **F-f-i = γ: run manifest = JSON** (machine record; the reviewable narrative is run.sh + islands). **F-f-ii = β: world fingerprint = SEMANTIC** — hash of the resolved world model + world qname in clear; **artifact RECORDS, capable invoker VERIFIES** (T6's split; bash pre-flight stays env/connection-shaped); per-island narrowing layerable in v2. · Rejected: TTR-family manifest doc (machine-output-only grammar kind); TOML (neither bash- nor tool-friendly); text-hash fingerprint (comment reflow "changes the world").
- **2026-07-03 · 🟢 F-LITE IS CONVERGED. The v1-blocking design set is COMPLETE (A · B · G · C0 · C3 · D · E · F-lite).** F proper (Kantheon orchestrator, events, FF, on-failure islands, retries/resume, runtime params) = v2/post-v1 design. Remaining sessions are non-blocking: H naming sweep (overdue), C1 graphical, C2 fragments, C4 NL, MD-sugar, then architecture/contracts consolidation → implementation planning.
- **2026-07-03 · [H-1 · THE NAME] · The product/family is TATRMAN ("table transformation manager", TTR): modeling = TTR-M, processing = TTR-P, NL surface = TTR-B (ex-Byx); TTR-SQL / TTR-pandas unchanged.** Prose convention: Tatrman = product/repo · TTR = family · TTR-x = languages. "PL" retires as scaffolding shorthand. The backronym now covers both halves: tables (M) + transformation (P). · Rejected: Tok / TTR Flow / TPL (Claude's catalogue — systematic family naming beat a new word-name).
- **2026-07-03 · [H-1-bis · REPO FORK] · modeler forks to `Collite/tatrman` (`~/Dev/collite/tatrman`) — CLONE WITH FULL HISTORY; old `Collite/modeler` freezes (maintenance) until consumers re-point, then archives.** The ai-platform→kantheon precedent. **Consequence sweep (bites silently):** kantheon re-points Maven URL + repo-scoped `gpr.*` PAT + CLAUDE.md §7.3 (companion to the queued Proteus-extraction arc); first publish from the new repo precedes the re-point; `~/Dev/tatrman` PoC repo renamed (`tatrman-poc`) to free the name; `@modeler/*` npm scope → `@tatrman/*` opportunistically; docs folder → `docs/ttr-p/`.
- **2026-07-03 · [H-2 · EXTENSIONS] · `.ttrp` (TTR-P programs; freed by F-f's manifest.json) · fragments = DOUBLE EXTENSION `report.ttr.sql` / `prep.ttr.py` (free foreign-editor highlighting; comment override still serves generic names) · `.ttrb` reserved for TTR-B · bundle = `<program>.bundle/` (proposed; `.ttrb` taken) · `.ttrm`/`.ttrg` stand.**
- **2026-07-03 · [H-2c · ONE LAYOUT SCHEME FAMILY-WIDE — extends C3-h, amends TTR-M v1.1 layout hosting] · Single view-state grammar + single extension `.ttrl`, per-document sidecar for BOTH languages: `x.ttrp`+`x.ttrl`, `x.ttrg`+`x.ttrl`, `report.ttr.sql`+`report.ttrl`.** TTR-M migrates off the v1.1 in-file `layout` block — exactly the branch C3-h left open ("TTR may migrate later"). Not a D4 revert (per-document pairing, not a project aggregate; `.ttrl` reclaimed with new semantics). One pair-integrity toolset serves both. **TTR-M-side amendment to be recorded in modeler/TTR docs post-fork.**
- **2026-07-03 · [H-3] · Translation lib = `org.tatrman:ttr-translator`** (sibling row: ttr-parser/writer/semantics; Kotlin root `org.tatrman.translator.*`) · **storage-grouping keyword = `namespace`** (confirmed) · **env prefix = `TTR_CONN_<NAME>`** (replaces `PL_CONN_`).
- **2026-07-03 · 🟢 WORKSTREAM H IS CONVERGED.** Leftovers opportunistic (npm scope, bundle-dir ratification, versioning → consolidation). Remaining sessions: C1 graphical, C2 fragments, C4 NL (TTR-B), MD-sugar, then consolidation. **Operational next step: execute the fork.**
- **2026-07-03 · [C1-a] · Viewing model = β: TWO-LEVEL — orchestration view + drill-into-container.** Top level = the container-collapsed graph (exactly the derived execution-layer graph, T9/B-T6) **plus program-level leaves** (movement, store, display) — the author sees what F-lite emits as waves; drill-in = container-as-function; nesting recurses; edit locality matches C3 (program wiring top, island edits inside). Semantic zoom = v2 rendering enhancement over the same model, not foreclosed. · Rejected: flat op-graph (container demoted to decoration; doesn't scale); text-primary + graph minimap (contradicts C0-γ).
- **2026-07-03 · [C1-b] · Rendering = SKINS: a pluggable presentation layer over the one graph.** Built-in set v1 (C1-b-i; authoring format deferred): "Alteryx/KNIME" (icons per node type, data edges prominent), "Enso" (text-forward nodes: description, else code), per-skin edge-orientation conventions (e.g. data L→R, control T↓). **Skin is per-canvas** (C1-b-iii), recorded in `.ttrl`; **TTR-M gets skins too** (field is family-generic). Fragment drill-ins render as **read-only derived sub-graphs** in every skin (γ generalized); positions survive skin switch, author owns the result (C1-b-ii). · Rejected: opaque-only code containers (kills the Designer for bare-fragment programs); graphically editable fragments with unparse-back (rewrites authored fragment text — violates C0).
- **2026-07-03 · [C1-b · layout] · LAYOUT MODE IS BINARY per canvas: deterministic AUTO until the user first rearranges, then fully MANUAL — no hybrid in v1.** First drag snapshots auto positions into `.ttrl`; "reset to auto" discards; auto mode persists nothing (P2: deterministic re-derivation beats stored state). **Derived canvases (fragment sub-graphs) are AUTO-ONLY** (C1-b-iv — unstable node identity).
- **2026-07-03 · [C1-c-i] · View-state node identity = ζ: SSA-QUALIFIED NAME KEYS (`crunch/sales#2`, anonymous `crunch/sums~1`) + maintained pair + DETERMINISTIC ORPHANING.** Scope shrink first: durable identity is needed ONLY by manual-canvas `.ttrl` entries — structured edits address nodes by source range (per document version). Discipline: LSP rename/structured edits rewrite the sidecar atomically (the G pair-integrity work item); if a name's SSA/chain length changed, ALL its entries orphan → auto-layout + visible diagnostic — never mis-attach, never guess (P2). Coherent with Q7-γ ("names survive as labels") and E-b (SSA names → CTE names): one naming story across text, emit, canvas, view-state. **Explicitly revisitable in practice; δ (explicit ids in text, the Alteryx/Enso way) = named fallback.** · Rejected: statement-index paths (line-number keying); content hashes (key changes when the node itself is edited); reconciliation matching (heuristic position jumps — P2-flavored miracle).
- **2026-07-03 · [C1-c-ii/iii] · `.ttrl` = SHARED TRUTH (versioned/committed) in v1; grammar = TTR-M-HOSTED (α): `ttr-parser` parses `.ttrl`, the v1.1 layout-block grammar promoted to document body.** Inventory: `ttrl <version>` header (target doc implicit by name pairing) + per-canvas blocks {canvas key (TTR-P: program/container path · TTR-M: qnames), skin, mode auto|manual, nodes (manual only, ζ keys), collapsed}. **Viewport (zoom/pan) DROPPED — ephemeral** (committed-viewport churn is what C3-h fought; check skin subsumes TTR-M's displayMode); **edge routing reserved, not v1.** The TTR-M migration (v1.1 docs §15) now has its concrete target schema. · Rejected: fresh `.g4` (third grammar for a config-shaped format); JSON/TOML (user-visible versioned text — F-f-i's reasoning applies doubly).
- **2026-07-03 · [C1-d] · Editing: scope = β MINIMAL AUTHORING VOCABULARY (the bar: the hero scenario buildable on canvas) — add/remove op, wire/unwire (cross-container = just a wire, C3-d-iv), create container + assign target/dialect, bind ports, rename variable, args/expressions via a textual property panel (T5-e), control edges drawable; the rest → text.** Text placement = **FORMATTER-OWNED** (deterministic; same edit ⇒ same text). Concurrency = **LSP document versioning** (stale edits rejected, Designer replays). **δ = INTERNAL-ONLY** — formatter-emitted γ-hybrid leaves no consumer for a writeable node/edge form (**closes the C3 leftover**). Methods namespace = **`ttrp/*`** (getGraph, applyGraphEdit, getLayout/setLayout — sidecar wholesale, getWorld, transpile, run); `modeler/*`→`ttrm/*` rename folds into the C1-f arc. · Rejected: full edit parity (delays everything); read-only v1 (dilutes C0-g's "2 full surfaces" + A4).
- **2026-07-03 · [C1-e · Q11 FULLY CLOSED] · Display transport = γ: `ttrp/run` shells out to the bundle exactly as a terminal would; display sinks land as ARROW IPC FILES in `out/` (the F-c drop, Arrow = the Designer-facing format = Q9's fingerprint format); the Designer watches `out/` and renders at completion.** One execution path — Designer-run and terminal-run indistinguishable; streaming (β) becomes a v2 additive upgrade, not a transport redo. Designer-server "run" = IDE-run-button territory; G-b's no-platform-runtime invariant holds. · Rejected: no interactive path (Designer can't show results); WS streaming now (v2-shaped machinery).
- **2026-07-03 · [C1-f · closes the G open item] · Designer-server convergence = γ: v1 server is TTR-P-ONLY; TTR-M converges WITH its `.ttrl` migration — one arc, later.** The sidecar/ζ/skin/pair-integrity code is written once, Kotlin-side; TTR-M adopts server + sidecar together; nothing TS-side is built to be discarded. · Rejected: converge now (TTR-M rework on the TTR-P critical path); two backends permanently (view-state machinery duplicated + drifting — the worst steady state).
- **2026-07-03 · 🟢 WORKSTREAM C1 (graphical surface) IS CONVERGED.** Leftover design points (canvas diagnostics, port affordances, provenance display, preview UI, skin roster) → `10-graphical-options.md` §Leftovers, fold into consolidation. Remaining sessions: C2 fragments (thin), C4 NL (TTR-B), MD-sugar, consolidation.
- **2026-07-04 · [C2-a = β] · Fragments DECOMPOSE FULLY into the standard node set** — clause-wise for TTR-SQL (FROM/JOIN→Join · WHERE→Filter · GROUP BY/HAVING→Aggregate+Filter · SELECT list→Project/Calc · DISTINCT→Aggregate sugar · ORDER BY→Sort · LIMIT→Limit; CTE = named sub-chain, **CTE name = SSA label — E-b's CTE-per-node emit in reverse**), statement-wise SSA for TTR-pandas. One graph vocabulary regardless of authoring surface; anything unmappable is a parse/check-time reject with a named diagnostic (P2). Fragments are **leaf content** — no containers inside (closes the C0 nesting open). · Rejected: opaque island node (violates T5-e, kills drill-in + provenance); dialect-flavored node kinds (second vocabulary, anti-P1).
- **2026-07-04 · [C2-b = α] · TTR-SQL = the WORKHORSE CUT: one query expression per fragment (`WITH` + final `SELECT`), full clause set, set-ops, `VALUES`, `LIMIT`; `EXISTS`/`IN (subquery)` desugar to semi/anti Join — the ONLY subquery forms.** Rejected forms are parse-time errors with named diagnostics: DML/DDL, vendor syntax (`NOLOCK`, `TOP`, `::`), scalar/correlated subqueries, window functions, procedural SQL, multi-statement. PIVOT authoring stays canonical-DSL-only in v1. **C2-b-ii: SQL's own syntax carries port assignment** (JOIN position = left/right; C3-c named-only is canonical-text law, not fragment law). **C2-b-iii = β: `SELECT *` allowed, expanded statically at compile** (deterministic under static schemas — P2 demands determinism, not verbosity; authored text keeps the `*`). · Rejected: minimal core (CTE-less SQL unwriteable); maximal generic (PIVOT/GROUPING SETS/LATERAL drag dialect-divergent semantics); `*` ban (buys little, costs paste-in friction).
- **2026-07-04 · [C2-c = β] · TTR-pandas = METHOD-CHAIN over the PL op vocabulary ("dataframe-shaped TTR-P", DFDSL-descended):** `sales.filter(amount > 0 and customer is not null).select(…)`; PL expressions bare (no boolean masks, no `pl.col`); SSA reassignment per Q7-γ; Python-*looking* for `.ttr.py` highlighting (H-2), not Python-parseable. The only shape whose expressions are *grammar*, not *API*. **C2-c-i: single default-out v1 for BOTH dialects** (final SELECT / last value = default out port; multi-out is what canonical containers are for — the graduation boundary). **C2-c-ii:** statement forms = assignment + final chain; Python control flow, lambdas, `.apply`, IO-beyond-`load()` = parse-time rejects. · Rejected: literal pandas subset (masks/`notna` can't lift to PL trees — T5-e breaks structurally; NaN→NULL semantics silently replaced — the dialect lies); Polars API subset (`pl.col` ceremony defeats the expression grammar; couples the dialect to one engine's API — dialect ≠ engine inverted).
- **2026-07-04 · [C2-d = α] · DOCUMENT SCOPE FLOWS INTO fragments:** resolution order in-ports > document imports > full qnames; same-level ambiguity = error. er refs + `on: relation` work inside fragments identically to canonical text (early rewrite + provenance — the D-a two-tier test runs through fragment-land). Expression scope = input columns only; document variables never reach fragment expressions (C3-a-iv-3 holds). Bare fragments: project defaults = implicit prelude (`default-imports` key → consolidation). · Rejected: hermetic fragments (qname noise in the friendly surface); per-fragment comment-imports (second mechanism, per-dialect divergence).
- **2026-07-04 · [C2-e = α] · Fragment containers expose `err` ONLY in v1; `rejects` stays canonical-land.** The signal port comes free (island fails ⇒ `err`; unconnected ⇒ fail-fast, F-d); no rejects-producing dialect syntax — B-T2 already flags erroneous-rows-in-SQL as unsolved; no grammar for a stream nothing fills. Reject-tap syntax waits for a producer semantics (E/T8 work item; v1.x at earliest). Graduation pressure toward canonical text accepted as correct. · Rejected: dialect taps (`WITH REJECTS`/`.rejects()` — invented syntax in "generic SQL", breaks single-result); header boundary-binding (leaks fragment internals through the closed-container membrane).
- **2026-07-04 · [C2-f = α] · Formatter NEVER touches fragment interiors — embedded or bare.** Formats canonical text around tagged blocks (fences included), never a byte inside; bare-fragment files never formatted (extends C0's source-text guarantee). Fragment trivia preserved verbatim — the fragment's `sourceText` is canonical, its parse is derived. · Rejected: formatter-owned interiors (two pretty-printers; rewrites authored text — collides with the bare-fragment guarantee); opt-in flag (same machinery, default-off).
- **2026-07-04 · [C2-g = α] · Fragment parsers = OWN ANTLR GRAMMARS (`TTRSql.g4`, `TTRPandas.g4`), same antlr-ng/Kotlin toolchain, sized to the subsets.** Rejects are *grammar* rejects with named diagnostics (P2 precision); byte-precise spans for provenance/hover (E-d, C1-b). Calcite stays at the emit boundary where E put it. · Rejected: Calcite SqlParser/Babel front-half (subset control inverts to a blocklist — anti-P2; coarse positions; asymmetric — TTR-pandas gets no such gift); hand-rolled descent (ANTLR is the family toolchain).
- **2026-07-04 · 🟢 WORKSTREAM C2 (fragment dialects) IS CONVERGED.** Leftover sub-points (LIMIT/OFFSET inclusion, keyword-table sharing, method-name roster, `default-imports` key, trivia→canvas hover, reject-diagnostic tables) → `11-fragments-options.md` §Leftovers, fold into consolidation. Remaining sessions: C4 NL (TTR-B), MD-sugar, consolidation.
- **2026-07-04 · [C4-a = α] · TTR-B (the strict controlled grammar, Byx evolved) is the THIRD FRAGMENT DIALECT** — container content + bare-program kind, exactly like TTR-SQL/TTR-pandas; **the C2 regime inherits wholesale** (sentence-wise decomposition to the node set; document scope; err-only; single default-out; formatter-untouchable interiors; own ANTLR grammar). Byx's statement set is island-shaped by birth. Interactive command mode (REPL/chat/voice over the same grammar) = v2 layer. · Rejected: full surface (C0-a-α's cost returns for the surface least suited to structural precision); command-language-only (kills the stored deterministic program text C0-d kept).
- **2026-07-04 · [C4-b] · Statement set = the Byx roster evolved, FULL BREADTH:** verb synonymy kept (`Keep/Take/Select…`); anaphora `that/this/it` + implicit = previous result (grammar-resolved, P2-clean); `as <name>`/`call it <name>` = SSA variable binding (Q7-γ); Sort/Limit/Combine added; Store/Display sentences in bare programs desugar to program-level leaves (C0 wrapper sugar). Out-of-roster sentences = named-diagnostic rejects; the diagnostic table doubles as the assist layer's repair vocabulary. · Rejected: trimmed one-verb-per-op (a stiff DSL wearing NL clothes).
- **2026-07-04 · [C4-c = β] · TTR-B expressions = a VERBOSE SKIN over the one PL expression grammar, canonical spellings also accepted.** Closed synonym table (`is more than`→`>`, `is empty`→`IS NULL`, `is one of`→`IN`, …); same trees, precedence, catalogue ids, 3VL; mixing allowed; author's text stays as written (C2-f). **Never NLP, never fuzzy — it's grammar** (T5-e holds; the lift table is closed and documented). · Rejected: strict-pin verbatim (guts the surface's identity); verbose-only (rejecting `>` is user-hostile purity).
- **2026-07-04 · [C4-d-i = α+γ] · The LLM-assist layer emits CANONICAL TTR-P; cursor-scoped insertion emits the pointed-at container's dialect** (TTR-SQL/TTR-pandas/TTR-B alike). The host declares the insertion target — never a heuristic (P2). · Rejected: assist-targets-TTR-B (caps assist at island expressiveness; the strict parser catches syntax hallucinations but the compiler catches semantic ones regardless).
- **2026-07-04 · [C4-d-ii = γ] · The LLM call lives OUTSIDE the toolchain. Tatrman ships two deterministic contracts: `ttrp/assistContext`** (world + engine manifests + model schemas + in-scope names + grammar/diagnostic spec, prompt-ready) **+ `ttrp/validate`** (parse/typecheck/capability-check → named diagnostics). Any host — VS Code ext, Designer, Claude, kantheon agent — runs generate → validate → repair → review. No model dependency, no secrets in editor infrastructure (G-b invariant); one contract pair serves human assist AND agent authoring. · Rejected: editor-side-only (each host reinvents context+validation); Designer-server proxy (an LSP with an API key).
- **2026-07-04 · [C4-d-iii = β] · No mandatory provenance on generated text; the git diff IS the review artifact.** Non-negotiable under P2: generated text is never applied silently — it arrives as a pre-validated proposed edit (parses + typechecks + capabilities pass, or it isn't presented). Optional knob `[pl] assist-provenance = none|comment`. · Rejected: mandatory generated-by header (churn; decays into a lie after the first manual edit).
- **2026-07-04 · [C4-e · Q1 RESOLVED] · The agent is a FIRST-CLASS AUTHOR of canonical TTR-P via the same `assistContext`+`validate` contracts** — no agent-special surface, no TTR-B detour (an LLM gains nothing from the controlled grammar; TTR-B is for humans avoiding LLMs). Kantheon agents consume **published toolchain artifacts** (§7.3 Maven pattern), never a running Tatrman service — **G-g softens at exactly this one seam** (agents may author source, not just consume compiled plans). Eval corpus (NL request → expected graph shape) = `pl-conform`-adjacent → consolidation.
- **2026-07-04 · [C4-f] · `.ttrb` CONFIRMED (bare files, single extension); TTR-B is embeddable, fence tag `"""ttrb`** (Bora: the explicit long tag over `"""b`; the short-tag pattern is not a rule). TTR-B is engine-agnostic — a sentence-island is legal in any container; the container carries the target (unlike TTR-SQL's SQL-shaped idiom, equally retargetable).
- **2026-07-04 · 🟢 WORKSTREAM C4 (NL / TTR-B) IS CONVERGED — and with it ALL of workstream C.** Leftovers (comment syntax, ref-word roster, localization stance, context-bundle format, eval-corpus home, method naming) → `12-nl-options.md` §Leftovers, fold into consolidation. Remaining sessions: MD-sugar (D-h), consolidation.
- **2026-07-04 · [D-h · DEFERRED] · The MD-sugar session is DEFERRED past v1 (Bora): md usage is out of v1 scope anyway (D-a ships db + er), and the design has already paid for the deferral** — package-derived ref kinds (D-a sub-1) make md refs a later *package path*, not a syntax change; no C2/C4 decision forecloses them. Consolidation's only duty: keep the reserved seat visible in contracts (ref-kind vocabulary extensible; `erp.md.*` resolution slot named, undesigned). MD-sugar returns as a v1.x/v2 design session alongside the md engine/emit story. · Not a cancellation — D-h keeps its seat in the parking lot.
- **2026-07-04 · [CONSOLIDATION SWEEP S1–S25 — batch-ratified] · All remaining micro-decisions closed in one pass:** **naming/tooling** — S1 bundle dir `<program>.bundle/` ratified · S2 CLI binary = **`ttrp`** (`build/run/explain/conform`) · S3 conformance harness = **`ttrp-conform`** (avoids colliding with the grammar cross-target harness) · S4 `ttrp explain` + LSP `ttrp/explain` · S5 project-manifest table = **`[ttrp]`** · S6 versioning: TTR-P spec version = integer via the grammar-master process; artifacts semver per PUBLISHING.md; bundle manifest records `ttrpVersion` + toolchain version · S7 npm scope rename → v1 plan Phase 0 checkbox · S8 assist methods = **`ttrp/authoringContext` + `ttrp/validate`**. **Canonical grammar** — S9 `=` is the one equality, context-separated; `==` = named-diagnostic reject EXCEPT accepted in TTR-pandas as closed dialect synonym · S10 reserved ports `in,out,err,rejects,true,false,else`, lowercase · S11 Union = list form `union(a,b,c)`, internal `in1..inN`, no named form · S12 **no `program` header** — filename-derived (P2) · S13 `materialize` confirmed non-surface · S14 source `load`/terminal `store` authored, engine-crossing movement synthesized-only. **Fragments** — S15 LIMIT/OFFSET both in TTR-SQL, **require ordered input else compile error** (A4/Q9-forced) · S16 one shared keyword/operator table · S17 TTR-pandas methods = full-word node names (`aggregate` not `agg`; abbreviations = named-diagnostic rejects) · S18 `[ttrp] default-imports` key added — the bare-fragment implicit prelude. **TTR-B** — S19 comments `#` (each dialect keeps native lexis; generic override `# ttr: dialect=b`) · S20 English-only v1. **World/D** — S21 world-extends-world not in v1 · S22 world docs live in the model repo, `[ttrp] world = <qname>` · S23 D-c schemas reuse TTR db-schema attribute types verbatim. **Tooling** — S24 Designer server v1 = loopback-only, no auth, single user · S25 lean: vendor `plan.v1` proto into ttr-translator at the Proteus-extraction arc (final call in that arc's kantheon-side planning). · Stale §Open sections in older catalogues = doc hygiene, not decisions; decision log remains ground truth.
- **2026-07-06 · [E-a/S25 · extraction arc planned — S25 FINAL CALL] · The Proteus-extraction arc is planned (docs home: `docs/ttr-translator/` here + `docs/implementation/v1/ttr-translator/` in kantheon — supersedes the earlier "plan it in the kantheon repo" stance) and S25 resolves to OWNERSHIP TRANSFER, not vendoring:** tatrman becomes the canonical owner of `plan.v1` (+ `transdsl.v1`/`dfdsl.v1`), published as `org.tatrman:ttr-plan-proto` (Maven + Python wheel), FQCNs unchanged; `org.tatrman:ttr-translator` = the whole query-translator lib (incl. TransDSL/DFDSL codecs), root package `org.tatrman.translator.*`, lockstep tag `kotlin-translator/v*`, first release 0.1.0. `context.proto` transfers too (separability check negative: same package, imports plan+parameters, translator compiles against `PipelineContext`). Guardrails: demand-driven language coverage (no TS/JS artifact w/o consumer); kantheon authors plan.v1 evolution via PRs + prompt lockstep releases. **TTR-P P3 gate = tatrman Phase A (publish), kantheon's switch (Phase B) off the critical path.** Decisions TR-1…TR-8 in `docs/ttr-translator/architecture/architecture.md`. · Rejected: relocated vendored copy w/ bytes-at-boundary (two sources of truth); same-package copy + Gradle exclude (silent duplicate-class failure mode); S25's original "lean vendoring" (the TTR.g4 canonical-source model applies to wire formats exactly as to grammars).
- **2026-07-02 · [C0 · bare-fragment programs] · A pure SQL or pure pandas document IS a valid PL program** — document-level sugar: the compiler synthesizes the wrapper (container + target + shell) from **project/world defaults**. Three commitments: **(a)** dialect is declared by an explicit cheap marker (file extension or one-line header), never content-sniffed; **(b)** the fragment is **TTR-SQL / TTR-pandas** (Bora's naming, confirmed explicitly): a *generic* SQL, not T-SQL — **pasting `NOLOCK` is an error** (T5-e pin: expressions lift to PL trees, catalogue functions, canonical NULL) — which is exactly what keeps it retargetable; **(c)** defaults convention: project/world supplies the container target and the output sink for the final result. **The source text stays as written** (the wrapper is desugaring, not a rewrite); the graphical view shows the derived container. No lineage edge/port kind in the model (deliberately dropping Tatrman's authored lineage edges); column-level lineage is computed from nodes + expression trees on demand (a compiler query, later a `pl/*` LSP method). · Rejected: first-class lineage edges (redundant with derivable information, another thing to keep consistent).

---

## 5. Parking lot / deferred

| Item | Why parked | Revisit when |
|---|---|---|
| Optimizer (workstream Z) | Brief defers to v2; separate design effort. | **Un-parked 2026-07-07** — divergence run and grounded (`13-optimizer-options.md`); implementation stays post-v1 (Z 1.0 = TTR-P v2). |
| ~~Orchestration layer (F)~~ | **Un-parked by FI-2** — multi-engine v1 forces it in. | Now in v1 scope. |
| Window functions | Deferred by Bora (B-T10). | v2. |
| Own execution engine (Kotlin+Arrow / Rust+Arrow) | Brief: "later, not now." v1 transpiles only. | Post-v1, if engine-affinity forces it. |
| MD syntactic-sugar deep design (D-h) | Brief: "special session"; **deferred past v1 (2026-07-04)** — md out of v1 scope (D-a), ref-kind machinery already extensible. | v1.x/v2, with the md engine/emit story. |
| Error-flow semantics depth | Needs internal model first. | During/after workstream B. |

---

## 6. Open questions (rolling)

Cross-session scratchpad of unresolved threads. Promote to a workstream deep-dive or the decision log as they mature.

- ~~Q1 (A): Is the AI agent (Pythia/Golem) a *first-class user* of one of the 5 surfaces, or only a consumer of the compiled graph?~~ — **RESOLVED 2026-07-04 (C4-e): first-class author of canonical TTR-P via `assistContext`+`validate`; agents consume published artifacts, never a running service.** See decision log.
- Q2 (B/E): Is the internal model literally Kantheon's `PlanNode`, a superset of it, or a sibling that lowers *to* it?
- Q3 (C): Which single surface language do we prototype first to pressure-test the internal model?
- Q4 (A): What's the v1 "hero scenario" — one end-to-end example we can carry through every workstream as the running case study?
- Q5 (G, Bora 2026-07-01): **The PL *compiler* moves to the JVM** (Kotlin + antlr4 + Calcite, the Tatrman stack). Modeler is TS for *frontend* reasons (designer + IDE ext). Which parts go JVM? Working answer: PL grammar (Kotlin-primary `.g4`), parser, semantics/resolver, normalizer/rewriter (T8), relational-island→PlanNode + Calcite (γ; JVM-mandatory), optimizer (later), codegen — all Kotlin. Stays TS: `designer` (React), `vscode-ext` shim. Bridge fork: **KMP** (front-half — parse/semantics/world-binding/author-time checking + graph model — compiles to JVM *and* JS for the browser designer; Calcite back-half stays JVM-only) **vs** a JVM LSP served to the designer over WebSocket. Assets that de-risk it: TTR already has a Kotlin ANTLR parser (`packages/kotlin/*`) + Tatrman proves Calcite-on-JVM. → open a **G** deep-dive.
- Q6 (G/D, Bora 2026-07-01): **Metadata service inside Modeler; Ariadne becomes a wrapper.** Modeler already *owns* TTR (grammar/parser/semantics) and Ariadne already consumes `org.tatrman:ttr-*` (kantheon §7.3). Claude's key distinction: modeler ships a **metadata *component*** (Kotlin, ideally KMP — pure: TTR model → model-graph + queries), Ariadne *is* the **service** wrapping it (bearer/RLS/session/gRPC/capabilities-mcp stay in Ariadne). Preserves modeler's "editor-tooling, no runtime" invariant (library ≠ service). Same component fuels the **designer** graphs (via KMP-JS or the LSP) and populates the PL **compile-time world** (Q5/T4) from TTR models. Coupling is *not new*: it's another artifact in the already-permanent `Collite/modeler` Maven group. → open a **G/D** deep-dive.
- Q7 (B/T4): Are surface variables (RAE `X = filter(Y,…)`) just sugar for named instances/edges, or a real binding construct? (↔ C.)
- ~~Q8 (E/F, from review 260702): Security/RLS across engines~~ — **RESOLVED 2026-07-03** (trusted principal + `rls: true` tripwire + `rls-egress` knob; propagation v2). See decision log.
- ~~Q9 (E, from review 260702): A4 "identical results" equivalence procedure~~ — **RESOLVED 2026-07-03** (seven-point procedure; `pl-conform` harness). See decision log.
- ~~Q10 (F, from review 260702): **FF cross-engine implementability**~~ — **DISSOLVED 2026-07-03: FF dropped from v1** (v1 control vocabulary = FS + SS; FF returns with F proper in v2). See decision log.
- ~~(C0 agenda, from review 260702): re-open A1-bis~~ — **resolved 2026-07-02** (all-5 re-scoped under C0-γ).
- ~~Q11 (B-T10/C/D, Bora 2026-07-02): **`Display` node** semantics~~ — **FULLY RESOLVED**: semantics 2026-07-03 (sink-only leaf, dynamic schema per T7 exception, named/multiple, bare-program default); **transport 2026-07-03 (C1-e = γ)**: `ttrp/run` shells out to the bundle, display sinks = Arrow IPC files in `out/`, Designer watches + renders; streaming = v2. See decision log.

---

## 7. Session index

| Date | Phase | Focus | Output doc |
|---|---|---|---|
| 2026-06-30 | Framing | Kickoff: process, dashboard, design-space map. | This doc + `01-design-space-map.md` |
| 2026-06-30 | Framing | **Workstream A converged** — A2 (separate TTR-family lang), A5 (cross-engine hero scenario), A3/A3-bis (scope-out), A1/A1-bis (personas + all 5 surfaces v1), A4 (success criteria), principle P1. | Decision log |
| 2026-06-30 | Divergence | **Workstream B — structural heart** — ports/edges/control/error (T2), events-v2, movement+materialize+container (T9), transform node set + engine-relative primitive/macro (T10), node-set/rewrite/normalization framing (T1/T8), Load/Store terminology. **Agreed resume order: T5 expressions → T3 Calcite/PlanNode → T4 → T6.** | `02-internal-model-options.md`, `next-steps-260701.md` |
| 2026-07-01 | Divergence | **Workstream G opened** — tooling & delivery. Grounded in the existing polyglot monorepo (TS pnpm + Kotlin Gradle `ttr-{parser,semantics,writer}` + Python + `intellij-plugin/`). Threads G-a…G-g; **crux = G-b** (JVM↔TS boundary: KMP front-half vs JVM-LSP-over-WS). Leans captured. | `03-tooling-delivery-options.md` |
| 2026-07-01 | Convergence + Divergence | **B-T3 decided → γ** (own model + own optimizer later; delegate relational islands to Calcite; T5-a thereby = adapt). Opened **T4**: PL-is-a-compiler framing → compile-time **world** (reuse Tatrman `world` as ideas), **table-instance = SSA-for-storage**, compile-time-vs-runtime reference split. New **G** directions logged: JVM compiler move (Q5) + metadata-component-in-modeler / Ariadne-as-wrapper (Q6). | `02-internal-model-options.md` §T3/§T4, Q5–Q7 |
| 2026-07-01 | Divergence | **Workstream B — T5 expression sublanguage** — option catalogue captured (🟡). Grounded in `plan.v1.Expression` (DFDSL reuses it verbatim), the typed MD calc catalog, and PoC string-vs-tree prior art. Threads: T5-a IR (adopt/adapt/fresh — **decide with T3**), T5-b engine-relative functions (unify with T8; missing-fn = hard error?), T5-c catalogue (two behind one interface; absorption → D), T5-d typing/coercion/**NULL canonical, forced by A4**, T5-e structured-canonical + one PL-expr grammar, never pass-through SQL. Provisional leans noted; **no decisions**. Open: T5-b-fork. | `02-internal-model-options.md` §T5 |
| 2026-07-02 | Review | **Independent progress review** — decisions confirmed/pushed-back; undiscovered option space flagged (RLS Q8, A4-equivalence Q9, FF Q10, F undesigned, A1-bis oversized); adjacent-session findings (qname v4.0, Calcite preserved-shape principle). | `review-260702.md` |
| 2026-07-02 | Convergence | **Ratifications + T6 + T4 + B-sweep → WORKSTREAM B CONVERGED 🟢, G CONVERGED 🟢.** v1 placement author-assigned; G-a/e/g ratified; lineage derived; T6 (β manifests, world-as-TTR-doc + runtime-compatibility, manifests⊥rewrites, **data/execution taxonomy: execution engines are engines**, invocation bindings, v1=bash); T4 (Q7=γ variables, data-only, one-doc-one-graph, WorkspaceRef runtime-only); sweep (AggregateCall arm, no subquery exprs, compile-time params, output-only dynamic schema, Distinct=sugar, Intersect/Except=nodes, semi/anti=Join types, Explode→v2). **C0 opened.** | Decision log + `02` §T6/§T4 + `04-surfaces-options.md` |
| 2026-07-02 | Convergence | **C0 CONVERGED 🟢** — γ tiered-at-container; flow-DSL canonical; TTR-SQL/TTR-pandas fragments; Byx strict + LLM assist; bare-fragment programs; A1-bis re-scoped; prototype order. **Principle P2 "No miracles."** Q11 (`Display` node) opened. **Next session resumes at C3** (canonical grammar). | `04-surfaces-options.md`, `next-steps-260702b.md` |
| 2026-07-03 | Divergence + Convergence | **E CONVERGED** — Proteus translation core moves to modeler as a published lib (metadata/Ariadne pattern); artifacts carry concrete payloads, PlanNode emission world-driven (Kantheon target ⇒ plans, else SQL); CTE-per-node preserved-shape SQL (SSA names → CTE names); dataframe emit = straight-line + generated prelude, v1 engines {PG, Polars}; er rewrite early + provenance; **Q9 resolved** (7-point procedure, `pl-conform`); **Q8 resolved** (trusted principal + tripwire); **Transfer generalized (amends B-T9)**. E-h bundle → F-lite. | `07-emit-options.md`, `next-steps-260703c.md` |
| 2026-07-03 | Divergence + Convergence | **D CONVERGED (core)** — γ ref tiers (v1 = db + er, md reserved); package-derived ref kinds + TTR imports; er depth = names + relation-joins; world hosts models (`hosts:`); world = new TTR `schema world`; `[pl]` project-manifest defaults; staging = declared + feasibility + `via` override; schemas both-homes P2-ordered; qnames + position-typing; D-g resolution picture confirmed (compiler embeds metadata component, offline). MD-sugar session stays parked. | `06-model-binding-options.md`, `next-steps-260703b.md` |
| 2026-07-03 | Convergence | **H CONVERGED 🟢 — the naming sweep.** **Tatrman** = the product ("table transformation manager"); **TTR-M / TTR-P / TTR-B**; `.ttrp` programs, `.ttr.sql`/`.ttr.py` fragments, **family-wide `.ttrl` view-state sidecar** (TTR-M migrates too — extends C3-h); `org.tatrman:ttr-translator`; `TTR_CONN_*`; `namespace` confirmed. **Repo fork decided: modeler → `Collite/tatrman`, clone with full history** (kantheon re-point consequences swept). | `09-naming-options.md`, `next-steps-260703e.md` |
| 2026-07-03 | Divergence + Convergence | **F-LITE CONVERGED 🟢 — the v1-blocking design set is COMPLETE.** Wave-parallel bash emit (F-a-β; bash manifest = FS+SS); **FF dropped from v1 (Q10 dissolved, amends B-T2 v1 scope)**; invocation-binding table (psql / python3 / headless display); Arrow IPC staging everywhere (= Q9 format); `PL_CONN_*` env-var credentials; fail-fast semantics (wait -n wave abort, exit 0/1/2, cross-container err = compile error); no retries/resume; bundle ratified (JSON manifest, semantic world fingerprint, sha256; **E-h closed**). | `08-orchestration-options.md`, `next-steps-260703d.md` |
| 2026-07-03 | Divergence + Convergence | **C3 (canonical flow-DSL) CONVERGED** — hero scenario rendered in 4 candidate styles; γ hybrid statement form; `->` connector; named-only multi-in; both arg forms (block for wide ops); keyword control (`after`/`with`/`finishes with`); `err`+`rejects` error ports; `"""sql` tagged-block fragments (TTR TAGGED_BLOCK_LITERAL reused, tag = dialect marker); closed containers + program-level wiring; synthesized movement; **layout/view-state SIDECAR (amends G-e)**; bare marker = extension + comment override; world = project default + `uses world` pin; composition-rules baseline; **Q11 Display semantics resolved** (transport → G/E). Kyx lineage table recorded. | `05-canonical-dsl-options.md`, `next-steps-260703.md` |
| 2026-07-03 | Divergence + Convergence | **C1 (graphical surface) CONVERGED 🟢** — β two-level viewing model (orchestration view = derived execution graph + program leaves; drill-in); **SKINS** (built-in set v1; per-canvas; family-wide incl. TTR-M; positions survive switch); **binary auto/manual layout** (auto persists nothing; derived canvases auto-only); **ζ node identity** (SSA-name keys + atomic pair rewrite + deterministic orphaning; δ ids-in-text = fallback); `.ttrl` = shared truth, TTR-M-hosted grammar, inventory ratified (viewport dropped, bendPoints reserved); **β minimal edit vocabulary** + formatter-owned placement + LSP versioning + δ internal-only (C3 leftover closed); **`ttrp/*` methods**; **C1-e Display transport γ (Q11 fully closed)**; **C1-f Designer-server convergence-at-migration (G open item closed)**. | `10-graphical-options.md`, `next-steps-260703f.md` |
| 2026-07-04 | Divergence + Convergence | **C2 (fragment dialects) CONVERGED 🟢** — hero island rendered in 3 TTR-pandas candidate shapes; **β full decomposition** (fragments parse to the standard node set; CTE name = SSA label, E-b's inverse; fragments = leaf content); **TTR-SQL workhorse cut** (WITH+SELECT, one query expression; EXISTS/IN→semi/anti; named-diagnostic reject list; SQL position carries ports; `SELECT *` expands statically); **TTR-pandas = method-chain over the op vocabulary** ("dataframe-shaped TTR-P"; literal-pandas + Polars-API rejected on T5-e); single default-out both dialects; **document scope flows in** (in-ports > imports > qnames; er refs work in fragment-land); **err-only v1** (rejects stays canonical-land); **formatter never touches interiors**; **own ANTLR grammars** (Calcite stays at emit). | `11-fragments-options.md`, `next-steps-260704.md` |
| 2026-07-04 | Divergence + Convergence | **C4 (NL / TTR-B) CONVERGED 🟢 — workstream C COMPLETE.** Hero island rendered in evolved-Byx; **TTR-B = third fragment dialect** (C2 regime inherits wholesale; command mode = v2 layer); **full Byx roster + anaphora** (that/it grammar-resolved; as-name = SSA; Sort/Limit/Combine added; named-diagnostic rejects = assist repair vocabulary); **verbose expression skin** over the one grammar (closed synonym table, canonical accepted, never NLP); **assist emits canonical TTR-P** (cursor-scoped → container's dialect, host declares target); **LLM call outside the toolchain — `ttrp/assistContext` + `ttrp/validate` contracts**; no mandatory provenance (git diff = review artifact; optional knob); **Q1 resolved** (agent = first-class author via the same contracts; published artifacts, never a running service; G-g softens at this one seam); **`.ttrb` confirmed, fence `"""ttrb`**. | `12-nl-options.md`, `next-steps-260704b.md` |
| 2026-07-04 | Convergence + Consolidation | **MD-SUGAR DEFERRED (D-h → v1.x/v2) + CONSOLIDATION EXECUTED.** Sweep S1–S25 batch-ratified (CLI `ttrp`; `ttrp-conform`; `[ttrp]` manifest table + `default-imports`; `ttrp/authoringContext`; `=` one equality (`==` = TTR-pandas synonym); Union list form; no program header; ordered-LIMIT rule; full-word pandas methods; TTR-B `#` comments, English-only; world in model repo; Designer server loopback/no-auth; plan.v1 vendored lean; bundle dir + versioning ratified). **The three artefacts written:** `../architecture/architecture.md` + `../architecture/contracts.md` + `../implementation/v1/plan.md` (8 phases, P0 prep → P7 TTR-B+assist; A4 exit at P5; Proteus-extraction arc = external P3 gate; TTR-M migration explicitly post-v1). Naming notes added to 07/08/12. **The TTR-P design effort is COMPLETE; next = plan review → per-stage task lists → kantheon-side extraction-arc planning.** | `architecture/`, `implementation/v1/plan.md`, `next-steps-260704c.md` |
| 2026-07-07 | Divergence + Grounding | **Workstream Z (optimizer) OPENED and GROUNDED — v2 design-ahead; question board emptied same day.** Reframe: Z optimizes **boundary economics** (placement, movement, materialization; never competes with engine optimizers — GI-1); containers get a role taxonomy (grouping/hint/directive + `together` cohesion; surface = `target`/`prefer`/`together`, GI-2/16); scale ceiling <100 nodes with explicit un-optimizable diagnostic (GI-3); budget = Designer user-setting (GI-4); machinery = **two-phase rewrite→exact solve (CP-SAT-class), min-cut fast path, HEFT seed — own code, "steal the ideas, develop ourselves"** (GI-8; Calcite/Wayang rejected as machinery); objective = makespan Z 1.0, **profiles** + dimensioned-resource-vector decide-now for multi-objective Z 2.0, **δ-memo-as-generator Z 3.0** (GI-9/10/12/14); stats = world metadata via **metadata server, two backings (served/repo-read), optimizer never calls instances, needs stats CONTRACT only** (GI-11/13/18); **D-g refinement confirmed verbatim: correctness-compile offline, optimize pass may consume served metadata, bundle records stats snapshot fingerprint** (GI-15); per-build, plan caching deferred (GI-17); ladder: no-metadata = hard error, lost-mid-session = stale warning (GI-19); hints = `prefer` alone (GI-20). Long-term register: elastic worlds. **To 🟢:** 5 work items (cost-shape sketch, stats contract, hero min-cut hand-run, B-T9 amendment draft, world-stats schema) + decision-log entries. | `13-optimizer-options.md` |
| 2026-07-03 | Operational | **Fork-sweep execution (H-1-bis consequences, repo-side).** TTR-M `.ttrl` amendment (H-2c) recorded in the v1.1 docs (`v1.1-packages-and-graphs.md` §15 addendum + contracts changelog v8 + CLAUDE.md pointer; design amendment only — in-file layout block stays shipped until migration, content schema → C1). Publish plumbing re-pointed `Collite/modeler` → `Collite/tatrman` (Kotlin `build.gradle.kts` ×3 publish URLs/POMs, `PUBLISHING.md`, `publish-python.yml`, `pyproject.toml`, vscode-ext `package.json`) — unblocks the first `kotlin/v*` publish from the new repo. Still external: first publish, kantheon re-point (gated on publish; folds into Proteus-extraction arc), `~/Dev/tatrman` → `tatrman-poc` rename, old-modeler freeze README, PyPI trusted-publisher re-point. | Session record (this row) |
