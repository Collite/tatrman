# Moved → `collite-gh/project/tatrman/features/ttr-p/design/rejects/`

The **rejects design effort** (erroneous-rows producer semantics — control room, design-space
map, option docs `02`–`05`, `design.md`, `detailed-design.md`) lives in the `project` repo as of
2026-07-15, per the post-restructure convention (see this repo's `CLAUDE.md` banner and
`project/README.md`):

- Design + decisions: `project/tatrman/features/ttr-p/design/rejects/`
- Implementation plan + task lists + STATUS: `project/tatrman/features/ttr-p/implementation/rejects/`

The copies that briefly lived here were moved to
`_to_delete/docs-features-ttr-p-design/rejects/` (recoverable, pending final cleanup — do not
edit them; they are stale by construction).
