# T3 — TS semantics validation (@tatrman/semantics)

Pre-flight: T2 green. The vocabulary + diagnostic codes are normative in `README.md` §Vocabulary — implement THAT table, not memory.

- [x] **T3.1 — Vocabulary module + tests first.** New `packages/semantics/src/semantics-block/vocabulary.ts`: exported const tables `ENTITY_KINDS`, `ATTRIBUTE_ROLES` (role → {extraKeys, typeConstraint}), `KIND_COMPLETENESS` — mirroring README exactly, plus `SEMANTICS_VOCABULARY_VERSION = 1` (the cross-repo sync key with ai-platform proto enums, md-catalog `MD_CATALOG_VERSION` precedent). Write `vocabulary.test.ts` asserting the exact role/kind rosters (drift guard — a failing test is the reminder that proto + this table move together).
- [x] **T3.2 — Validator tests first.** `packages/semantics/src/__tests__/semantics-block-validation.test.ts`: one test per diagnostic code TTR-SEM-200…211 (fixtures inline): unknown key; unknown role (with nearest-match suggestion `role: event_dat` → "did you mean event_date"); unknown kind; duplicate key (from `duplicateProperties`); kind on attribute + role on entity; `role: amount` on `type: varchar` (205); `period_table` missing `period_end` (206); two `event_date` on one entity (207); `period:` → nonexistent entity and → entity without `kind: period_table` (208); `currency:` → missing sibling and → sibling without `role: currency_code` (209); `geo_lat` without `geo_lon` and `geo_point` + pair (210); `valid_from` without `valid_to` (211). Plus one green-path test on the full `59-semantics.ttrm` fixture — zero diagnostics.
- [x] **T3.3 — Typed result model.** `packages/semantics/src/semantics-block/model.ts`: `ResolvedSemantics` — `{ kind?: EntityKind }` on entity/table symbols; `{ role: AttributeRole, refs: { period?: SymbolRef, currency?: SymbolRef }, params: { codeFormat?: string } }` on attribute/column symbols. Extend `SymbolEntry` with `semantics?: ResolvedSemantics`.
- [x] **T3.4 — Validator implementation.** `semantics-block/validator.ts` wired into the existing per-kind validation pass: shape check (keys/values against vocabulary) → cross-ref resolution (`period:` via entity-ref resolution; `currency:` via sibling-attribute resolution — follow `name_attribute` resolution as the pattern) → type-constraint check against the attribute's declared `type` → per-entity aggregation checks (completeness, event_date cardinality, geo/valid pairs). Populate `ResolvedSemantics` only when diagnostics-free for that element.
- [x] **T3.5 — Suggested alternatives.** Nearest-match (Levenshtein ≤ 2) over the closed vocabulary for 200/201/202, reusing whatever suggestion helper the existing diagnostics use (grep for the search-block or TTRP `suggested-alternative` helper before writing a new one).
- [x] **T3.6 — Green + integration.** `pnpm --filter @tatrman/semantics test` green; add one LSP-level test in `tests/integration/` (PassThrough-paired-connection harness): open a doc with `role: amout` → diagnostic TTR-SEM-201 with suggestion arrives via `didOpen`. `pnpm -r lint && pnpm -r typecheck`.
