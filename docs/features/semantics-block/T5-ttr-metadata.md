# T5 — ttr-metadata typed-model surface + publish

Pre-flight: T4 green. ttr-metadata pins ttr-parser/semantics versions and re-exports them as `api` deps (contracts.md §1) — bump the pins to the 4.2 builds first.

- [x] **T5.1 — Specs first.** In `packages/kotlin/ttr-metadata` add `SemanticsSurfaceSpec` (Kotest, loads an annotated fixture model via `LocalFsStorage`): (a) `Attribute` model objects expose `semantics: ResolvedSemantics?`; (b) `Entity`/`DbTable` expose `semanticsKind`; (c) `MetadataQuery` answers `periodTableFor(pkg)`, `semanticRole(attrQname)`, `attributesByRole(pkg, role)`, `poiEntities(pkg)`, `fxRateTableFor(pkg)`; (d) a model whose semantics blocks have diagnostics loads with those as `LoadIssue`s and WITHOUT the offending `ResolvedSemantics` (degrade, don't fail). All red. — _9 tests, loads golden `59-semantics.ttrm` (er) + `60-semantics-db.ttrm` (db) as clean input + an inline invalid model for (d)._
- [x] **T5.2 — Typed model.** Extend `ModelObject` variants (`Entity`, `Attribute`, `DbTable`, `DbColumn`) with the resolved-semantics fields, populated in the load pipeline (parse → reconcile → resolve) from ttr-semantics' `ResolvedSemantics`. Immutable, snapshot semantics as everywhere in the library. — _`Entity`/`DbTable` carry `semanticsKind: String?` (T4 resolved `kind` as `String`, not an enum); `Attribute`/`DbColumn` carry `semantics: ResolvedAttributeSemantics?`. Populated in `Source.kt` from a per-document `SemanticsAnalyzer.analyzeSemantics(...).resolved`._
- [x] **T5.3 — MetadataQuery accessors.** Implement the five accessors from T5.1(c) on the query facade (these are what ai-platform's Ariadne gRPC layer and the grounding services' metadata lookups will call). Kdoc each with its grounding use-case. — _Return types made consistent/useful: `periodTableFor`/`fxRateTableFor` → `ModelObject?`, `poiEntities`/`attributesByRole` → `List<ModelObject>` (period/POI/fx tables may be er `Entity` OR db `DbTable`; roles live on `Attribute` OR `DbColumn`); `semanticRole` → `String?`._
- [x] **T5.4 — LoadIssue taxonomy.** Map TTR-SEM-2xx diagnostics into `LoadIssue` (id-free taxonomy per the extraction design) with source positions; test T5.1(d) goes green. — _New `LoadIssue.Category.SEMANTICS_INVALID`, matched off a leading `TTR-SEM-\d{3}`; the loader emits each diagnostic as a `LoadWarning` (`"<code>: <message>"`, source line/column) that categorizes to it._
- [ ] **T5.5 — Version + publish.** Bump lockstep pins; update `PUBLISHING.md` rows if wording drifted; tag `kotlin/v0.9.0` (parser/writer/semantics bundle with grammar 4.2) then `kotlin-metadata/v0.2.0`. Verify both resolve from GitHub Packages in a scratch consumer (or Maven Local first per the local-iteration flow). — _Pins: none to bump — versions are purely tag-driven via `-Pversion` (root `build.gradle.kts`); ttr-metadata depends on ttr-parser/writer/semantics as in-repo `api(project(...))` deps, so they build lockstep. `PUBLISHING.md` wording unchanged (no drift). **Tags left for the human** (do-not-tag)._
- [x] **T5.6 — Changelog + docs.** ttr-metadata `contracts.md` changelog entry (new API surface — the five accessors + model fields); grammar CHANGELOG cross-link; mark T5 done in README task list. — _contracts.md v1.5 entry + §2 API additions; grammar `CHANGELOG.md` 4.2 gains a downstream cross-link item; these T5 boxes ticked._
