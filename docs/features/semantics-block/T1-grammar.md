# T1 — Grammar 4.2 + regeneration

Pre-flight: worktree off `master` exists (`git worktree add ../tatrman-semantics master && git switch -c feature/semantics-block`); `pnpm install` green there.

- [ ] **T1.1 — Claim the version.** `packages/grammar/CHANGELOG.md`: add the `## 4.2` entry (additive; `semantics` block per `docs/features/semantics-block/README.md`). Bump `// @grammar-version: 4.2` in `packages/grammar/src/TTR.g4` line 4. Commit immediately (reserves 4.2 against parallel work).
- [ ] **T1.2 — Negative fixtures first (TDD).** Under `tests/conformance/fixtures/` add parser-reject roster `semantics-negative/`: `neg-01` `semantics` block on a `relation` (not attachable), `neg-02` `semantics` on `query`, `neg-03` bare `semantics` without a block (`semantics: true`) — expect parse errors. Add golden fixture `59-semantics.ttrm` containing the full README illustration (entity + attribute + table + column attachments, every role once, incl. `due_date`, `calendar_date`, an fx_rate table). Run the conformance suite — must FAIL (rules absent).
- [ ] **T1.3 — Lexer.** Add `SEMANTICS : 'semantics' ;` in the keyword section of `TTR.g4` (beside `SEARCH`). Add `SEMANTICS` to the `idPart` alternatives (4.1 `WORLD`/`ENGINE` precedent — keeps files using `semantics` as an identifier parsing; verify with a fixture `def attribute semantics { type: int }` in the golden roster).
- [ ] **T1.4 — Parser rule.** `semanticsBlockProperty : SEMANTICS propSep? object_ ;` next to `searchBlockProperty` (~line 464). Add `semanticsBlockProperty` to exactly four property alternatives: `tableProperty` (295), `columnProperty` (299), `entityProperty` (309), `attributeProperty` (314). Do NOT add to view/relation/query/role. Comment the rule: free-form body, shape-checked in semantics (cite `attributesMapProperty` precedent).
- [ ] **T1.5 — Regenerate TS parser.** `cd packages/parser && pnpm run prebuild` (antlr-ng via `packages/grammar/scripts/generate-typescript-parser.sh`); `pnpm --filter @tatrman/parser build`. Conformance parse of `59-semantics.ttrm` now succeeds; negatives reject.
- [ ] **T1.6 — TextMate grammar.** `cd packages/vscode-ext && node scripts/generate-tm-grammar.ts`; verify `semantics` highlights as keyword in `syntaxes/ttr.tmLanguage.json`; commit the regenerated file (it IS committed, unlike generated parsers).
- [ ] **T1.7 — Verify + lint.** `pnpm -r typecheck && pnpm --filter @tatrman/grammar test && pnpm --filter @tatrman/parser test` green; `TTR_GRAMMAR_VERSION` export now reads `4.2` (assert in an existing version test). Tick all boxes; hand to T2.
