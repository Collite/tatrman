# PL-P2 (③) — The hall: Kyklop + workers transplant · secrets at dispatch (stages S5–S6)

> Pre-flight: S4 done (an executor exists to call the hall); kantheon donor commit **re-pinned and recorded** in `TRANSPLANTS.md`. DoD: [`../plan.md`](../plan.md) §PL-P2. Check each box the moment its task is done. Transplant discipline (D-5 mini-arc): **move → adopt → delete** — the kantheon-side delete of moved modules closes this phase's arc; kantheon's mini-spine (its own theseus/argos path) stays untouched until ⑥.

## S5 · Kyklop + Cyclopes workers transplant {#s5}

Verify: `./gradlew build` green in `tatrman-platform` (all four moved modules + suites); dependency-rule test still green; a dispatched fixture plan executes on each worker kind in the component lane (Testcontainers PG + MSSQL, local Polars).

- [ ] **T1.** `git filter-repo` moves from the pinned kantheon commit into `tatrman-platform`: `services/kyklop` → `services/kyklop`, `workers/{arges,brontes,steropes}` → `workers/…`, plus their proto slices `shared/proto/src/main/proto/org/tatrman/{kyklop,worker}` → a new `proto/hall-proto` module. History-preserving; record source commit + paths in `TRANSPLANTS.md` (S5 template from PL-P1).
- [ ] **T2.** DQ-2 sweep: proto packages `org.tatrman.{kyklop,worker}.*` → `cz.tatrman.{kyklop,worker}.*` (platform-owned per D-3 — the donor's `org.tatrman` roots are wrong post-split), Kotlin package roots likewise; wire modules into the version catalog; transplanted test suites green unmodified in behavior.
- [ ] **T3 (tests first).** `CapabilityRoutingTest.kt` — the **CQ-4 mapping layer** (contracts §1 row 22): `"a T6 worker capability manifest (fixture: arges postgres-16) maps to Kyklop's routing vocabulary via the mapping table"` · `"an island whose engine no routed worker supports → structured routing error naming engine + candidates"` · `"mapping is data (config), not code — adding a worker kind touches no dispatcher source"`. Write against a not-yet-existing `CapabilityMapping` class.
- [ ] **T4 (tests first).** `QuotaDisciplineTest.kt` — F-5-γ: `"interactive outranks batch at the dispatch slot"` · `"batch yields: a queued interactive plan dispatches before queued batch"` · `"running batch work is never preempted"` · `"weighted-fair floor: batch share never starves below the configured floor"` · `"pool labels (deployment config) partition dispatch when set"`.
- [ ] **T5.** Implement `CapabilityMapping` (config-driven table per T3) in Kyklop's admission path; the executor's dispatch call carries the island's engine/executor pair from the manifest.
- [ ] **T6.** Implement F-5-γ priority + admission in Kyklop (two classes: `interactive` | `batch`; program-door dispatches tag `batch`, query-door `interactive`; floor + labels from config). Radegast's FakeHall swaps for the real Kyklop client behind the same interface — S4's component suite re-runs unchanged.
- [ ] **T7.** Helm charts for kyklop + three workers; `tatry clusters/local` roster grows; **kantheon-side delete PR** for the moved modules opened as DRAFT and parked — kantheon's mini-spine still builds and runs these until the ⑥ adoption arc (D-5: accepted, time-boxed, P2-legal duplication; the mini-arc's delete leg executes in PL-P6.S2).
- [ ] **T8.** Run Verify, check tracker boxes, commit `PL-P2.S5: Kyklop + Cyclopes transplant, CQ-4 mapping, F-5 quotas`.

## S6 · Secret-store SPI + dispatch injection + canary suite {#s6}

Verify: `./gradlew :shared:secrets-spi:test :services:kyklop:test` green; the **canary CI job** is green and provably sensitive (temporarily plant a leak, watch it fail, remove the plant — record in the PR).

- [ ] **T1 (tests first).** `SecretRefTest.kt`: `"secret://k8s/arges-prod parses to {store:k8s, path:arges-prod}"` · malformed URIs rejected · `SecretMaterial.toString() == "SecretMaterial(REDACTED)"` · `"zeroize clears the backing array"` (contracts §17).
- [ ] **T2 (tests first).** `DispatchInjectionTest.kt` (component, fake store): `"ONLY the island's manifest-declared connections resolve and inject as TTR_CONN_*"` (an undeclared connection in the world is never resolved) · `"store unreachable → island fails PRE-FLIGHT with PLT-SEC-001, retryable per manifest, before any worker invocation"` · `"resolution happens at dispatch, material lives only in the invocation env"` (fake store records resolution timestamps vs dispatch).
- [ ] **T3.** Implement `shared/secrets-spi` (contracts §17 interfaces verbatim, ServiceLoader registry) + the `k8s` binding (fabric8 or official client — smallest dependency wins; read-only Secret access, namespace from deployment config).
- [ ] **T4.** Wire injection into Kyklop's worker invocation path (env assembly = params from S4 + `TTR_CONN_*` from here; one assembly point, unit-tested) — the F-lite contract verbatim, both modes identical (H-5 parity).
- [ ] **T5.** **No-secret-API guard:** component test walking every registered Radegast/Kyklop/Veles route asserting no response body/header ever contains a resolved `SecretMaterial` (serializer-level: `SecretMaterial` has NO serializer registered — compilation-enforced; test asserts attempting to serialize throws).
- [ ] **T6.** The **H-5 canary CI job** (contracts §21): deploy the hero envelope with `TTR_CONN_CANARY=canary-<random-fixed>` bound via a fixture store; run it; grep the canary bytes across envelope rows, run-store dump, emitted events, service logs, and all artifacts — assert ZERO hits. Wire into the platform CI as a required check from this commit on.
- [ ] **T7.** Run Verify, check tracker boxes, commit `PL-P2.S6: secret-store SPI + dispatch injection + canary suite`.
