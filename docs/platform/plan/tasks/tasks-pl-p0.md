# PL-P0 — Contract pinning + the S1 amendment batch

> Pre-flight: tracker gate checked. DoD: [`../plan.md`](../plan.md) §PL-P0. Check each box the moment its task is done. This is a **docs + spec phase** — "tests first" here means fixture/validation artifacts before prose; no behavior changes, all existing suites stay green. Everything lands in the `tatrman` repo.

## S1 · Record the S1 amendment batch {#s1}

Verify: `pnpm -r test` and `./gradlew build` green (no code change); the new JSON-Schema fixture test fails-then-passes; TTR-P contracts changelog carries ONE batch entry listing all seven amendments.

- [ ] **T1 (test first).** In `packages/kotlin/ttrp-emit/src/test/…`, add `BundleManifestV2SchemaTest` (FunSpec): loads `src/test/resources/fixtures/manifest-v2-hero.json` (write this fixture by hand from contracts §6 — hero program, params `run_date`, one `onFailureOf`, lineage section with one `aggregate:SUM` column; **no provenance block** — provenance rides the envelope + sidecar, contracts §5) and validates it against `packages/kotlin/ttrp-emit/src/main/resources/schemas/bundle-manifest-v2.schema.json` using `com.networknt:json-schema-validator`. Write against the not-yet-existing schema — watch it fail.
- [ ] **T2.** Author `bundle-manifest-v2.schema.json` (JSON Schema draft 2020-12) transcribing contracts §6 exactly: `schemaVersion` const 2; per-island `retries`/`onFailureOf`/`connections`/`params`; top-level `params[]` (name/type/required/default), `lineage` (version, columns[].output/inputs/transform with the ⚑ transform-tag enum). No provenance property (contracts §6 rule). T1 goes green. Note: the schema *permits* `params`/`onFailureOf` from day one; the toolchain *emits* them only from PL-P2 (grammar lands there).
- [ ] **T3.** Amend `docs/ttr-p/architecture/contracts.md`: new §"Bundle manifest v2 (E-5 graduation)" referencing the schema file + the F-4 vocabulary (params F-4-i, retries, wave-resume guard, on-failure F-4-iv — semantics verbatim from platform `contracts.md` §6/§7); mark manifest v1 as "accepted by doors during the SZ-3 window".
- [ ] **T4.** Record the **FQ-4 executor manifest** and the **T6 format pin + ownership amendment** in `docs/ttr-p/architecture/contracts.md`: type manifests are TTR-M documents; core engine types ship with the compiler; **executor types ship with their emit plugin** (amends the original "ship with the compiler" wording — say so explicitly); include the `def executor tatrman {…}` and bash-subset artifacts verbatim from platform contracts §7.
- [ ] **T5.** Record the **K `extends`-platform-world grammar surface** (T4-adjacent): world docs may declare `extends: "<platform-world-qname>"` at `def world` level; composition semantics (platform authoritative, add/extend legal, contradiction = compile error) per platform contracts §3/§16. File the grammar-master work item (`docs/grammar-master/` process) with the spec text; implementation lands PL-P1.S4.
- [ ] **T6.** Record the **H-1 `security`-block grammar reservation** in TTR-M contract docs (shape from platform contracts §11: `own`/`classify`/`grant read`/`mask`; deny-overrides; fingerprint-neutral; generator one-way). File its grammar-master work item; implementation lands PL-P4.
- [ ] **T7.** Cut ONE changelog entry in `docs/ttr-p/architecture/contracts.md` (the S1 batch: F-4-i · F-4-iv · FQ-4 · E-5 v2 + lineage · T6 ownership+format · K extends · H-1 reservation), cross-linking `docs/platform/plan/contracts.md`. Run Verify, check tracker boxes, commit `PL-P0.S1: record the S1 amendment batch (TTR-P/TTR-M contracts)`.

## S2 · Ratify platform contracts + repo lines {#s2}

Verify: `grep -n "⚑" docs/platform/plan/contracts.md` returns only entries marked ratified/amended in the v2 changelog; markdown links resolve (`npx markdown-link-check` over `docs/platform/plan/*.md`); suites still green.

- [ ] **T1.** Walk every ⚑ in `docs/platform/plan/contracts.md` with Bora (async review is fine — collect verdicts in the PR). Apply amendments; where a ⚑ is accepted unchanged, mark it "⚑ ratified v2". Cut contracts changelog **v2** listing each verdict.
- [ ] **T2.** Apply **S6**: edit the tatrman `CLAUDE.md` line "this repo … never talks to those services" → "the MIT toolchain is a client of the platform's published snapshot contract (`ttr fetch`, `MetadataServerSource`); it never talks to a server *mid-compile*."
- [ ] **T3.** Add the planning set to the docs map: link `docs/platform/plan/{architecture,contracts,plan}.md` from `docs/platform/design.md` §12 (one line, "planning artefacts") and from the control room close-out note.
- [ ] **T4.** Create `docs/platform/implementation/` with an empty `reviews.md` stub noting the serial `/review` numbering convention (shared with the repo's other efforts).
- [ ] **T5.** Confirm the four grammar-master work items from S1 (params, on-failure, extends-world, security-block) are filed with owners + target phases (PL-P1.S4 / PL-P2 / PL-P2 / PL-P4). Record the mapping in `docs/grammar-master/` index.
- [ ] **T6.** Capture the **life-1 conformance baseline**: run the full `ttrp conform` corpus and record its fingerprints to `docs/platform/implementation/baseline-p0.json` (the PL-P7.S5.T4 acceptance diff target). Run Verify, check tracker boxes (S2 + the phase row), commit `PL-P0.S2: ratify platform contracts v2 + S6 CLAUDE.md line + conformance baseline`. Request the PL-P0 `/review`.
