[project]
name = "retail-shop"

[language]
preferred = "en"

[schemas]
declared = ["db", "er", "map"]
namespaces = { db = "dbo", er = "entity", map = "er2db" }

[stock]
load = ["cnc-roles"]

[lint]
strict = false
require-descriptions = false
