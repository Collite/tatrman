Load file "C:\fff.csv".
Load file C:\fff.csv.
Load file fff.xlsx as fifa.
Rename amount as amt.
Rename amount as amt, customer as cust, trx as tr.
Change the name of amount to amt.
Change the type of amount to float.
Remove the records where amount < 100.
Create new summary by customer summarize sum(amount).
Create new summary by customer summarize sum(amount), avg(transaction) as aver.
Create new summary by customer summarize amount and transaction.
Kaspar select where amount > 0.
Select where amount >>> 0.
Select where amount > 0 poser se klidne celej.
Amount > 0.
Amount, customer and transaction.
Amount as amt, customer as cust.
Amount * 1.2 as more_amount.
Sum(amount) as sum_amt
Amount * 1.2 as more_amt, Amount * 1.4 as even_more.


