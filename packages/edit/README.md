# @modeler/edit

WorkspaceEdit type re-export from `vscode-languageserver-types`.

## Rationale

The edit synthesizer (WorkspaceEdit -> structured graph ops) lands in v1.1.
This package provides the type placeholder so downstream packages can import it.