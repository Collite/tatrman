container frag_sql target erp_pg """sql
-- a SQL comment
select id from t where id > 0
"""

container frag_pd target polars """pandas
# a pandas comment   
df.filter(amount > 0)
# d = { 'k': 1 }

"""

container frag_b target polars """ttrb
# a ttrb comment
Load sales as sales.
Keep the rows where amount is more than 0.
"""
