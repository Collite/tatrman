// expect: syntax error — `def engine` is valid ONLY inside `def world`
// (engine/executor/storage/world-schema are grammar-nested; D-d-α).
package acme.worlds
model world

def engine erp_pg { type: postgres }
