apply target=dim_customer verb=entry.effective-date-change semantics=scd2 program=dim_customer-entry-apply@0.0.0-dev
proposal 1:
  steps:
    [CLOSED] UPDATE "dim_customer" SET "valid_to" = ? WHERE "customer_id" = ? AND "valid_to" IS NULL
      binds: DATE(2026-01-01), BIGINT(1)
    [INSERTED] INSERT INTO "dim_customer" ("customer_id", "customer_name", "valid_from") VALUES (?, ?, ?)
      binds: BIGINT(1), TEXT(Acme), DATE(2026-01-01)
