package org.tatrman.ttr.semantics

/**
 * Scaffolding placeholder so the module compiles before the real types land.
 *
 * Removed in stage 2.3 once [Qname], [SymbolTable], etc. exist. See
 * `docs/grammar-master/tasks/phase-2/`.
 */
internal const val TTR_SEMANTICS_SCAFFOLD = true
