apply target=raw_notes verb=entry.update-rows semantics=optimistic
proposal 1:
  reads:
    currentVersion = CurrentRowVersion(table=raw_notes, versionCol=row_version, key={k=key.k})
  steps:
    OptimisticGuard(table=raw_notes, key={k=key.k}, base='v3', read=currentVersion)
    UpdateRow(table=raw_notes, key={k=key.k}, set={v=batch.v})
