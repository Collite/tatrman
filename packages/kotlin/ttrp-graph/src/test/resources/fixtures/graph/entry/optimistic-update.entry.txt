apply target=plain_notes verb=entry.update-rows semantics=optimistic
proposal 1:
  reads:
    currentVersion = CurrentRowVersion(table=plain_notes, key={note_id=key.note_id})
  steps:
    OptimisticGuard(table=plain_notes, key={note_id=key.note_id}, base='v3', read=currentVersion)
    UpdateRow(table=plain_notes, key={note_id=key.note_id}, set={body=batch.body})
