# Connect

> **I build agents.**

Tatrman speaks MCP. Your agent asks questions through a small, versioned surface
of doors and tools, forwards the end user's identity, and gets back an answer
*plus* the trail of everything the platform did to keep that answer legal.

This track is the contract, written for you rather than for us.

## What will live here

<!-- TODO(S6): this track is mcp-surface.md (RO-25) rewritten for the OUTSIDE
     reader — reference genre is the backbone, the tutorial and the framework
     how-tos wrap it. The conformance fixtures double as worked examples: each
     fixture is a real request/response pair we already assert on, so they cannot
     rot into fiction. -->

- **Your first agent against the door** — the tutorial: ask one question, read
  the envelope, see the provenance.
- **The doors and tools** — the reference: every tool, its arguments, its result
  envelope, and its refusal modes.
- **The identity contract** — on-behalf-of: what your agent MUST forward, what
  comes back, and why an agent that guesses instead of refusing is a bug.
- **The compat rule** — what we may change under you, and what we may not.
- **Framework how-tos** — LangGraph, Koog, and MCP-capable assistants.
- **The conformance suite as your test harness** — the same fixtures we gate on,
  pointed at your agent.
