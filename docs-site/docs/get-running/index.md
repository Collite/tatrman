# Get running

> **I have a database; show me the promise.**

This track takes you from a database you already own to a governed answer with
its provenance attached — in under an hour, using nothing but public artifacts
and these pages.

Start with the **[Quickstart](quickstart.md)**. It is the whole promise in seven
steps, and every step tells you what you should see before you move on.

## What will live here

<!-- TODO(S6): this track is the acceptance run's script — the outsider executing
     the RO-3 dry run (S7) reads exactly these pages and nothing else. Every page
     below is drafted against the real chart in S6, not from memory. -->

- **[Quickstart](quickstart.md)** — the seven-step path to your first governed
  answer *(skeleton; the commands land in S6, verified against the chart)*.
- **Install on kind** — a local cluster from zero, for evaluating without a
  cloud account.
- **Connect your database** — the per-engine specifics (MSSQL, PostgreSQL):
  drivers, permissions, and what the importer needs to see.
- **Troubleshooting first contact** — the failures a stranger actually hits on
  the first attempt, and what each one means.
